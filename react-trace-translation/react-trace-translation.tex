\documentclass{article}
\usepackage[utf8]{inputenc}

\usepackage{mathpartir}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage[standard,thref,thmmarks]{ntheorem}
\usepackage{listings}
\usepackage{stmaryrd}
\usepackage{parskip}
\usepackage{mathtools}
\usepackage[x11names]{xcolor}
\usepackage{enumitem}

\newcommand{\num}[1]{\overline{#1}}
\newcommand{\mf}[1]{\mathsf{#1}}
\newcommand{\mc}[1]{\mathcal{#1}}
\newcommand{\mb}[1]{\mathbf{#1}}
\newcommand{\tb}[1]{\textbf{#1}}
\newcommand{\tf}[1]{\textsf{#1}}
\newcommand{\ts}[1]{\textsc{#1}}
\newcommand{\tn}[1]{\textnormal{#1}}
\newcommand{\semi}{\,;\,}
\newcommand{\dom}{\mf{dom}}
\newcommand{\po}{\sqsubseteq}
\newcommand{\rpo}{\sqsupseteq}

\newcommand{\mhl}[1]{\colorbox{LightGoldenrod1}{$#1$}}
\newcommand{\hl}[1]{\colorbox{LightGoldenrod1}{#1}}
\newcommand{\grey}[1]{\colorbox{lightgray}{#1}}
\newcommand{\mgrey}[1]{\colorbox{lightgray}{$#1$}}
\newcommand{\llrr}[1]{\llbracket #1 \rrbracket}

\newcommand{\pair}[2]{\langle #1, #2 \rangle}
\newcommand{\trip}[3]{\langle #1, #2, #3 \rangle}
\newcommand{\four}[4]{\langle #1, #2, #3, #4 \rangle}
\newcommand{\map}[2]{[\num{#1 \mapsto #2}]}
\newcommand{\arrow}[1]{\xrightarrow{#1}}

\newcommand{\ptp}{A}
\newcommand{\rtp}{\alpha}

\newcommand{\tval}[4]{#1 \semi #2 \vdash_\Delta #3 : #4}
\newcommand{\texp}[5]{#1 \semi #2 \vdash_\Delta #3 : #4 \mid #5}
\newcommand{\tbod}[5]{#1 \semi #2 \vdash_\Delta #3 : #4\{#5\}}
\newcommand{\tprog}[3]{#1 \vdash_\Delta #2: #3}
\newcommand{\tmem}[3]{#1 \semi #2 \vDash_\Delta #3}
\newcommand{\tdef}[3]{#1 \semi #2 \vDash #3}
\newcommand{\trho}[5]{#1 \semi #2 \semi #3 \Vdash_\Delta #4 : #5}
\newcommand{\tcfg}[7]{#1 \semi #2 \semi #3 \Vdash_\Delta \trip{#4}{#5}{#6} : #7}

\newcommand{\vdval}{\vdash_\Delta}
\newcommand{\vdexp}{\vdash_\Delta}
\newcommand{\vdtop}{\vdash^{\mf{Top}}_\Delta}
\newcommand{\vdprog}{\vdash^{\mf{Prog}}_\Delta}
\newcommand{\vdrt}{\Vdash_\Delta}
\newcommand{\vdtree}{\vdash^{\mf{Tree}}_\Delta}
\newcommand{\vdmu}{\vdash^{\mf{Mode}}_\Delta}
\newcommand{\vdcfg}{\vdash^{\mf{Cfg}}_\Delta}
\newcommand{\vdm}{\vDash_\Delta}
\newcommand{\vdd}{\vDash}
\newcommand{\wf}{\vdash^{\mf{wf}}_\Delta}
\newcommand{\vdashv}{\vdash_v}
\newcommand{\vdashe}{\vdash_e}

\newcommand{\topstep}[2]{\mapsto^{#1}_{#2}}
\newcommand{\Topstep}[2]{\Mapsto^{#1}_{#2}}
\newcommand{\bsinit}[1]{\Downarrow^{\mf{Init}}_{#1}}
\newcommand{\divinit}[1]{\Uparrow^{\mf{Init}}_{#1}}
\newcommand{\bssucc}[1]{\Downarrow^{\mf{Succ}}_{#1}}
\newcommand{\divsucc}[1]{\Uparrow^{\mf{Succ}}_{#1}}

\newcommand{\tone}{\mb{1}}
\newcommand{\tstr}{\mf{String}}
\newcommand{\tview}{\mf{View}}
\newcommand{\tattr}{\mf{Attr}}
\newcommand{\tcomp}[1]{\mf{Comp}\ #1}
\newcommand{\tset}[1]{\mf{Setter}\ #1}
\newcommand{\emut}{\mf{Mut}}
\newcommand{\ttree}{\mf{Tree}}

\renewcommand{\tt}{\mf{tt}}
\newcommand{\ff}{\mf{ff}}

\newcommand{\estep}[4]{#1 \semi #2 \mapsto #3 \semi #4}
\newcommand{\bstep}[6]{#1 \semi #2 \mapsto^{#3}_{#4} #5 \semi #6}
\newcommand{\pstep}[4]{#1 \semi #2 \mapsto #3 \semi #4}
\newcommand{\cstep}[6]{\trip{#1}{#2}{#3} \hookrightarrow \trip{#4}{#5}{#6}}
\newcommand{\zstep}[6]{\trip{#1}{#2}{#3} \hookrightarrow_0 \trip{#4}{#5}{#6}}

\newcommand{\C}{\mc{C}}
\newcommand{\vtxt}[1]{\mf{text}\ #1}
\newcommand{\vtag}[2]{\mf{tag}(#1, #2)}
\newcommand{\vattr}[2]{\mf{attr}(#1, #2)}
\newcommand{\vclick}[1]{\mf{onClick}\ #1}
\newcommand{\vset}[2]{\mf{set}@^{#1}_{#2}}
\newcommand{\D}{\mc{D}}
\newcommand{\E}{\mc{E}}

\newcommand{\elet}[3]{\mf{let}\ #1 = #2\ \mf{in}\ #3}
\newcommand{\eletrec}[3]{\mf{let}\ \mf{rec}\ #1 = #2\ \mf{in}\ #3}
\newcommand{\erun}[1]{\mf{run}\ #1}
\newcommand{\eif}[3]{\mf{if}\ #1\ \mf{then}\ #2\ \mf{else}\ #3}
\newcommand{\eret}[1]{\mf{return}\ #1}
\newcommand{\ecase}[5]{\mf{case}\ #1\ \{#2 \Rightarrow #3 \mid #4 \Rightarrow #5\}}
\newcommand{\estate}[4]{\mf{let}\ (#1, #1_{\mf{set}}) = \mf{useState}^{#2}(#3)\ \mf{in}\ #4}
\newcommand{\eref}[1]{\mf{ref}(#1)}
\newcommand{\eget}[1]{\ !#1}
\newcommand{\eset}[2]{#1 := #2}
\newcommand{\vrec}[4]{\mf{rec}\ #1(#2 : #3).#4}
\newcommand{\vcomp}[4]{\mf{comp}\ #1(#2 : #3).#4}

\newcommand{\elit}[1]{\mf{lit}\ #1}
\newcommand{\enode}[3]{\mf{node}(\mf{#1}, #2, #3)}
\newcommand{\esearch}[2]{\mf{search}_\mf{#1}\ #2}
\newcommand{\esearches}[2]{\mf{searches}_\mf{#1}\ #2}
\newcommand{\eeval}[3]{\mf{eval}^{#1}_{#2}\ #3}
\newcommand{\enew}[3]{\mf{new}^{#1}_{#2}\ #3}
\newcommand{\ehdl}[1]{\mf{handle}\ #1}
\newcommand{\eprog}[1]{\mf{prog}\ #1}
\newcommand{\einit}[1]{\mf{init}\ #1}
\newcommand{\einits}[1]{\mf{inits}\ #1}
\newcommand{\escan}[1]{\mf{scan}\ #1}
\newcommand{\escans}[1]{\mf{scans}\ #1}
\newcommand{\echk}[1]{\mf{check}\ #1}
\newcommand{\echks}[1]{\mf{checks}\ #1}



\title{React-tRanslation}
\author{Toby Ueno}

\begin{document}

\maketitle

\section{React-tRace Core}

\subsection{Program-Level Syntax}

Throughout, constructs which only appear in intermediate stages 
of evaluation are in \grey{grey}.
Other non-standard/noteworthy constructs are \hl{highlighted}.
\begin{align*}
\text{Value } v &::= x \mid () \mid s \mid \lambda x.e \mid [] \mid v :: v\\
&\quad\mid \mhl{\C} \mid \mhl{\vtxt{v}} \mid \mhl{\vtag{v}{v}}
\mid \mhl{\vattr{v}{v}} \mid \mhl{\vclick{v}}\\
&\quad\mid \mgrey{\pair{\C}{v}} \mid \mgrey{\vset{\ell}{p}}\\
\text{Expression } e &::= \eret{v} \mid \elet{x}{e}{e} \mid v\ v \\
&\quad\mid \ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}\\
\text{Body }\hat{e} &::= e \mid \mhl{\estate{x}{\ell}{v}{\hat{e}}} \mid \elet{x}{e}{\hat{e}}\\
\text{Program } P &::= e \mid \elet{x}{e}{P} \mid \mhl{\elet{\C(x)}{\hat{e}}{P}}\\
\text{Type } \ptp &::= \tone \mid \tstr \mid \ptp \arrow{E} \ptp
\mid [\ptp] \\
&\quad\mid \mhl{\tview} \mid \mhl{\tattr}
\mid \mhl{\tcomp{\ptp}} \mid \mhl{\tset{\ptp}}\\
\text{Effect } E &::= \epsilon \mid \mhl{\emut}
\end{align*}

\emph{\tb{Internal Representations}}.
\begin{align*}
\text{Boolean flag }b &::= \tt \mid \ff\\
\text{Type context }\Gamma &::= \map{x}{\ptp}\\
\text{Label set }\Sigma &::=\{\num{\ell}\}\\
\text{Component set }\Omega &::=\{\num{\C}\}\\
\text{Path context }\Pi &::= \map{p}{\{
    \mf{spec}: \C, \mf{labs}: \Sigma, \mf{tree}: b
\}}\\
\text{Component context }\Delta &::= \map{\C}{\pair{\ptp}{\map{\ell}{\ptp}}}\\
\text{Phase }\phi &::= \mf{Init} \mid \mf{Succ}\\
\text{Realized component }\pi &::= \{\mf{spec}: \pair{\C}{v}, 
\mf{st}: \map{\ell}{v}, \mf{tree}: u, \mf{rerender}: b\}\\
\text{Tree memory }m &::= \map{p}{\pi}\\
\text{Component definitions }\D &::= \map{\C}{x.\hat{e}}
\end{align*}
We elide the grammar of "runtime values" $u$,
which are distinct from normal values $v$, until a later section.

\emph{\tb{Notation}}.

We overload indexing notation on $\Delta$ for brevity,
i.e. $\Delta[\C]=\ptp$ and $\Delta[\C][\ell]=\ptp$.

We abbreviate $\Pi[p].\mf{spec}$ as simply $\Pi[p]$.

We use the notation $m \mid x = v$
to concisely denote nested map/record updates.
The above should be read as ``$m$ with field $x$ set to $v$''.
For instance, $m \mid m[p].\mf{st}[\ell] = v$ updates the
state field of $m$ at path $p$ and location $\ell$ to value $v$.

\emph{\tb{Ordering}}.

We define $\Pi \po \Pi'$ to hold when $\dom(\Pi) \subseteq \dom(\Pi')$
and, for all $p \in \mf{dom}(\Pi)$: \begin{itemize}
\item $\Pi[p] = \Pi'[p]$,
\item $\Pi[p].\mf{labs} \subseteq \Pi'[p].\mf{labs}$, and
\item if $\Pi[p].\mf{tree} = \mf{tt}$, then $\Pi'[p].\mf{tree} = \mf{tt}$.
\end{itemize}

$\po$ on all other contexts is given by standard subset inclusion.

\subsection{Program-Level Typing}

\subsubsection{Value Typing}

Judgment: $\tval{\Gamma}{\Pi}{v}{\ptp}$
\begin{mathpar}
\inferrule*[right=TV-Var] {
  \Gamma[x] = \ptp
} {
  \tval{\Gamma}{\Pi}{x}{\ptp}
}
\and
\inferrule*[right=TV-Unit] { } {
  \tval{\Gamma}{\Pi}{()}{\tone}
}
\and
\inferrule*[right=TV-Str] { } {
  \tval{\Gamma}{\Pi}{s}{\tstr}
}
\and
\inferrule*[right=TV-Lam] {
  \texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e}{\ptp_2}{E}
} {
  \tval{\Gamma}{\Pi}{\lambda x.e}{\ptp_1 \arrow{E} \ptp_2}
}
\and
\inferrule*[right=TV-Nil] { } {
  \tval{\Gamma}{\Pi}{[]}{[\ptp]}
}
\and
\inferrule*[right=TV-Cons] {
  \tval{\Gamma}{\Pi}{v_1}{\ptp}
  \and
  \tval{\Gamma}{\Pi}{v_2}{[\ptp]}
} {
  \tval{\Gamma}{\Pi}{v_1 :: v_2}{[\ptp]}
}
\and
\inferrule*[right=TV-Comp] {
  \Delta[\C] = \ptp
} {
  \tval{\Gamma}{\Pi}{\C}{\mf{Comp}\ \ptp}
}
\and
\inferrule*[right=TV-Text] {
  \tval{\Gamma}{\Pi}{v}{\tstr}
} {
  \tval{\Gamma}{\Pi}{\mf{text}\ v}{\mf{View}}
}
\and
\inferrule*[right=TV-Tag] {
  \tval{\Gamma}{\Pi}{v_1}{[\mf{Attr}]}
  \and
  \tval{\Gamma}{\Pi}{v_2}{[\mf{View}]}
} {
  \tval{\Gamma}{\Pi}{\mf{tag}(v_1, v_2)}{\mf{View}}
}
\and
\inferrule*[right=TV-Attr] {
  \tval{\Gamma}{\Pi}{v_1}{\tstr}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\tstr}
} {
  \tval{\Gamma}{\Pi}{\mf{tag}(v_1, v_2)}{\mf{Attr}}
}
\and
\inferrule*[right=TV-OnClick] {
  \tval{\Gamma}{\Pi}{v}{\tone \arrow{E} \tone}
} {
  \tval{\Gamma}{\Pi}{\mf{onClick}\ v}{\mf{Attr}}
}
\and
\inferrule*[right=\grey{TV-Spec}] {
  \tval{\Gamma}{\Pi}{v}{\Delta[\C]}
} {
  \tval{\Gamma}{\Pi}{\pair{\C}{v}}{\mf{View}}
}
\and
\inferrule*[right=\grey{TV-Set}] {
  \Delta[\Pi[p]][\ell] = \ptp
  \and
  \ell \in \Pi[p].\mf{labs}
} {
  \tval{\Gamma}{\Pi}{\vset{\ell}{p}}{\tset{\ptp}}
}
\end{mathpar}

\subsubsection{Expression Typing}

Judgment: $\texp{\Gamma}{\Pi}{e}{\ptp}{E}$
\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \tval{\Gamma}{\Pi}{v}{\ptp}
} {
  \texp{\Gamma}{\Pi}{\eret{v}}{\ptp}{\epsilon}
}
\and
\inferrule*[right=TE-Let] {
  \texp{\Gamma}{\Pi}{e_1}{\ptp_1}{E}
  \and
  \texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}
} {
  \texp{\Gamma}{\Pi}{\elet{x}{e_1}{e_2}}{\ptp_2}{E}
}
\and
\inferrule*[right=TE-AppL] {
  \tval{\Gamma}{\Pi}{v_1}{\ptp_1 \arrow{E} \ptp_2}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp_1}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\ptp_2}{E}
}
\and
\inferrule*[right=TE-AppC] {
  \tval{\Gamma}{\Pi}{v_1}{\tcomp{\ptp}}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\tview}{\epsilon}
}
\and
\inferrule*[right=TE-AppS] {
  \tval{\Gamma}{\Pi}{v_1}{\tset{\ptp}}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\tone}{\emut}
}
\and
\inferrule*[right=TE-Case] {
  \tval{\Gamma}{\Pi}{v}{[\ptp_1]}
  \and
  \texp{\Gamma}{\Pi}{e_1}{\ptp_2}{E}
  \\
  \texp{\Gamma[x_\mf{hd} \mapsto \ptp_1, x_\mf{tl} \mapsto [\ptp_1]]}
  {\Pi}{e_2}{\ptp_2}{E}
} {
  \texp{\Gamma}{\Pi}{\ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}}{\ptp_2}{E}
}
\and
\inferrule*[right=TE-Sub] {
  \texp{\Gamma}{\Pi}{e}{\ptp}{\epsilon}
} {
  \texp{\Gamma}{\Pi}{e}{\ptp}{\emut}
}
\end{mathpar}

\subsubsection{Body Typing}

Judgment: $\tbod{\Gamma}{\Pi}{\hat{e}}{\C}{\Sigma}$
\begin{mathpar}
\inferrule*[right=TB-Lift] {
  \texp{\Gamma}{\Pi}{e}{\tview}{\epsilon}
} {
  \tbod{\Gamma}{\Pi}{e}{\C}{\emptyset}
}
\and
\inferrule*[right=TB-Let] {
  \texp{\Gamma}{\Pi}{e}{\ptp}{\epsilon}
  \and
  \tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{e}{\hat{e}}}{\C}{\Sigma}
}
\and
\inferrule*[right=TB-State] {
  \Delta[\C][\ell] = \ptp
  \and
  \tval{\Gamma}{\Pi}{v}{\ptp}
  \\
  \tbod{\Gamma[x \mapsto \ptp, x_\mf{set} \mapsto \tset{\ptp}]}
  {\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\Gamma}{\Pi}{\estate{x}{\ell}{v}{\hat{e}}}{\C}{\Sigma \cup \{\ell\}}
}
\end{mathpar}

\subsubsection{Program Typing}

Program judgment: $\tprog{\Gamma}{P}{\Omega}$
\begin{mathpar}
\inferrule*[right=TP-Lift] {
  \texp{\Gamma}{\cdot}{e}{\tview}{\epsilon}
} {
  \tprog{\Gamma}{e}{\emptyset}
}
\and
\inferrule*[right=TP-Let] {
  \texp{\Gamma}{\cdot}{e}{\ptp}{\epsilon}
  \and 
  \tprog{\Gamma[x \mapsto \ptp]}{P}{\Omega}
} {
  \tprog{\Gamma}{\elet{x}{e}{P}}{\Omega}
}
\and
\inferrule*[right=TP-Def] {
  \tbod{\Gamma[x \mapsto \Delta[\C]]}{\cdot}{\hat{e}}{\C}{\dom(\Delta[\C])}
  \and
  \tprog{\Gamma}{P}{\Omega}
} {
  \tprog{\Gamma}{\elet{\C(x)}{\hat{e}}{P}}{\Omega \cup \{\C\}}
}
\end{mathpar}

\subsubsection{Memory Typing}

Define $\tmem{\Gamma}{\Pi}{m}$ to hold if $\dom(\Pi) = \dom(m)$
and, for all $p \in m$ (where $m[p].\mf{spec} = \pair{\C}{v}$):
\begin{itemize}
\item $\Pi[p] = \C$,
\item $\tval{\Gamma}{\Pi}{v}{\Delta[\C]}$,
\item $\dom(m[p].\mf{st}) = \Pi[p].\mf{labs}$,
\item $\forall \ell \in \dom(m[p].\mf{st})$,
$\tval{\Gamma}{\Pi}{m[p].\mf{st}[\ell]}{\Delta[\C][\ell]}$, and
\item if $\Pi[p].\mf{tree} = \tt$, then 
$\Gamma \semi \Pi \vdval m[p].\mf{tree}: \mf{Tree}$.
\end{itemize}

\subsubsection{Component Definition Typing}

Define $\tdef{\Gamma}{\Delta}{\D}$ to hold if,
for all $\C \in \D$ (where $\D[\C] = x.\hat{e}$),
we have $\tbod{\Gamma[x \mapsto \Delta[\C]]}{\cdot}{\hat{e}}{\C}{\dom(\Delta[\C])}$.

\subsection{Program-Level Semantics}

\subsubsection{Expression Semantics}

Judgment: $\estep{m}{e}{e'}{m'}$
\begin{mathpar}
\inferrule*[right=E-Let] {
  \estep{m}{e_1}{e_1'}{m'}
} {
  \estep{m}{\elet{x}{e_1}{e_2}}{\elet{x}{e_1'}{e_2}}{m'}
}
\and
\inferrule*[right=\hl{E-AppS}] {
  m' = m \mid m[p].\mf{st}[\ell] = v\ \mf{and}\ m[p].\mf{rerender} = \tt
} {
  \estep{m}{\vset{\ell}{p}\ v}{\eret{()}}{m'}
}
\end{mathpar}
\begin{align*}
\estep{m}{\elet{x}{\eret{v}}{e}&}{e[v/x]}{m}
&&\ts{E-Ret}\\
\estep{m}{(\lambda x. e)\ v&}{e[v/x]}{m}
&&\ts{E-AppL}\\
\estep{m}{\C\ v&}{\eret{\pair{\C}{v}}}{m}
&&\ts{\hl{E-AppC}}\\
\estep{m}{\ecase{[]}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}&}{e_1}{m}
&&\ts{E-CaseN}\\
\estep{m}{\ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}&}
{e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}]}{m}
&&\ts{E-CaseC}
\end{align*}

\subsubsection{Body Semantics}

Judgment: $\bstep{m}{\hat{e}}{\phi}{p}{\hat{e}'}{m'}$
\begin{mathpar}
\inferrule*[right=EB-Lift] {
  \estep{m}{e}{e'}{m}
} {
  \bstep{m}{e}{\phi}{p}{e'}{m}
}
\and
\inferrule*[right=EB-Let] {
  \estep{m}{e}{e'}{m}
} {
  \bstep{m}{\elet{x}{e}{\hat{e}}}{\phi}{p}{\elet{x}{e'}{\hat{e}}}{m}
}
\end{mathpar}
\begin{align*}
\bstep{m}{\elet{x}{\eret{v}}{\hat{e}}&}{\phi}{p}{\hat{e}[v/x]}{m}
&&\ts{EB-Ret}\\
\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}&}{\mf{Init}}{p}
{\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]}{m'}
&&\ts{\hl{EB-StI}}\\
(m' &= m \mid m[p].\mf{st}[\ell] = v)\\
\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}&}{\mf{Succ}}{p}
{\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}]}{m}
&&\ts{\hl{EB-StS}}\\
(v' &= m[p].\mf{st}[\ell])
\end{align*}

\subsubsection{Program Semantics}

Judgment: $\pstep{\D}{P}{P'}{\D'}$
\begin{mathpar}
\inferrule*[right=EP-Lift] {
  \estep{\cdot}{e}{e'}{\cdot}
} {
  \pstep{\D}{e}{e'}{\D}
}
\and
\inferrule*[right=EP-Let] {
  \estep{\cdot}{e}{e'}{\cdot}
} {
  \pstep{\D}{\elet{x}{e}{P}}{\elet{x}{e'}{P}}{\D}
}
\end{mathpar}
\begin{align*}
\pstep{\D}{\elet{x}{\eret{v}}{P}&}{P[v/x]}{\D}
&&\ts{EP-Ret}\\
\pstep{\D}{\elet{\C(x)}{\hat{e}}{P}&}{P}{\D[\C \mapsto x.\hat{e}]}
&&\ts{\hl{EP-Def}}
\end{align*}

\subsection{Program-Level Proofs}

Throughout, \emph{propositions} refer to (as-of-yet) unproved assumptions.

I denote \emph{deductions via inversion} with a label above the conclusion,
e.g.:
\begin{mathpar}
\inferrule* {(1)} {\tn{some judgment}}
\end{mathpar}

Comparatively, I denote the \emph{use of deductions} in a top-down derivation
with a label \emph{besides} the rule, e.g.:
\begin{mathpar}
\inferrule*[right=(1)] { } {\tn{some judgment}}
\end{mathpar}

\subsubsection{General Lemmas}

\begin{proposition}[Canonical Forms]
\label{thm:canon}
If $\tval{\cdot}{\Pi}{v}{\ptp}$, then:
\begin{itemize}
\item if $\ptp=\tone$, then $v=()$.
\item if $\ptp=\tstr$, then $v=s$.
\item if $\ptp=\ptp_1 \arrow{E} \ptp_2$, then $v=\lambda x.e$.
\item if $\ptp=[\ptp']$, then either $v=[]$ or $v = v_1 :: v_2$.
\item if $\ptp=\tview$, then either $v=\vtxt{v'}$,
    $v=\vtag{v_1}{v_2}$, or $v=\pair{\C}{v'}$.
\item if $\ptp=\tattr$, then either $v=\vattr{v_1}{v_2}$ 
    or $v=\vclick{v'}$.
\item if $\ptp=\tcomp{\ptp'}$, then $v=\C$.
\item if $\ptp=\tset{\ptp'}$, then $v=\vset{\ell}{p}$.
\end{itemize}
\end{proposition}

\begin{proposition}[Path context weakening, values.]
\label{thm:wkpiv}
If $\tval{\Gamma}{\Pi}{v}{\ptp}$ and $\Pi \po \Pi'$,
then $\tval{\Gamma}{\Pi'}{v}{\ptp}$.
\end{proposition}

\begin{proposition}[Substitution lemma, expressions.]
\label{thm:subexp}
If $\texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e}{\ptp_2}{E}$
and $\tval{\Gamma}{\Pi}{v}{\ptp_1}$,
then $\texp{\Gamma}{\Pi}{e[v/x]}{\ptp_2}{E}$.
\end{proposition}

\begin{proposition}[Substitution lemma, bodies.]
\label{thm:subbod}
If we have $\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}$
and $\tval{\Gamma}{\Pi}{v}{\ptp}$,
then $\tbod{\Gamma}{\Pi}{\hat{e}[v/x]}{\C}{\Sigma}$.
\end{proposition}

\begin{proposition}[Substitution lemma, programs.]
\label{thm:subprog}
If $\tprog{\Gamma[x \mapsto \ptp]}{P}{\Omega}$
and $\tval{\Gamma}{\cdot}{v}{\ptp}$,
then $\tprog{\Gamma}{P[v/x]}{\Omega}$.
\end{proposition}

\subsubsection{Expressions}

\begin{lemma}[Progress of expressions.]
\label{thm:progexp}
If $\texp{\cdot}{\Pi}{e}{\ptp}{E}$ and $\tmem{\cdot}{\Pi}{m}$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $e = \eret{v}$, or
\item there exist some $e'$ and $m'$ such that
$\estep{m}{e}{e'}{m'}$.
\end{enumerate}

\begin{Proof}
By induction on the derivation of the expression typing judgment.

\tb{Case} \ts{TE-Ret}: We have 
$\texp{\cdot}{\Pi}{\eret{v}}{\ptp}{\epsilon}$,
and the theorem holds trivially by case \emph{(i)}.

\tb{Case} \ts{TE-Let}: \begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e_1}{\ptp_1}{E}}
  \and
  \texp{[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}
} {
  \texp{\cdot}{\Pi}{\elet{x}{e_1}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item By inductive hypothesis on (1), we have two subcases on $e_1$:
either (a) $e_1 = \eret{v}$,
or (b) we have $\estep{m}{e_1}{e_1'}{m'}$.
\item In subcase (a), \ts{E-Ret} gives 
$\estep{m}{\elet{x}{\eret{v}}{e_2}}{e_2[v/x]}{m}$.
\item In subcase (b), we derive a step as follows:
\begin{mathpar}
\inferrule*[right=E-Let] {
  \inferrule*[right=(\tn{b})] { } {\estep{m}{e_1}{e_1'}{m'}}
} {
  \estep{m}{\elet{x}{e_1}{e_2}}{\elet{x}{e_1'}{e_2}}{m'}
}
\end{mathpar}
\item Thus, in any subcase, $\elet{x}{e_1}{e_2}$ steps,
and we satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppL}: \begin{mathpar}
\inferrule*[right=TE-AppL] {
  \inferrule* {(1)} {\tval{\Gamma}{\Pi}{v_1}{\ptp_1 \arrow{E} \ptp_2}}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp_1}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\ptp_2}{E}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), $v_1=\lambda x.e$.
\item We apply \ts{E-AppL} to get that
$\estep{m}{(\lambda x. e)\ v_2}{e[v_2/x]}{m}$,
satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppC}: \begin{mathpar}
\inferrule*[right=TE-AppC] {
  \inferrule* {(1)} {\tval{\Gamma}{\Pi}{v_1}{\tcomp{\ptp}}}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\tview}{\epsilon}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), $v_1 = \C$.
\item \ts{E-AppC} gives
$\estep{m}{\C\ v_2}{\eret{\pair{\C}{v_2}}}{m}$,
satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppS}: \begin{mathpar}
\inferrule*[right=TE-AppS] {
  \inferrule* {(1)} {\tval{\Gamma}{\Pi}{v_1}{\tset{\ptp}}}
  \and
  \tval{\Gamma}{\Pi}{v_2}{\ptp}
} {
  \texp{\Gamma}{\Pi}{v_1\ v_2}{\tone}{\emut}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), $v_1 = \vset{\ell}{p}$.
\item We apply \ts{E-AppS} to get that
$\estep{m}{\vset{\ell}{p}\ v}{\eret{()}}{m'}$,
where we define $m' = m \mid m[p].\mf{st}[\ell] = v_2$ 
and $m[p].\mf{rerender} = \tt$,
satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-Case}: \begin{mathpar}
\inferrule*[right=TE-Case] {
  \inferrule* {(1)} {\tval{\Gamma}{\Pi}{v}{[\ptp_1]}}
  \and
  \texp{\Gamma}{\Pi}{e_1}{\ptp_2}{E}
  \\
  \texp{\Gamma[x_\mf{hd} \mapsto \ptp_1, x_\mf{tl} \mapsto [\ptp_1]]}
  {\Pi}{e_2}{\ptp_2}{E}
} {
  \texp{\Gamma}{\Pi}{\ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), either 
(a) $v=[]$ or (b) $v=v_\mf{hd} :: v_\mf{tl}$.
\item (a) If $v=[]$, then \ts{E-CaseN} gives\\
$\estep{m}{\ecase{[]}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}{e_1}{m}$.
\item (b) If $v=v_\mf{hd} :: v_\mf{tl}$, then \ts{E-CaseC} gives\\
$\estep{m}{\ecase{v_\mf{hd} :: v_\mf{tl}}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}}
{e_2[v_\mf{hd} / x_\mf{hd}, v_\mf{tl} / x_\mf{tl}]}{m}$.
\item In either subcase, we satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-Sub}: \begin{mathpar}
\inferrule*[right=TE-Sub] {
  \inferrule* {(1)} {\texp{\Gamma}{\Pi}{e}{\ptp}{\epsilon}}
} {
  \texp{\Gamma}{\Pi}{e}{\ptp}{\emut}
}
\end{mathpar}
The result follows immediately from the inductive hypothesis,
applied to the (more conservative) premise (1).
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of expressions.]
\label{thm:prezexp}
If
\begin{enumerate}[label=(\roman*)]
\item $\texp{\Gamma}{\Pi}{e}{\ptp}{E}$,
\item $\tmem{\Gamma}{\Pi}{m}$, and
\item $\estep{m}{e}{e'}{m'}$ for some $e'$ and $m'$,
\end{enumerate}
then
\begin{enumerate}[label=(\roman*)]
\item $\texp{\Gamma}{\Pi}{e'}{\ptp}{E}$ and
\item $\tmem{\Gamma}{\Pi}{m'}$.
\end{enumerate}

\begin{Proof}
By induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{E-Let}: We have \begin{mathpar}
\inferrule*[right=E-Let] {
  \inferrule* {(1)} {\estep{m}{e_1}{e_1'}{m'}}
} {
  \estep{m}{\elet{x}{e_1}{e_2}}{\elet{x}{e_1'}{e_2}}{m'}
}
\and
\inferrule*[right=TE-Let] {
  \inferrule* {(2)} {\texp{\Gamma}{\Pi}{e_1}{\ptp_1}{E}}
  \and
  \inferrule* {(3)} {\texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}}
} {
  \texp{\Gamma}{\Pi}{\elet{x}{e_1}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item We apply the inductive hypothesis with (1), (2), and premise \emph{(ii)}
to get $\Gamma \semi \Pi \vdm m'$, satisfying conclusion \emph{(ii)}, and
(4) $\Gamma \semi \Pi \vdexp e_1': \ptp_1 \mid E$.
\item We reapply \ts{TE-Let} as follows to get conclusion \emph{(i)}:
\begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule*[right=(4)] { } {\texp{\Gamma}{\Pi}{e_1'}{\ptp_1}{E}}
  \and
  \inferrule*[right=(3)] { } {
    \texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}
  }
} {
  \texp{\Gamma}{\Pi}{\elet{x}{e_1'}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{E-Ret}: We have $\estep{m}{\elet{x}{\eret{v}}{e}}{e[v/x]}{m}$ and
\begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule* {(1)} {\texp{\Gamma}{\Pi}{\eret{v}}{\ptp_1}{E}}
  \and
  \inferrule* {(2)} {\texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}}
} {
  \texp{\Gamma}{\Pi}{\elet{x}{\eret{v}}{e}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item (1) lets us derive (3) $\tval{\Gamma}{\Pi}{v}{\ptp_1}$
by subcasing on the value of $E$:
\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \inferrule* {(3)} {\tval{\Gamma}{\Pi}{v}{\ptp_1}}
} {
  \texp{\Gamma}{\Pi}{\eret{v}}{\ptp_1}{\epsilon}
}
\and
\inferrule*[right=TE-Sub] {
  \inferrule*[right=TE-Ret] {
    \inferrule* {(3)} {\tval{\Gamma}{\Pi}{v}{\ptp_1}}
  } {
    \texp{\Gamma}{\Pi}{\eret{v}}{\ptp_1}{\epsilon}
  }
} {
  \texp{\Gamma}{\Pi}{\eret{v}}{\ptp_1}{\emut}
}
\end{mathpar}
\item By \thref{thm:subexp} (substitution for expressions)
on (2) and (3), we get
$\texp{\Gamma}{\Pi}{e[v/x]}{\ptp_2}{E}$ and we are done.
\end{itemize}

\tb{Case} \ts{E-AppL}: We have $\estep{m}{(\lambda x. e)\ v}{e[v/x]}{m}$ and
\begin{mathpar}
\inferrule*[right=TE-AppL] {
  \inferrule*[right=TV-Lam] {
    \inferrule* {(1)} {\texp{\Gamma[x \mapsto \ptp_1]}{\Pi}{e}{\ptp_2}{E}}
  } {
    \tval{\Gamma}{\Pi}{\lambda x.e}{\ptp_1 \arrow{E} \ptp_2}
  }
  \and
  \inferrule* {(2)} {\tval{\Gamma}{\Pi}{v}{\ptp_1}}
} {
  \texp{\Gamma}{\Pi}{(\lambda x. e)\ v}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:subexp} (substitution for expressions)
on (1) and (2), we get
$\texp{\Gamma}{\Pi}{e[v/x]}{\ptp_2}{E}$ and we are done.
\end{itemize}

\tb{Case} \ts{E-AppC}: We have $\estep{m}{\C\ v}{\eret{\pair{\C}{v}}}{m}$ and
\begin{mathpar}
\inferrule*[right=TE-AppC] {
  \inferrule*[right=TV-Comp] {
    \inferrule* {(1)} {\Delta[\C] = \ptp}
  } {
    \tval{\Gamma}{\Pi}{\C}{\mf{Comp}\ \ptp}
  }
  \and
  \inferrule* {(2)} {\tval{\Gamma}{\Pi}{v}{\ptp}}
} {
  \texp{\Gamma}{\Pi}{\C\ v}{\tview}{\epsilon}
}
\end{mathpar}
\begin{itemize}
\item From (1) and (2) we get (3) that $\tval{\Gamma}{\Pi}{v}{\Delta[\C]}$.
We can then derive conclusion \emph{(i)}:
\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \inferrule*[right=TV-Spec] {
    \inferrule* {(3)} {\tval{\Gamma}{\Pi}{v}{\Delta[\C]}}
  } {
    \tval{\Gamma}{\Pi}{\pair{\C}{v}}{\tview}
  }
} {
  \texp{\Gamma}{\Pi}{\eret{\pair{\C}{v}}}{\tview}{\epsilon}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{E-AppS}: We have $\estep{m}{\vset{\ell}{p}\ v}{\eret{()}}{m'}$ and
\begin{mathpar}
\inferrule*[right=TE-AppS] {
  \inferrule*[right=TV-Set] {
    \inferrule* {(1)} {\Delta[\Pi[p]][\ell] = \ptp}
    \and
    \inferrule* {(2)} {\ell \in \Pi[p].\mf{labs}}
  } {
    \tval{\Gamma}{\Pi}{\vset{\ell}{p}}{\tset{\ptp}}
  }
  \
  \inferrule* {(3)} {\tval{\Gamma}{\Pi}{v}{\ptp}}
} {
  \texp{\Gamma}{\Pi}{\vset{\ell}{p}\ v}{\tone}{\emut}
}
\end{mathpar}
where $m' = m \mid m[p].\mf{st}[\ell] = v$ and $m[p].\mf{rerender} = \mf{tt}$.
\begin{itemize}
\item Conclusion \emph{(i)} follows from the derivation:
\begin{mathpar}
\inferrule*[right=TE-Sub] {
  \inferrule*[right=TE-Ret] {
    \inferrule*[right=TV-Unit] { } {
      \tval{\Gamma}{\Pi}{()}{\tone}
    } 
  } {
    \texp{\Gamma}{\Pi}{\eret{()}}{\tone}{\epsilon}
  }
} {
  \texp{\Gamma}{\Pi}{\eret{()}}{\tone}{\emut}
}
\end{mathpar}
\item To show $\tmem{\Gamma}{\Pi}{m'}$ given premise $\tmem{\Gamma}{\Pi}{m}$,
it suffices to show (\emph{ii}-a) that $\dom(m'[p].\mf{st}) = \Pi[p].\mf{labs}$
and (\emph{ii}-b) that 
$\tval{\Gamma}{\Pi}{m[p].\mf{st}[\ell]}{\Delta[\Pi[p]][\ell]}$.
\item (\emph{ii}-a) follows from (2) and definition of $m'$.
\item To show (\emph{ii}-b), by (1) and definition of $m'$,
we only need to show that $\tval{\Gamma}{\Pi}{v}{\ptp}$.
This is given by (3), and we are done.
\end{itemize}

\tb{Case} \ts{E-CaseN}: We have 
$\estep{m}{\ecase{[]}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}{e_1}{m}$ and
\begin{mathpar}
\inferrule*[right=TE-Case] {
  \inferrule* {(1)} {\texp{\Gamma}{\Pi}{e_1}{\ptp_2}{E}} \and \cdots
} {
  \texp{\Gamma}{\Pi}{\ecase{[]}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item The conclusion follows immediately from (1).
\end{itemize}

\tb{Case} \ts{E-CaseC}: We have\\
$\estep{m}{\ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}
{e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}]}{m}$ and
\begin{mathpar}
\inferrule*[right=TE-Case] {
  \inferrule* {(1)} {\tval{\Gamma}{\Pi}{v_\mf{h} :: v_\mf{t}}{[\ptp_1]}}
  \and
  \inferrule* {(2)} {\texp{\Gamma'}{\Pi}{e_2}{\ptp_2}{E}}
  \and
  \cdots
} {
  \texp{\Gamma}{\Pi}{\ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}
  {\ptp_2}{E}
}
\end{mathpar}
where $\Gamma' = \Gamma[x_\mf{h} \mapsto \ptp', x_\mf{t} \mapsto [\ptp']]$.
\begin{itemize}
\item We can further derive from (1):
\begin{mathpar}
\inferrule*[right=TV-Cons] {
  \inferrule* {(3)} {\Gamma \semi \Pi \vdval v_\mf{h}: \ptp'}
  \and
  \inferrule* {(4)} {\Gamma \semi \Pi \vdval v_\mf{t}: [\ptp']}
} {
  \Gamma \semi \Pi \vdval v_\mf{h} :: v_\mf{t} : [\ptp']\ (1)
}
\end{mathpar}
\item We apply \thref{thm:subexp} (substitution) twice to (2) 
using values (3) and (4) to get that 
$\Gamma \semi \Pi \vdval e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}]: \ptp \mid E$
and we are done.
\end{itemize}
\end{Proof}
\end{lemma}

The above two lemmas are sufficient for type soundness of expressions,
but we also prove a "purity lemma" on expressions for later use:

\begin{lemma}[Purity of expressions.]
\label{thm:pureexp}
If $\cdot \semi \Pi \vdexp e: \ptp \mid \epsilon$
and $m \semi e \mapsto e' \semi m'$,
then $m' = m$.
\begin{Proof}
By induction on the derivation of the typing judgment.

\tb{Case} \ts{TE-Ret}: We have \begin{mathpar}
\inferrule*[right=TE-Ret] {
  \tval{\cdot}{\Pi}{v}{\ptp}
} {
  \texp{\cdot}{\Pi}{\eret{v}}{\ptp}{\epsilon}
}
\end{mathpar}
But there is no rule that steps an expression of form $\eret{v}$,
so the theorem holds vacuously.

\tb{Case} \ts{TE-Let}: We have \begin{mathpar}
\and
\inferrule*[right=TE-Let] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e_1}{\ptp_1}{E}}
  \and
  \texp{[x \mapsto \ptp_1]}{\Pi}{e_2}{\ptp_2}{E}
} {
  \texp{\cdot}{\Pi}{\elet{x}{e_1}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:progexp} (progress) on (1), we either have (a) $e=\eret{v}$
or (b) $\estep{m}{e}{e'}{m'}$.
\item If (a) $e=\eret{v}$, then only \ts{E-Ret} applies, giving us
$\estep{m}{\elet{x}{\eret{v}}{e}}{e[v/x]}{m}$, and $m'=m$.
\item If instead (b) $\estep{m}{e}{e'}{m'}$, then we apply the inductive hypothesis
on (1) and (b), giving us again that $m'=m$.
\item Thus, in either case, the desired statement holds.
\end{itemize}

\tb{Case} \ts{TE-AppL}: We have
\begin{mathpar}
\inferrule*[right=TE-AppL] {
  \inferrule* {(1)} {\tval{\cdot}{\Pi}{v_1}{\ptp_1 \arrow{\epsilon} \ptp_2}}
  \and
  \tval{\cdot}{\Pi}{v_2}{\ptp_1}
} {
  \texp{\cdot}{\Pi}{v_1\ v_2}{\ptp_2}{\epsilon}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), $v_1 = \lambda x.e$.
\item The only step rule which applies is \ts{E-AppL}, which says
$\estep{m}{(\lambda x. e)\ v_2}{e[v_2/x]}{m}$,
i.e. $m' = m$ and we are done.
\end{itemize}

\tb{Case} \ts{TE-AppC}: We have
\begin{mathpar}
\inferrule*[right=TE-AppC] {
  \inferrule* {(1)} {\tval{\cdot}{\Pi}{v_1}{\tcomp{\ptp}}}
  \and
  \tval{\cdot}{\Pi}{v_2}{\ptp}
} {
  \texp{\cdot}{\Pi}{v_1\ v_2}{\tview}{\epsilon}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), $v_1 = \C$.
\item The only step rule that applies is \ts{E-AppC}, which says
that $\estep{m}{\C\ v}{\eret{\pair{\C}{v}}}{m}$,
i.e. $m' = m$ and we are done.
\end{itemize}

\tb{Case} \ts{TE-Case}: We have \begin{mathpar}
\inferrule*[right=TE-Case] {
  \inferrule* {(1)} {\tval{\cdot}{\Pi}{v}{[\ptp_1]}}
  \and \cdots
} {
  \texp{\cdot}{\Pi}{\ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}}{\ptp_2}{E}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:canon} (canonical forms) on (1), 
$v_1 = []$ or $v_1 = v_\mf{hd} :: v_\mf{tl}$.
\item If $v_1=[]$, then \ts{E-CaseN} says that\\
$\estep{m}{\ecase{[]}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}{e_1}{m}$.
\item If $v_1=\mf{false}$, then \ts{E-CaseC} says that\\
$\estep{m}{\ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2}}
{e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}]}{m}$.
\item In either case, $m' = m$ and we are done.
\end{itemize}

\tb{Cases} \ts{TE-AppS} and \ts{TE-Sub}
are both impossible, since they type their conclusions
with the $\mf{Mut}$ effect.
\end{Proof}
\end{lemma}

\subsubsection{Bodies}

\begin{lemma}[Progress of bodies, initial phase.]
\label{thm:progbodi}
If
$\tbod{\cdot}{\Pi}{\hat{e}}{\C}{\Sigma}$ and $\tmem{\cdot}{\Pi}{m}$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $\hat{e} = \eret{v}$, or
\item there exist some $\hat{e}'$ and $m'$ such that
$\bstep{m}{\hat{e}}{\mf{Init}}{p}{\hat{e}'}{m'}$.
\end{enumerate}

\begin{Proof}
By induction on the derivation of the body typing judgment.

\tb{Case} \ts{TB-Lift}: \begin{mathpar}
\inferrule*[right=TB-Lift] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e}{\tview}{\epsilon}}
} {
  \tbod{\cdot}{\Pi}{e}{\C}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:progexp} (expression progress) on (1), we have two subcases:
either $e=\eret{v}$ (and we are done by conclusion (\emph{i})), 
or we have some $e'$ and $m'$ such that
(2) $\estep{m}{e}{e'}{m'}$.
\item By \thref{thm:pureexp} (purity) on (1) and (2), we get (3) that
$\estep{m}{e}{e'}{m}$.
\item We derive the conclusion as follows:
\begin{mathpar}
\inferrule*[right=EB-Lift] {
  \inferrule*[right=(3)] { } {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{e}{\mf{Init}}{p}{e'}{m}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{TB-Let}: \begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e}{\ptp}{\epsilon}}
  \and
  \tbod{[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\cdot}{\Pi}{\elet{x}{e}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:progexp} (expression progress) on (1), we have two subcases:
either (a) $e = \eret{v}$,
or (b) we have $\estep{m}{e}{e'}{m'}$.
\item In subcase (a), \ts{EB-Ret} gives
$\bstep{m}{\elet{x}{\eret{v}}{\hat{e}}}{\mf{Init}}{p}{\hat{e}[v/x]}{m}$.
\item In subcase (b), by \thref{thm:pureexp} (purity) on (1) and (b),
we get (3) that $\estep{m}{e}{e'}{m}$.
We derive a step as follows:
\begin{mathpar}
\inferrule*[right=EB-Let] {
  \inferrule*[right=(3)] { } {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{\elet{x}{e}{\hat{e}}}{\mf{Init}}{p}{\elet{x}{e'}{\hat{e}}}{m}
}
\end{mathpar}
\item Thus, in either subcase, we take a step and satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TB-State}: \begin{mathpar}
\inferrule*[right=TB-State] {
  \Delta[\C][\ell] = \ptp
  \and
  \tval{\cdot}{\Pi}{v}{\ptp}
  \\
  \tbod{[x \mapsto \ptp, x_\mf{set} \mapsto \tset{\ptp}]}
  {\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\cdot}{\Pi}{\estate{x}{\ell}{v}{\hat{e}}}{\C}{\Sigma \cup \{\ell\}}
}
\end{mathpar}
\begin{itemize}
\item Rule \ts{EB-StI} immediately applies, giving us
$$\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}}{\mf{Init}}{p}
{\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]}{m'}$$
for $m' = m \mid m[p].\mf{st}[\ell] = v$.
\end{itemize}
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of bodies, initial phase.]
\label{thm:prezbodi}
If
\begin{enumerate}[label=(\roman*)]
\item $\tbod{\Gamma}{\Pi}{\hat{e}}{\C}{\Sigma}$,
\item $\tmem{\Gamma}{\Pi}{m}$, and
\item $\bstep{m}{\hat{e}}{\mf{Init}}{p}{\hat{e}'}{m'}$,
\end{enumerate}
for $\C=\Pi[p]$,
then there exist some $\Pi' \rpo \Pi$ and $\Sigma' \subseteq \Sigma$ such that:
\begin{enumerate}[label=(\roman*)]
\item $\tbod{\Gamma}{\Pi'}{\hat{e}'}{\C}{\Sigma'}$,
\item $\tmem{\Gamma}{\Pi'}{m'}$, and
\item $\Pi'[p].\mf{labs} \cup \Sigma' = \Pi[p].\mf{labs} \cup \Sigma$.
\end{enumerate}

\begin{Proof}
Unless otherwise specified, we choose $\Pi' = \Pi$ and $\Sigma' = \Sigma$
such that conclusions \emph{(ii)} and \emph{(iii)} are trivial.

We proceed by induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EB-Lift}: We have \begin{mathpar}
\inferrule*[right=EB-Lift] {
  \inferrule* {(1)} {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{e}{\mf{Init}}{p}{e'}{m}
}
\and
\inferrule*[right=TB-Lift] {
  \inferrule* {(2)} {\texp{\Gamma}{\Pi}{e}{\tview}{\epsilon}}
} {
  \tbod{\Gamma}{\Pi}{e}{\C}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:prezexp} (expression preservation)
on (1), (2), and premise \emph{(ii)},
we get (3) $\texp{\Gamma}{\Pi}{e'}{\tview}{\epsilon}$.
\item We satisfy conclusion \emph{(i)} via the following derivation:
\begin{mathpar}
\inferrule*[right=TB-Lift] {
  \inferrule*[right=(3)] { } {\texp{\Gamma}{\Pi}{e'}{\tview}{\epsilon}}
} {
  \tbod{\Gamma}{\Pi}{e'}{\C}{\emptyset}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EB-Let}: We have \begin{mathpar}
\inferrule*[right=EB-Let] {
  \inferrule* {(1)} {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{\elet{x}{e}{\hat{e}}}{\mf{Init}}{p}{\elet{x}{e'}{\hat{e}}}{m}
}
\and
\inferrule*[right=TB-Let] {
  \inferrule* {(2)} {\texp{\Gamma}{\Pi}{e}{\ptp}{\epsilon}}
  \and
  \inferrule* {(3)} {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{e}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:prezexp} (expression preservation)
on (1), (2), and premise \emph{(ii)},
we get that (4) $\Gamma \semi \Pi \vdexp e': \ptp \mid \epsilon$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule*[right=(4)] { } {\texp{\Gamma}{\Pi}{e'}{\ptp}{\epsilon}}
  \and
  \inferrule*[right=(3)] { } {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{e'}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EB-Ret}: We have
(1) $\bstep{m}{\elet{x}{\eret{v}}{\hat{e}}}{\mf{Init}}{p}{\hat{e}[v/x]}{m}$
and:
\begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule*[right=TE-Ret] {
    \inferrule* {(2)} {\tval{\Gamma}{\Pi}{v}{\ptp}}
  } {
    \texp{\Gamma}{\Pi}{\eret{v}}{\ptp}{\epsilon}
  }
  \
  \inferrule* {(3)} {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{\eret{v}}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:subbod} (substitution) on (2) and (3),
we get that $\tbod{\Gamma}{\Pi}{\hat{e}[v/x]}{\C}{\Sigma}$,
satisfying conclusion \emph{(i)}.
\end{itemize}

\tb{Case} \ts{EB-StI}: We have\\
$\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}}{\mf{Init}}{p}
{\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]}{m'}$\\
where $m' = m \mid m[p].\mf{st}[\ell] = v$, and:
\begin{mathpar}
\inferrule*[right=TB-State] {
  \Delta[\C][\ell] = \ptp \ (1)
  \and
  \tval{\Gamma}{\Pi}{v}{\ptp}\ (2)
  \\
  \tbod{\Gamma[x \mapsto \ptp, x_\mf{set} \mapsto \tset{\ptp}]}
  {\Pi}{\hat{e}}{\C}{\hat{\Sigma}}\ (3)
} {
  \tbod{\Gamma}{\Pi}{\estate{x}{\ell}{v}{\hat{e}}}{\C}{\hat{\Sigma} \cup \{\ell\}}
}
\end{mathpar}
\begin{itemize}
\item Choose $\Pi' = \Pi \mid \Pi[p].\mf{labs} = \Pi[p].\mf{labs} \cup \{\ell\}$
and $\Sigma' = \hat{\Sigma}$ (such that $\Sigma = \Sigma' \cup \{\ell\}$).
Clearly both $\Pi \po \Pi'$ and $\Sigma' \subseteq \Sigma$.
\item We derive the setter type as follows 
(recalling that we defined $\C=\Pi[p]$):
\begin{mathpar}
\inferrule*[right=TV-Set] {
  \inferrule*[right=(1)] { } {\Delta[\Pi'[p]][\ell] = \ptp}
  \and
  \inferrule* { } {\ell \in \Pi'[p].\mf{labs}}
} {
  \tval{\Gamma}{\Pi'}{\vset{\ell}{p}}{\tset{\ptp}}\ (4)
}
\end{mathpar}
\item We apply \thref{thm:wkpiv} (weakening) to (2) to get (5) that
$\tval{\Gamma}{\Pi'}{v}{\ptp}$. Then \thref{thm:subbod} (substitution),
applied twice to (3) using (4) and (5),
yields $\tbod{\Gamma}{\Pi'}{\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]}{\C}{\Sigma'}$,
satisfying conclusion \emph{(i)}.
\item To show conclusion \emph{(ii)} given premise \emph{(ii)},
it suffices to show that\\
$\dom(m'[p].\mf{st}) = \Pi'[p].\mf{labs}$
(which is immediate from choice of $\Pi'$), and that
$\tval{\Gamma}{\Pi}{m[p].\mf{st}[\ell]}{\Delta[\C][\ell]}$.
\item By construction of $m'$, we need to show
$\Gamma \semi \Pi \vdval v : \Delta[\C][\ell]$.
This is given by (1) and (2).
\item Finally, we need to show conclusion \emph{(iii)}, i.e.
$\Pi'[p].\mf{labs} \cup \Sigma' = \Pi[p].\mf{labs} \cup \Sigma$.
Note that $\Sigma = \Sigma' \cup \{\ell\}$
and $\Pi'[p].\mf{labs} = \Pi[p].\mf{labs} \cup \{\ell\}$.
The conclusion follows from the equality:
$$(\Pi[p].\mf{labs} \cup \{\ell\}) \cup \Sigma' 
= \Pi[p].\mf{labs} \cup (\Sigma' \cup \{\ell\})$$
\end{itemize}

\tb{Case} \ts{EB-StS}: Impossible,
since the judgment requires that $\phi=\mf{Succ}$.
\end{Proof}
\end{lemma}

\begin{lemma}[Progress of bodies, successor phase.]
\label{thm:progbods}
If \begin{enumerate}[label=(\roman*)]
\item $\tbod{\cdot}{\Pi}{\hat{e}}{\Pi[p]}{\Sigma}$,
\item $\tmem{\cdot}{\Pi}{m}$, and
\item $\Sigma \subseteq \Pi[p].\mf{labs}$,
\end{enumerate}
then either:
\begin{enumerate}[label=(\roman*)]
\item $\hat{e} = \eret{v}$, or
\item there exists some $\hat{e}'$ such that
$\bstep{m}{\hat{e}}{\mf{Succ}}{p}{\hat{e}'}{m}$.
\end{enumerate}
\begin{Proof}
By induction over the derivation of the body typing judgment.

\tb{Case} \ts{TB-Lift}: \begin{mathpar}
\inferrule*[right=TB-Lift] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e}{\tview}{\epsilon}}
} {
  \tbod{\cdot}{\Pi}{e}{\C}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:progexp} (expression progress) on (1), we have two subcases:
either $e=\eret{v}$ (and we are done by conclusion (\emph{i})), 
or we have some $e'$ and $m'$ such that
(2) $\estep{m}{e}{e'}{m'}$.
\item By \thref{thm:pureexp} (purity) on (1) and (2), we get (3) that
$\estep{m}{e}{e'}{m}$.
\item We derive conclusion \emph{(ii)} as follows:
\begin{mathpar}
\inferrule*[right=EB-Lift] {
  \inferrule*[right=(3)] { } {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{e}{\mf{Succ}}{p}{e'}{m}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{TB-Let}: \begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule* {(1)} {\texp{\cdot}{\Pi}{e}{\ptp}{\epsilon}}
  \and
  \tbod{[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\cdot}{\Pi}{\elet{x}{e}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:progexp} (expression progress) on (1), we have two subcases:
either (a) $e = \eret{v}$,
or (b) we have $\estep{m}{e}{e'}{m'}$.
\item In subcase (a), \ts{EB-Ret} gives
$\bstep{m}{\elet{x}{\eret{v}}{\hat{e}}}{\mf{Succ}}{p}{\hat{e}[v/x]}{m}$.
\item In subcase (b), by \thref{thm:pureexp} (purity) on (1) and (b),
we get (3) that $\estep{m}{e}{e'}{m}$.
We derive a step as follows:
\begin{mathpar}
\inferrule*[right=EB-Let] {
  \inferrule*[right=(3)] { } {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{\elet{x}{e}{\hat{e}}}{\mf{Succ}}{p}{\elet{x}{e'}{\hat{e}}}{m}
}
\end{mathpar}
\item Thus, in either subcase, we take a step (with an unchanged memory)
and satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TB-State}: \begin{mathpar}
\inferrule*[right=TB-State] {
  \Delta[\C][\ell] = \ptp
  \and
  \tval{\cdot}{\Pi}{v}{\ptp}
  \\
  \tbod{\cdot[x \mapsto \ptp, x_\mf{set} \mapsto \tset{\ptp}]}
  {\Pi}{\hat{e}}{\C}{\Sigma}
} {
  \tbod{\cdot}{\Pi}{\estate{x}{\ell}{v}{\hat{e}}}{\C}{\Sigma \cup \{\ell\}}
}
\end{mathpar}
\begin{itemize}
\item We want to apply step rule \ts{EB-StS}, which says:
$$\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}}{\mf{Succ}}{p}
{\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}]}{m}$$
where $v' = m[p].\mf{st}[\ell]$.
It suffices to show that $v'$ is well-defined.
\item By premise \emph{(iii)} and the typing rule conclusion, 
we have that $\ell \in \Pi[p].\mf{labs}$.
Then by premise \emph{(ii)}, we get that $\ell \in \dom(m[p].\mf{st})$,
i.e. $v'$ is well-defined and we are done.
\end{itemize}
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of bodies, successor phase.]
\label{thm:prezbods}
If
\begin{enumerate}[label=(\roman*)]
\item $\tbod{\Gamma}{\Pi}{\hat{e}}{\C}{\Sigma}$,
\item $\tmem{\Gamma}{\Pi}{m}$, and
\item $\bstep{m}{\hat{e}}{\mf{Succ}}{p}{\hat{e}'}{m}$,
\end{enumerate}
for $\C=\Pi[p]$,
then there exists some $\Sigma' \subseteq \Sigma$ such that
$\tbod{\Gamma}{\Pi}{\hat{e}'}{\C}{\Sigma'}$.

\begin{Proof}
Unless otherwise specified, we choose $\Sigma' = \Sigma$.

We proceed by induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EB-Lift}: We have \begin{mathpar}
\inferrule*[right=EB-Lift] {
  \inferrule* {(1)} {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{e}{\mf{Succ}}{p}{e'}{m}
}
\and
\inferrule*[right=TB-Lift] {
  \inferrule* {(2)} {\texp{\Gamma}{\Pi}{e}{\tview}{\epsilon}}
} {
  \tbod{\Gamma}{\Pi}{e}{\C}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:prezexp} (expression preservation)
on (1), (2), and premise \emph{(ii)},
we get (3) $\texp{\Gamma}{\Pi}{e'}{\tview}{\epsilon}$.
\item We conclude via the following derivation:
\begin{mathpar}
\inferrule*[right=TB-Lift] {
  \inferrule*[right=(3)] { } {\texp{\Gamma}{\Pi}{e'}{\tview}{\epsilon}}
} {
  \tbod{\Gamma}{\Pi}{e'}{\C}{\emptyset}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EB-Let}: We have \begin{mathpar}
\inferrule*[right=EB-Let] {
  \inferrule* {(1)} {\estep{m}{e}{e'}{m}}
} {
  \bstep{m}{\elet{x}{e}{\hat{e}}}{\mf{Succ}}{p}{\elet{x}{e'}{\hat{e}}}{m}
}
\and
\inferrule*[right=TB-Let] {
  \inferrule* {(2)} {\texp{\Gamma}{\Pi}{e}{\ptp}{\epsilon}}
  \and
  \inferrule* {(3)} {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{e}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}\begin{itemize}
\item By \thref{thm:prezexp} (expression preservation)
on (1), (2), and premise \emph{(ii)},
we get that (4) $\Gamma \semi \Pi \vdexp e': \ptp \mid \epsilon$.
\item We derive the conclusion as follows:
\begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule*[right=(4)] { } {\texp{\Gamma}{\Pi}{e'}{\ptp}{\epsilon}}
  \and
  \inferrule*[right=(3)] { } {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{e'}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EB-Ret}: We have 
(1) $\bstep{m}{\elet{x}{\eret{v}}{\hat{e}}}{\mf{Succ}}{p}{\hat{e}[v/x]}{m}$
and:
\begin{mathpar}
\inferrule*[right=TB-Let] {
  \inferrule*[right=TE-Ret] {
    \inferrule* {(2)} {\tval{\Gamma}{\Pi}{v}{\ptp}}
  } {
    \texp{\Gamma}{\Pi}{\eret{v}}{\ptp}{\epsilon}
  }
  \
  \inferrule* {(3)} {\tbod{\Gamma[x \mapsto \ptp]}{\Pi}{\hat{e}}{\C}{\Sigma}}
} {
  \tbod{\Gamma}{\Pi}{\elet{x}{\eret{v}}{\hat{e}}}{\C}{\Sigma}
}
\end{mathpar}
\begin{itemize}
\item By \thref{thm:subbod} (substitution) on (2) and (3),
we get that $\tbod{\Gamma}{\Pi}{\hat{e}[v/x]}{\C}{\Sigma}$,
satisfying the conclusion.
\end{itemize}

\tb{Case} \ts{EB-StI}: Impossible,
since the judgment requires that $\phi=\mf{Init}$.

\tb{Case} \ts{EB-StS}: We have
$$\bstep{m}{\estate{x}{\ell}{v}{\hat{e}}}{\mf{Succ}}{p}
{\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}]}{m}$$
where $v' = m[p].\mf{st}[\ell]$, and:
\begin{mathpar}
\inferrule*[right=TB-State] {
  \Delta[\C][\ell] = \ptp\ (1)
  \and
  \tval{\Gamma}{\Pi}{v}{\ptp}
  \\
  \tbod{\Gamma[x \mapsto \ptp, x_\mf{set} \mapsto \tset{\ptp}]}
  {\Pi}{\hat{e}}{\C}{\hat{\Sigma}}\ (2)
} {
  \tbod{\Gamma}{\Pi}{\estate{x}{\ell}{v}{\hat{e}}}{\C}{\hat{\Sigma} \cup \{\ell\}}
}
\end{mathpar}
\begin{itemize}
\item Choose $\Sigma' = \hat{\Sigma}$, such that $\Sigma = \Sigma' \cup \{\ell\}$
(i.e. $\Sigma' \subseteq \Sigma$.)
\item Since $m[p].\mf{st}[\ell]$ is defined,
by premise \emph{(ii)}, we have (3) that $\ell \in \Pi[p].\mf{labs}$.
We can now derive the setter type as follows 
(recalling that we defined $\C=\Pi[p]$):
\begin{mathpar}
\inferrule*[right=TV-Set] {
  \inferrule*[right=(1)] { } {\Delta[\Pi[p]][\ell] = \ptp}
  \and
  \inferrule*[right=(3)] { } {\ell \in \Pi[p].\mf{labs}}
} {
  \tval{\Gamma}{\Pi'}{\vset{\ell}{p}}{\tset{\ptp}}\ (4)
}
\end{mathpar}
\item By premise \emph{(ii)},
we get that $\tval{\Gamma}{\Pi}{v'}{\Delta[\C][\ell]}$.
By (1), we get (5) that $\tval{\Gamma}{\Pi}{v'}{\ptp}$.
\item Now, we apply \thref{thm:subbod} (substitution)
twice to (2) using (4) and (6) to get that 
$\tbod{\Gamma}{\Pi}{\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}]}{\C}{\Sigma'}$
and we are done.
\end{itemize}

\end{Proof}
\end{lemma}

\subsubsection{Programs}

\begin{lemma}[Progress of programs.]
\label{thm:progprog}
If $\tprog{\Gamma}{P}{\Omega}$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $P = \eret{v}$, or
\item there exist some $P'$ and $\D'$ such that, for any $\D$, we have
$\pstep{\D}{P}{P'}{\D'}$.
\end{enumerate}
\begin{Proof}
Note that the empty memory is well-typed (only) by the empty path context,
i.e. we have (1) that $\tmem{\cdot}{\cdot}{\cdot}$.

We proceed by induction on the program typing derivation.

\tb{Case} \ts{TP-Lift}: \begin{mathpar}
\inferrule*[right=TP-Lift] {
  \inferrule* {(2)} {\texp{\cdot}{\cdot}{e}{\tview}{\epsilon}}
} {
  \tprog{\cdot}{e}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item We apply \thref{thm:progexp} (expression progress)
on (2) and the empty memory (1)
to get that either $e = \eret{v}$ (and we are done by conclusion \emph{(i)}),
or that (3) $\estep{\cdot}{e}{e'}{m}$.
\item In the latter case, we note by \thref{thm:pureexp} (purity)
In subcase (b), by \thref{thm:pureexp} (purity) on (3), we get
(4) that $\estep{\cdot}{e}{e'}{\cdot}$.
We derive a step as follows, satisfying conclusion \emph{(ii)}:
\end{itemize}
\begin{mathpar}
\inferrule*[right=EP-Lift] {
  \inferrule*[right=(4)] { } {\estep{\cdot}{e}{e'}{\cdot}}
} {
  \pstep{\D}{e}{e'}{\D}
}
\end{mathpar}

\tb{Case} \ts{TP-Let}: \begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule* {(2)} {\texp{\cdot}{\cdot}{e}{\ptp}{\epsilon}}
  \and 
  \tprog{[x \mapsto \ptp]}{P}{\Omega}
} {
  \tprog{\cdot}{\elet{x}{e}{P}}{\Omega}
}
\end{mathpar}
\begin{itemize}
\item We again apply \thref{thm:progexp} (expression progress)
on (2) and the empty memory (1)
to get that either (a) $e = \eret{v}$ or (b) $\estep{\cdot}{e}{e'}{m}$.
\item In subcase (a), \ts{EP-Ret} gives that
$\pstep{\D}{\elet{x}{\eret{v}}{P}}{P[v/x]}{\D}$.
\item In subcase (b), by \thref{thm:pureexp} (purity) on (2), we get
(3) that $\estep{\cdot}{e}{e'}{\cdot}$. We derive a step as follows:
\begin{mathpar}
\inferrule*[right=EP-Let] {
  \inferrule*[right=(3)] { } {\estep{\cdot}{e}{e'}{\cdot}}
} {
  \pstep{\D}{\elet{x}{e}{P}}{\elet{x}{e'}{P}}{\D}
}
\end{mathpar}
\item Thus, in either case, we take a step and we are done.
\end{itemize}

\tb{Case} \ts{TP-Def}: \begin{mathpar}
\inferrule*[right=TP-Def] {
  \tbod{\Gamma[x \mapsto \Delta[\C]]}{\cdot}{\hat{e}}{\C}{\dom(\Delta[\C])}
  \and
  \tprog{\Gamma}{P}{\Omega}
} {
  \tprog{\Gamma}{\elet{\C(x)}{\hat{e}}{P}}{\Omega \cup \{\C\}}
}
\end{mathpar}
\begin{itemize}
\item Rule \ts{EP-Def} immediately applies, giving us 
$$\pstep{\D}{\elet{\C(x)}{\hat{e}}{P}}{P}{\D[\C \mapsto x.\hat{e}]}$$
and we are done.
\end{itemize}

\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of programs.]
\label{thm:prezprog}
If
\begin{enumerate}[label=(\roman*)]
\item $\tprog{\Gamma}{P}{\Omega}$,
\item $\tdef{\Gamma}{\Delta}{\D}$, and
\item $\pstep{\D}{P}{P'}{\D'}$,
\end{enumerate}
then there exists some $\Omega' \subseteq \Omega$ such that:
\begin{enumerate}[label=(\roman*)]
\item $\tprog{\Gamma}{P'}{\Omega'}$,
\item $\tdef{\Gamma}{\Delta}{\D'}$, and
\item $\Omega' \cup \dom(\D') = \Omega \cup \dom(\D)$.
\end{enumerate}

\begin{Proof}
Unless stated otherwise, let $\Omega' = \Omega$ 
(such that both conclusions \emph{(ii)} and \emph{(iii)}
are trivial when $\D' = \D$.)

Again note that the empty memory is well-typed (only) by the empty path context,
i.e. we have (1) that $\tmem{\cdot}{\cdot}{\cdot}$.

We proceed by induction on the derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EP-Lift}: We have \begin{mathpar}
\inferrule*[right=EP-Lift] {
  \inferrule* {(2)} {\estep{\cdot}{e}{e'}{\cdot}}
} {
  \pstep{\D}{e}{e'}{\D}
}
\and
\inferrule*[right=TP-Lift] {
  \inferrule* {(3)} {\texp{\Gamma}{\cdot}{e}{\tview}{\epsilon}}
} {
  \tprog{\Gamma}{e}{\emptyset}
}
\end{mathpar}
\begin{itemize}
\item \thref{thm:prezexp} (expression preservation)
on (2), (3), and the empty memory (1) gives us (4) that 
$\texp{\Gamma}{\cdot}{e'}{\tview}{\epsilon}$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TP-Lift] {
  \inferrule*[right=(3)] { } {\texp{\Gamma}{\cdot}{e'}{\tview}{\epsilon}}
} {
  \tprog{\Gamma}{e'}{\emptyset}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EP-Let}: We have \begin{mathpar}
\inferrule*[right=EP-Let] {
  \inferrule* {(2)} {\estep{\cdot}{e}{e'}{\cdot}}
} {
  \pstep{\D}{\elet{x}{e}{P}}{\elet{x}{e'}{P}}{\D}
}
\and
\inferrule*[right=TP-Let] {
  \inferrule* {(3)} {\texp{\Gamma}{\cdot}{e}{\ptp}{\epsilon}}
  \and 
  \inferrule* {(4)} {\tprog{\Gamma[x \mapsto \ptp]}{P}{\Omega}}
} {
  \tprog{\Gamma}{\elet{x}{e}{P}}{\Omega}
}
\end{mathpar}
\begin{itemize}
\item \thref{thm:prezexp} (expression preservation)
on (2), (3), and the empty memory (1) gives us (5) that 
$\texp{\Gamma}{\cdot}{e'}{\ptp}{\epsilon}$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule*[right=(5)] { } {\texp{\Gamma}{\cdot}{e'}{\ptp}{\epsilon}}
  \and 
  \inferrule*[right=(4)] { } {\tprog{\Gamma[x \mapsto \ptp]}{P}{\Omega}}
} {
  \tprog{\Gamma}{\elet{x}{e'}{P}}{\Omega}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{EP-Ret}: We have
$\D \semi \elet{x}{\eret{v}}{P} \mapsto P[v/x] \semi \D$ and 
\begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule*[right=TE-Ret] {
    \inferrule* {(2)} {\tval{\Gamma}{\cdot}{v}{\ptp}}
  } {
    \texp{\Gamma}{\cdot}{\eret{v}}{\ptp}{\epsilon}
  }
  \and 
  \inferrule* {(3)} {\tprog{\Gamma[x \mapsto \ptp]}{P}{\Omega}}
} {
  \tprog{\Gamma}{\elet{x}{\eret{v}}{P}}{\Omega}
}
\end{mathpar}
\begin{itemize}
\item Conclusion \emph{(i)}, that $\tprog{\Gamma}{P[v/x]}{\Omega}$,
follows directly from \thref{thm:subprog} (substitution) on (2) and (3).
\end{itemize}

\tb{Case} \ts{EP-Def}: We have
$\pstep{\D}{\elet{\C(x)}{\hat{e}}{P}}{P}{\D[\C \mapsto x.\hat{e}]}$
and
\begin{mathpar}
\inferrule*[right=TP-Def] {
  \inferrule* {(2)} {
    \tbod{\Gamma[x \mapsto \Delta[\C]]}{\cdot}{\hat{e}}{\C}{\dom(\Delta[\C])}
  }
  \and
  \inferrule* {(3)} {\tprog{\Gamma}{P}{\hat{\Omega}}}
} {
  \tprog{\Gamma}{\elet{\C(x)}{\hat{e}}{P}}{\hat{\Omega} \cup \{\C\}}
}
\end{mathpar}
\begin{itemize}
\item Choose $\Omega' = \hat{\Omega}$, such that $\Omega = \Omega' \cup \{\C\}$.
Clearly $\Omega' \subseteq \Omega$, and furthermore,
conclusion \emph{(i)} is immediate from (3).
\item In order to show conclusion \emph{(ii)} given premise \emph{(ii)},
it suffices to show that 
$\tbod{\Gamma[x \mapsto \Delta[\C]]}{\cdot}{\hat{e}}{\C}{\dom(\Delta[\C])}$.
This is immediate from (2).
\item Finally, we need to show conclusion \emph{(iii)}, i.e.
$\Omega' \cup \dom(\D') = \Omega \cup \dom(\D)$.
Note that $\dom(\D') = \dom(\D) \cup \{\C\}$
and recall that $\Omega = \Omega' \cup \{\C\}$.
The conclusion follows from the equality:
$$\Omega' \cup (\dom(\D) \cup \{\C\}) = (\Omega' \cup \{\C\}) \cup \dom(\D)$$
\item All conclusions hold, and we are done.
\end{itemize}
\end{Proof}
\end{lemma}

\section{Small-Step Runtime}

\subsection{Runtime Syntax}

\begin{align*}
\text{Runtime values } u
&::= ()_\rho \mid []_\rho \mid u ::_\rho u
\mid \elit{v} \mid \enode{id}{v}{u}
\mid p\\
\text{Runtime expressions } \rho
&::= ()_\rho \mid []_\rho \mid \rho ::_\rho \rho
\mid \elit{v} \mid \enode{id}{v}{\rho}
\mid p\\
&\quad\mid \rho \semi \rho \mid
\einit{v} \mid \einits{v} \mid \escan{v} \mid \escans{v}\\
&\quad\mid \echk{\rho} \mid \echks{\rho}
\mid \esearch{id}{\rho} \mid \esearches{id}{\rho}\\
&\quad\mid \eeval{\phi}{p}{\hat{e}} \mid \enew{\phi}{p}{\rho}
\mid \ehdl{e} \mid \eprog{P}\\
\text{Evaluation contexts } \E
&::= [\cdot] \mid \enode{id}{v}{\E} \mid \E ::_\rho \rho
\mid u ::_\rho \E \mid \E \semi \rho\\
&\quad\mid \enew{\phi}{p}{\E}
\mid \echk{\E} \mid \echks{\E}\\
&\quad\mid \esearch{id}{\E} \mid \esearches{id}{\E}\\
\text{Runtime type } \rtp &::= \tone_\rho \mid \mf{Tree}
\mid [\rtp]_\rho
\end{align*}

\tb{\emph{Configurations.}} Our configurations are triples of the form
$\trip{\D}{m}{\rho}$.

\tb{\emph{Subscripts.}} 
Some forms are subscripted with $\rho$ to indicate a
\emph{syntactic distinction} between them and their program-level
counterparts. (This $\rho$ is just a marker, not a runtime expression.)

In general, however, we omit these markers when doing so causes no ambiguity.

\tb{\emph{Phase Types.}}
We associate each phase $\phi$ with a runtime type $\rtp$ as follows:
\[
\rtp(\phi) = \begin{cases}
    \mf{Tree} & \phi=\mf{Init}\\
    \tone & \phi=\mf{Succ}
\end{cases}
\]

\tb{\emph{Constants.}} We distinguish three constants:
\begin{itemize}
\item $p_0$, the ``root path'' in memory,
\item $\C_0$, the (dummy) component name associated with that path, and
\item $x_0$, the variable name bound in that component's body.
\end{itemize}
We assume these constants are \emph{globally fresh},
i.e. they do not conflict with any other names in the program
or that are ``generated'' at runtime, e.g. by the premise ``$p$ fresh''.

\subsection{Runtime-Level Typing}

Runtime judgment: $\trho{\Gamma}{\Pi}{\Omega}{\rho}{\rtp}$

\subsubsection{Programs}

\begin{mathpar}
\inferrule*[right=TR-Prog] {
  \tprog{\Gamma}{P}{\Omega'}
  \and
  \Delta[\C_0] = \tone
  \and
  \dom(\Delta) = \Omega \cup \Omega' \cup \{\C_0\}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\eprog{P}}{\tone}
}
\end{mathpar}

In every rule \emph{besides} \ts{TR-Prog},
we have the (implicit) additional premise that
$\mf{dom}(\Delta) = \Omega$,
i.e. all components in $\Delta$ have been defined.

\subsubsection{Values (and Sequencing)}

\begin{mathpar}
\inferrule*[right=TR-Unit] { } {
  \trho{\Gamma}{\Pi}{\Omega}{()}{\tone}
}
\and
\inferrule*[right=TR-Nil] { } {
  \trho{\Gamma}{\Pi}{\Omega}{[]}{[\rtp]}
}
\and
\inferrule*[right=TR-Cons] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho_\mf{hd}}{\rtp}
  \and
  \trho{\Gamma}{\Pi}{\Omega}{\rho_\mf{tl}}{[\rtp]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\rho_\mf{hd} :: \rho_\mf{tl}}{[\rtp]}
}
\and
\inferrule*[right=TR-Lit] {
  \tval{\Gamma}{\Pi}{v}{\tstr}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\elit{v}}{\ttree}
}
\and
\inferrule*[right=TR-Node] {
  \tval{\Gamma}{\Pi}{v}{[\tattr]}
  \and
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{[\ttree]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\enode{id}{v}{\rho}}{\ttree}
}
\and
\inferrule*[right=TR-Path] {
  \Pi[p].\mf{tree} = \tt
  \and
  \Pi[p].\mf{labs} = \dom(\Delta[\Pi[p]])
} {
  \trho{\Gamma}{\Pi}{\Omega}{p}{\ttree}
}
\and
\inferrule*[right=TR-Seq] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho_1}{\tone}
  \and
  \trho{\Gamma}{\Pi}{\Omega}{\rho_2}{\rtp}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\rho_1 \semi \rho_2}{\rtp}
}
\end{mathpar}

\subsubsection{Initialization/Reinitialization}

\begin{mathpar}
\inferrule*[right=TR-Init] {
  \tval{\Gamma}{\Pi}{v}{\tview}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\einit{v}}{\ttree}
}
\and
\inferrule*[right=TR-Inits] {
  \tval{\Gamma}{\Pi}{v}{[\tview]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\einits{v}}{[\ttree]}
}
\and
\inferrule*[right=TR-EvalI] {
  \tbod{\Gamma}{\Pi}{\hat{e}}{\Pi[p]}{\Sigma}
  \and
  \dom(\Delta[\Pi[p]]) = \Sigma \cup \Pi[p].\mf{labs}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\eeval{\mf{Init}}{p}{\hat{e}}}{\ttree}
}
\and
\inferrule*[right=TR-EvalS] {
  \tbod{\Gamma}{\Pi}{\hat{e}}{\Pi[p]}{\Sigma}
  \and
  \Sigma \subseteq \Pi[p].\mf{labs} = \dom(\Delta[\Pi[p]])
} {
  \trho{\Gamma}{\Pi}{\Omega}{\eeval{\mf{Succ}}{p}{\hat{e}}}{\tone}
}
\and
\inferrule*[right=TR-New] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{\ttree}
  \and
  \Pi[p].\mf{labs} = \dom(\Delta[\Pi[p]])
} {
  \trho{\Gamma}{\Pi}{\Omega}{\enew{\phi}{p}{\rho}}{\rtp(\phi)}
}
\end{mathpar}

\subsubsection{Event Handling}

\begin{mathpar}
\inferrule*[right=TR-Srch] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{\ttree}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\esearch{id}{\rho}}{\tone}
}
\and
\inferrule*[right=TR-Srchs] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{[\ttree]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\esearches{id}{\rho}}{\tone}
}
\and
\inferrule*[right=TR-Sc] {
  \tval{\Gamma}{\Pi}{v}{\tattr}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\escan{v}}{\tone}
}
\and
\inferrule*[right=TR-Scs] {
  \tval{\Gamma}{\Pi}{v}{[\tattr]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\escans{v}}{\tone}
}
\and
\inferrule*[right=TR-Hdl] {
  \texp{\Gamma}{\Pi}{e}{\tone}{E}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\ehdl{e}}{\tone}
}
\and
\inferrule*[right=TR-Chk] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{\ttree}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\echk{\rho}}{\tone}
}
\and
\inferrule*[right=TR-Chks] {
  \trho{\Gamma}{\Pi}{\Omega}{\rho}{[\ttree]}
} {
  \trho{\Gamma}{\Pi}{\Omega}{\echks{\rho}}{\tone}
}
\end{mathpar}

\subsubsection{Configurations}

Define
$\tcfg{\Gamma}{\Pi}{\Omega}{\D}{m}{\rho}{\rtp}$
to hold if:
\begin{itemize}
\item $\tdef{\Gamma}{\Delta}{\D}$,
\item $\tmem{\Gamma}{\Pi}{m}$, and
\item $\trho{\Gamma}{\Pi}{\Omega}{\rho}{\rtp}$.
\end{itemize}

\subsection{Runtime-Level Semantics}

Judgment: $\cstep{\D}{m}{\rho}{\D}{m}{\rho}$

(The initial configuration is $\trip{\cdot}{\cdot}{\mf{prog}\ P}$.)
\subsubsection{General Rules}

\begin{mathpar}
\inferrule*[right=ER-Ctx] {
  \cstep{\D}{m}{\rho}{\D'}{m'}{\rho'}
} {
  \cstep{\D}{m}{\E[\rho]}{\D'}{m'}{\E'[\rho]}
}
\end{mathpar}
\begin{align*}
\cstep{\D}{m}{\mf{done} \semi \rho&}{\D}{m}{\rho}
&&\ts{ER-Seq}
\end{align*}

\subsubsection{Program Evaluation}

\begin{mathpar}
\inferrule*[right=ER-Prog] {
  \pstep{\D}{P}{P'}{\D'}
} {
  \cstep{\D}{m}{\eprog{P}}{\D'}{m}{\eprog{P'}}
}
\and
\inferrule*[right=ER-PDone] {
  \D' = \D[\C_0 \mapsto x.\eret{v}]
  \\
  m' = m[p_0 \mapsto \{\mf{spec}: \pair{\C_0}{()}, \mf{st}: \cdot,
  \mf{tree}: \cdot, \mf{rerender}: \mf{ff}\}]
} {
  \cstep{\D}{m}{\eprog{\eret{v}}}{\D'}{m'}{\enew{\mf{Succ}}{p_0}{\einit{v}}}
}
\end{mathpar}

\subsubsection{Initialization}
\begin{mathpar}
\inferrule*[right=ER-ISpec] {
  p\ \mf{fresh}
  \and 
  \D[\C]=x.\hat{e}
  \\
  m'=m[p \mapsto \{
    \mf{spec}=\pair{\C}{v}, 
    \mf{st}: \cdot, 
    \mf{tree}: \cdot, 
    \mf{rerender}: \mf{ff}
  \}]
} {
  \cstep{\D}{m}{\einit{\pair{\C}{v}}}{\D}{m'}{\eeval{\mf{Init}}{p}{\hat{e}[v/x]}}
}
\end{mathpar}
\begin{align*}
\cstep{\D}{m}{\einit{\vtxt{v}}&}{\D}{m}{\elit{v}}
&&\ts{ER-IText}\\
\cstep{\D}{m}{\einit{\vtag{v_1}{v_2}}&}{\D}{m}{\enode{id}{v_1}{\einits{v_2}}}
&&\ts{ER-ITag}\\
&(\mf{id\ fresh})\\
\cstep{\D}{m}{\einits{[]}&}{\D}{m}{[]}
&&\ts{ER-INil}\\
\cstep{\D}{m}{\einits{v :: vs}&}{\D}{m}{\einit{v} :: \einits{vs}}
&&\ts{ER-ICons}\\
\end{align*}

\subsubsection{Component Evaluation}
\begin{mathpar}
\inferrule*[right=ER-Eval] {
  \bstep{m}{\hat{e}}{\phi}{p}{\hat{e}'}{m'}
} {
  \cstep{\D}{m}{\eeval{\phi}{p}{\hat{e}}}{\D}{m'}{\eeval{\phi}{p}{\hat{e}'}}
}
\end{mathpar}
\begin{align*}
\cstep{\D}{m}{\eeval{\phi}{p}{\eret{v}}&}{\D}{m}{\enew{\phi}{p}{\einit{v}}}
&&\ts{ER-New}\\
\cstep{\D}{m}{\enew{\mf{Init}}{p}{u}&}{\D}{m \mid m[p].\mf{tree} = u}{p}
&&\ts{ER-NewI}\\
\cstep{\D}{m}{\enew{\mf{Succ}}{p}{u}&}{\D}{m \mid m[p].\mf{tree} = u}{()}
&&\ts{ER-NewS}\\
\end{align*}

\subsubsection{Handler Search/Evaluation}
\begin{align*}
\cstep{\D}{m}{\esearch{id}{\elit{v}}&}{\D}{m}{()}
&&\ts{ER-SLit}\\
\cstep{\D}{m}{\esearch{id}{p}&}{\D}{m}{\esearch{id}{m[p].\mf{tree}}}
&&\ts{ER-SPath}\\
\cstep{\D}{m}{\esearch{id}{\enode{id'}{v}{\rho}}&}{\D}{m}{\esearches{id}{\rho}}
&&\ts{ER-SNodeF}\\
\cstep{\D}{m}{\esearch{id}{\enode{id}{v}{\rho}}&}
{\D}{m}{\escans{v} \semi \esearches{id}{\rho}}
&&\ts{ER-SNodeT}\\
\cstep{\D}{m}{\esearches{id}{[]}&}{\D}{m}{()}
&&\ts{ER-SsNil}\\
\cstep{\D}{m}{\esearches{id}{\rho :: \rho s}}
{&\D}{m}{\esearch{id}{\rho} \semi \esearches{id}{\rho s}}
&&\ts{ER-SsCons}
\end{align*}
\begin{align*}
\cstep{\D}{m}{\escan{\vattr{v_1}{v_2}}&}{\D}{m}{()}
&&\ts{ER-ScAttr}\\
\cstep{\D}{m}{\escan{\vclick{v}}&}{\D}{m}{\ehdl{v\ ()}}
&&\ts{ER-ScClick}\\
\cstep{\D}{m}{\escans{[]}&}{\D}{m}{()}
&&\ts{ER-ScsNil}\\
\cstep{\D}{m}{\escans{v :: vs}&}{\D}{m}{\escan{v} \semi \escans{vs}}
&&\ts{ER-ScsCons}
\end{align*}
\begin{mathpar}
\inferrule*[right=ER-Hdl] {
  \estep{m}{e}{e'}{m'}
} {
  \cstep{\D}{m}{\ehdl{e}}{\D}{m'}{\ehdl{e'}}
}
\end{mathpar}
\begin{align*}
\cstep{\D}{m}{\ehdl{\eret{()}}}{\D}{m}{()}
&&\ts{ER-HdlDone}\\
\end{align*}

\subsubsection{Checking}

\begin{mathpar}
\inferrule*[right=ER-ChkPF] {
  m[p].\mf{rerender} = \ff
} {
  \cstep{\D}{m}{\echk{p}}{\D}{m}{\echk{m[p].\mf{tree}}}
}
\and
\inferrule*[right=ER-ChkPT] {
  m[p].\mf{rerender} = \tt
  \and
  m[p].\mf{spec} = \pair{\C}{v}
  \and
  \D[\C] = x.\hat{e}
} {
  \cstep{\D}{m}{\echk{p}}
  {\D}{m \mid m[p].\mf{rerender} = \ff}{\eeval{\mf{Succ}}{p}{\hat{e}[v/x]}}
}
\end{mathpar}
\begin{align*}
\cstep{\D}{m}{\echk{\elit{v}}&}{\D}{m}{()}
&&\ts{ER-ChkLit}\\
\cstep{\D}{m}{\echk{\enode{id}{v}{\rho}}&}{\D}{m}{\echks{\rho}}
&&\ts{ER-ChkNode}\\
\cstep{\D}{m}{\echks{[]}&}{\D}{m}{()}
&&\ts{ER-ChksNil}\\
\cstep{\D}{m}{\echks{\rho :: \rho s}&}{\D}{m}{\echk{\rho} \semi \echks{\rho s}}
&&\ts{ER-ChksCons}
\end{align*}

\subsubsection{Events}

We introduce a second step judgment, $\zstep{\D}{m}{\rho}{\D}{m}{\rho}$,
which subsumes $\hookrightarrow$ and additionally allows for
nondeterministic event occurrence at idle state.
(We distinguish $\hookrightarrow_0$ to prevent events from occuring inside nested terms.)
\begin{mathpar}
\inferrule*[right=ER-Top] {
  \cstep{\D}{m}{\rho}{\D'}{m'}{\rho'}
} {
  \zstep{\D}{m}{\rho}{\D'}{m'}{\rho'}
}
\end{mathpar}
\begin{align*}
\zstep{\D}{m}{()}{\D}{m}{\esearch{id}{p_0} \semi \echk{p_0}}
&&\ts{ER-Event}\\
\end{align*}

\subsection{Runtime-Level Proofs}

\begin{lemma}[Progress of configurations.]
If $\tcfg{\Gamma}{\Pi}{\Omega}{\D}{m}{\rho}{\rtp}$, we then
have either
\begin{enumerate}[label=(\roman*)]
\item $\rho = u$, or
\item there exists some $\trip{\D'}{m'}{\rho'}$ such that
$\cstep{\D}{m}{\rho}{\D'}{m'}{\rho'}$.
\end{enumerate}
\end{lemma}

\subsection{Realization}

We define the $\mf{unfold}$ meta-function to ``realize'' a memory into
a concrete tree structure as follows:
\begin{align*}
\mf{unfold}(m, \elit{v}) &= \elit{v}\\
\mf{unfold}(m, \enode{id}{v}{u}) &= \enode{id}{v}{\mf{unfold}\ u}\\
\mf{unfold}(m, p) &= \mf{unfold}(m[p].\mf{tree})\\
\mf{unfold}(m, []) &= []\\
\mf{unfold}(m, u :: us) &= \mf{unfold}\ u :: \mf{unfold}\ us\\
\end{align*}

\section{Old Typing Rules and Proofs}

\subsection{Program-Level Typing}

Names of noteworthy rules are \hl{highlighted}.
Rules for typing internal representations,
i.e. that are necessary for soundness but not typechecking,
are \grey{greyed}. 
Similarly, $\Pi$ is unnecessary for typechecking.

\subsubsection{Value Typing}

Judgment: $\Gamma \semi \Pi \vdval v: \ptp$

\begin{mathpar}
\inferrule*[right=TV-Var] {
  \Gamma[x] = \ptp
} {
  \Gamma \semi \Pi \vdval x: \ptp
}
\and
\inferrule*[right=TV-Unit] { } {
  \Gamma \semi \Pi \vdval () : \tone
}
\and
\inferrule*[right=TV-Str] { } {
  \Gamma \semi \Pi \vdval s : \tstr
}
\and
\inferrule*[right=TV-Lam] {
  \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e : \ptp_2 \mid E
} {
  \Gamma \semi \Pi 
  \vdval
  \lambda x.e: \ptp_1 \xrightarrow{E} \ptp_2
}
\and
\inferrule*[right=TV-Nil] { } {
  \Gamma \semi \Pi \vdval [] : [\ptp]
}
\and
\inferrule*[right=TV-Cons] {
  \Gamma \semi \Pi \vdval v_1 : \ptp
  \and
  \Gamma \semi \Pi \vdval v_2 : [\ptp]
} {
  \Gamma \semi \Pi \vdval v_1 :: v_2 : [\ptp]
}
\and
\inferrule*[right=\hl{TV-Comp}] {
  \Delta[\C] = \ptp
} {
  \Gamma \semi \Pi \vdval \C : \mf{Component}\ \ptp
}
\and
\inferrule*[right=\hl{TV-Text}] {
  \Gamma \semi \Pi \vdval v: \tstr
} {
  \Gamma \semi \Pi \vdval \mf{text}\ v: \mf{View}
}
\and
\inferrule*[right=\hl{TV-Tag}] {
  \Gamma \semi \Pi \vdval v_1: [\mf{Attr}]
  \and
  \Gamma \semi \Pi \vdval v_2: [\mf{View}]
} {
  \Gamma \semi \Pi \vdval \mf{tag}(v_1, v_2): \mf{View}
}
\and
\inferrule*[right=\hl{TV-Attr}] {
  \Gamma \semi \Pi \vdval v_k: \tstr
  \and
  \Gamma \semi \Pi \vdval v_v: \tstr
} {
  \Gamma \semi \Pi \vdval \mf{attr}(v_k, v_v): \mf{Attr}
}
\and
\inferrule*[right=\hl{TV-OnClick}] {
  \Gamma \vdval v: \tone \xrightarrow{E} \tone
} {
  \Gamma \vdval \mf{onClick}\ v: \mf{Attr}
}
\and
\inferrule*[right=\grey{TV-Spec}] {
  \Gamma \semi \Pi \vdval \C: \mf{Component}\ \ptp
  \and
  \Gamma \semi \Pi \vdval v: \ptp
} {
  \Gamma \semi \Pi \vdval \pair{\C}{v}: \mf{View}
}
\and
\inferrule*[right=\grey{TV-Set}] {
  \Delta[\Pi[p]][\ell] = \ptp
  \and
  \ell \in \Pi[p].\mf{labs}
} {
  \Gamma \semi \Pi \vdval \vset{\ell}{p} : \mf{Setter}\ \ptp
}
\end{mathpar}

\subsubsection{Expression Typing}

Judgment: $\Gamma \semi \Pi \vdexp e : \ptp \mid E$

\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \Gamma \semi \Pi \vdval v: \ptp
} {
  \Gamma \semi \Pi \vdexp \eret{v}: \ptp \mid \epsilon
}
\and
\inferrule*[right=TE-Let] {
  \Gamma \semi \Pi \vdexp e_1 : \ptp_1 \mid E
  \and
  \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e_2 : \ptp_2 \mid E
} {
  \Gamma \semi \Pi \vdexp \elet{x}{e_1}{e_2}: \ptp_2 \mid E
}
\and
\inferrule*[right=TE-AppL] {
  \Gamma \semi \Pi \vdval v_1 : \ptp_1 \xrightarrow{E} \ptp_2
  \and
  \Gamma \semi \Pi \vdval v_2 : \ptp_1
} {
  \Gamma \semi \Pi \vdexp v_1\ v_2: \ptp_2 \mid E
}
\and
\inferrule*[right=\hl{TE-AppC}] {
  \Gamma \semi \Pi \vdval v_1 : \mf{Component}\ \ptp
  \and
  \Gamma \semi \Pi \vdval v_2 : \ptp
} {
  \Gamma \semi \Pi \vdexp v_1\ v_2: \mf{View} \mid \epsilon
}
\and
\inferrule*[right=\hl{TE-AppS}] {
  \Gamma \semi \Pi \vdval v_1 : \mf{Setter}\ \ptp
  \and
  \Gamma \semi \Pi \vdval v_2 : \ptp
} {
  \Gamma \semi \Pi \vdexp v_1\ v_2: \tone \mid \mf{Mut}
}
\and
\inferrule*[right=TE-Case] {
  \Gamma \semi \Pi \vdval v : [\ptp_1]
  \and
  \Gamma \semi \Pi \vdexp e_1 : \ptp_2 \mid E
  \\
  \Gamma[x_\mf{hd} \mapsto \ptp_1, x_\mf{tl} \mapsto [\ptp_1]]
  \semi \Pi \vdexp e_2 : \ptp_2 \mid E
} {
  \Gamma \semi \Pi \vdexp \ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2} : \ptp \mid E
}
\and
\inferrule*[right=\hl{TE-Sub}] {
  \Gamma \semi \Pi \vdexp e: \ptp \mid \epsilon
} {
  \Gamma \semi \Pi \vdexp e: \ptp \mid \mf{Mut}
}
\end{mathpar}

\subsubsection{Top-Level Expression Typing}

Judgment: $\Gamma \semi \Pi \semi \C \vdtop \hat{e}$
\begin{mathpar}
\inferrule*[right=TH-Lift] {
  \Gamma \semi \Pi \vdexp e: \mf{View} \mid \epsilon
} {
  \Gamma \semi \Pi \semi \C \vdtop e
}
\and
\inferrule*[right=TH-Let] {
  \Gamma \semi \Pi \vdexp e: \ptp_1 \mid \epsilon
  \and
  \Gamma[x \mapsto \ptp_1] \semi \Pi \semi \C \vdtop \hat{e}
} {
  \Gamma \semi \Pi \semi \C \vdtop \elet{x}{e}{\hat{e}}
}
\and
\inferrule*[right=\hl{TH-State}] {
  \Delta[\C][\ell] = \ptp_1
  \and
  \Gamma \semi \Pi \vdval v: \ptp_1
  \\
  \Gamma[x \mapsto \ptp_1, x_\mf{set} \mapsto \mf{Setter}\ \ptp_1]
  \semi \Pi \semi \C \vdtop \hat{e}
} {
  \Gamma \semi \Pi \semi \C \vdtop \estate{x}{\ell}{v}{\hat{e}}
}
\end{mathpar}

\subsubsection{Program Typing}

Judgment: $\Gamma \vdprog P$

\begin{mathpar}
\inferrule*[right=TP-Lift] {
  \Gamma \semi \cdot \vdexp e : \mf{View} \mid \epsilon
} {
  \Gamma \vdprog e
}
\and
\inferrule*[right=TP-Let] {
  \Gamma \semi \cdot \vdexp e: \ptp \mid \epsilon
  \and 
  \Gamma[x \mapsto \ptp] \vdprog P
} {
  \Gamma \vdprog \elet{x}{e}{P}
}
\and
\inferrule*[right=\hl{TP-Def}] {
  \Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}
  \and
  \Gamma \vdprog P
} {
  \Gamma \vdprog \elet{\C(x)}{\hat{e}}{P}
}
\end{mathpar}

\subsubsection{Memory Typing}

Define $\Gamma \semi \Pi \vdm m$ if, for all $p \in \mf{dom}(m)$:
\begin{itemize}
\item for $m[p].\mf{spec}=\pair{\C}{v}$,
$\Pi[p] = \C$ and $\Gamma \semi \Pi \vdval v: \Delta[\C]$,
\item for all $\ell$ in $m[p].\mf{st}$,
$\Gamma \semi \Pi \vdval m[p].\mf{st}[\ell] : \Delta[\C][\ell]$, and
\item if $m[p].\mf{tree}$ is defined, then 
$\Gamma \semi \Pi \vdrt m[p].\mf{tree}: \mf{Tree}$.
\end{itemize}

Note that we have not yet defined what it means
for a runtime value such as $m[p].\mf{tree}$,
to be well-typed; we postpone this notion for later,
as doing so is unnecessary for the program-level proofs,
in which no trees are initialized.

\subsubsection{Component Context Typing}
Define $\Gamma \semi \Delta \vdd \D$ to say that,
for all $\C \in \dom(\D)$
(such that $\D[\C] = x.\hat{e}$),
we have $\Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}$.

\subsection{Program-Level Soundness}

Throughout, \emph{propositions} refer to (as-of-yet) unproved assumptions.

\subsubsection{General Lemmas}

We postulate weakening on $\Gamma$ for all typing judgments involving $\Gamma$.
For instance, the statement for weaking $\Gamma$ on values is:
\begin{proposition}[Type context weakening, values]
\label{oldthm:wktpv}
If $\Gamma \semi \Pi \vdval v: \ptp$ and $\Gamma \po \Gamma'$,
then $\Gamma' \semi \Pi \vdval v: \ptp$.
\end{proposition}

Similarly, we require weakening on $\Pi$ for all typing judgments involving $\Pi$:
\begin{proposition}[Path context weakening, values]
\label{oldthm:wkpiv}
If $\Gamma \semi \Pi \vdval v: \ptp$ and $\Pi \po \Pi'$,
then $\Gamma \semi \Pi' \vdval v: \ptp$.
\end{proposition}

\begin{proposition}[Path context weakening, top-level expressions]
\label{oldthm:wkpih}
If both $\Gamma \semi \Pi \semi \C \vdtop \hat{e}$ and $\Pi \po \Pi'$,
then $\Gamma \semi \Pi' \semi \C \vdtop \hat{e}$.
\end{proposition}

\begin{proposition}[Path context weakening, memory]
\label{oldthm:wkpim}
If both $\Gamma \semi \Pi \vdm m$ and $\Pi \po \Pi'$,
then $\Gamma \semi \Pi' \vdm m$.
\end{proposition}

We elide statements of all other weakening lemmas.

\begin{proposition}[Canonical Forms]
\label{oldthm:canon}
If $\cdot \semi \Pi \vdval v: \ptp$, then:
\begin{itemize}
\item if $\ptp=\tone$, then $v=()$.
\item if $\ptp=\tstr$, then $v=s$.
\item if $\ptp=\ptp_1 \xrightarrow{E} \ptp_2$, then $v=\lambda x: \ptp_1.e$.
\item if $\ptp=[\ptp']$, then either $v=[]$ or $v = v_1 :: v_2$.
\item if $\ptp=\mf{View}$, then either $v=\mf{text}\ v'$,
    $v=\mf{tag}(v' \num{v_i}, \num{v_j})$, or $v=\pair{\C}{v'}$.
\item if $\ptp=\mf{Attr}$, then either $v=\mf{attr}(v_1, v_2)$ or $v=\mf{onClick}\ v'$.
\item if $\ptp=\mf{Component}\ \ptp'$, then $v=\C$.
\item if $\ptp=\mf{Setter}\ \ptp'$, then $v=\vset{\ell}{p}$.
\end{itemize}
\end{proposition}

We postulate substitution lemmas on all typing judgments involving $\Gamma$.
\begin{proposition}[Substitution lemma, expressions.]
\label{oldthm:subexp}
If $\Gamma[x \mapsto \ptp_v] \semi \Pi \vdexp e: \ptp \mid E$
and $\Gamma \semi \Pi \vdval v: \ptp_v$,
then $\Gamma \semi \Pi \vdexp e[v/x]: \ptp \mid E$.
\end{proposition}

\begin{proposition}[Substitution lemma, top-level expressions.]
\label{oldthm:subtle}
If\\$\Gamma[x \mapsto \ptp_v] \semi \Pi \semi \C \vdtop \hat{e}$
and $\Gamma \semi \Pi \vdval v: \ptp_v$,
then $\Gamma \semi \Pi \semi \C \vdtop \hat{e}[v/x]$.
\end{proposition}

\begin{proposition}[Substitution lemma, programs.]
\label{oldthm:subprog}
If\\$\Gamma[x \mapsto \ptp_v] \vdprog P$
and $\Gamma \semi \cdot \vdval v: \ptp_v$,
then $\Gamma \vdprog \hat{e}[v/x]$.
\end{proposition}

Finally, we have the following properties regarding syntactic function $\mc{L}$:
\begin{proposition}[Label-set substitution]
\label{oldthm:sublab}
For all top-level expressions $\hat{e}$, values $v$, 
and variables $x$, 
$\mc{L}(\hat{e}[v/x]) = \mc{L}(\hat{e})$.
\end{proposition}

\begin{proposition}[Component-name substitution]
\label{oldthm:subname}
For all programs $P$, all values $v$, 
and all variables $x$, 
$\mc{L}(P[v/x]) = \mc{L}(P)$.
\end{proposition}

\subsubsection{Expressions}

\begin{lemma}[Progress of expressions.]
\label{oldthm:progexp}
If $\cdot \semi \Pi \vdexp e: \ptp \mid E$ and $\cdot \semi \Pi \vdm m$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $e = \eret{v}$, or
\item there exist some $e'$ and $m'$ such that
$m \semi e \mapsto e' \semi m'$.
\end{enumerate}


\begin{Proof}
By induction on the derivation of the expression typing judgment.

\tb{Case} \ts{TE-Ret}: We have 
$\cdot \semi \Pi \vdexp \eret{v}: \ptp \mid \epsilon$,
and the theorem holds trivially by case \emph{(i)}.

\tb{Case} \ts{TE-Let}: \begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdexp e_1 : \ptp_1 \mid E}
  \and
  \inferrule*[right=(2)] { } {
    [x \mapsto \ptp_1] \semi \Pi \vdexp e_2 : \ptp_2 \mid E
  }
} {
  \cdot \semi \Pi \vdexp \elet{x}{e_1}{e_2}: \ptp_2 \mid E
}
\end{mathpar}
\begin{itemize}
\item By inductive hypothesis on (1), we have two subcases on $e_1$:
either (a) $e_1 = \eret{v}$,
or (b) we have $m \semi e_1 \mapsto e_1' \semi m'$.
\begin{itemize}
\item Subcase (a): \ts{E-Ret} gives 
  $m \semi \elet{x}{\eret{v}}{e_2} \mapsto e_2[v/x] \semi m$.
\item Subcase (b):
  \begin{mathpar}
    \inferrule*[right=E-Let] {
  \estep{m}{e_1}{e_1'}{m'}
} {
  \estep{m}{\elet{x}{e_1}{e_2}}{\elet{x}{e_1'}{e_2}}{m'}
}
  \end{mathpar}
\end{itemize}
\item Thus, in any subcase, $\elet{x}{e_1}{e_2}$ steps,
and we satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppL}: \begin{mathpar}
\inferrule*[right=TE-AppL] {
  \cdot \semi \Pi \vdval v_1 : \ptp_1 \xrightarrow{E} \ptp_2
  \and
  \cdot \semi \Pi \vdval v_2 : \ptp_1
} {
  \cdot \semi \Pi \vdexp v_1\ v_2: \ptp_2 \mid E
}
\end{mathpar}\begin{itemize}
\item By canonical forms, $v_1=\lambda x.e$.
\item We apply \ts{E-AppL} to get that
$m \semi (\lambda x.e)\ v_2 \mapsto e[v_2/x] \semi m$,
satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppC}: \begin{mathpar}
\inferrule*[right=TE-AppC] {
  \cdot \semi \Pi \vdval v_1 : \mf{Component}\ \ptp
  \and
  \cdot \semi \Pi \vdval v_2 : \ptp
} {
  \cdot \semi \Pi \vdexp v_1\ v_2: \mf{View} \mid \epsilon
}
\end{mathpar}\begin{itemize}
\item By canonical forms, $v_1 = \C$.
\item \ts{E-AppC} gives
$m \semi \C\ v_2 \mapsto \eret{\pair{\C}{v_2}} \semi m$,

satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-AppS}: \begin{mathpar}
\inferrule*[right=TE-AppS] {
  \cdot \semi \Pi \vdval v_1 : \mf{Setter}\ \ptp
  \and
  \cdot \semi \Pi \vdval v_2 : \ptp
} {
  \cdot \semi \Pi \vdexp v_1\ v_2: \tone \mid \mf{Mut}
}
\end{mathpar}\begin{itemize}
\item By canonical forms, $v_1 = \vset{\ell}{p}$.
\item We apply \ts{E-AppS} to get that
$m \semi \vset{\ell}{p}\ v_2 \mapsto \eret{()} \semi m'$,
where $m' = m \mid m[p].\mf{st}[\ell] = v_2$ 
and $m[p].\mf{rerender} = \mf{tt}$,
satisfying conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-Case}: \begin{mathpar}
\inferrule*[right=TE-Case] {
  \cdot \semi \Pi \vdval v : [\ptp_1]
  \and
  \cdot \semi \Pi \vdexp e_1 : \ptp_2 \mid E
  \\
  [x_\mf{hd} \mapsto \ptp_1, x_\mf{tl} \mapsto [\ptp_1]]
  \semi \Pi \vdexp e_2 : \ptp_2 \mid E
} {
  \cdot \semi \Pi \vdexp \ecase{v}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2} : \ptp \mid E
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms), either $v=[]$ or $v=v_\mf{hd} :: v_\mf{tl}$, 
giving two subcases.
\begin{itemize}
\item If $v=[]$, then \ts{E-CaseN} gives\\
$m \semi \ecase{[]}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2} \mapsto e_1 \semi m$.
\item If $v=v_\mf{hd} :: v_\mf{tl}$, ththen \ts{E-CaseC} gives\\
$m \semi \ecase{v_\mf{hd} :: v_\mf{tl}}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2}
\mapsto e_2[v_\mf{hd} / x_\mf{hd}, v_\mf{tl} / x_\mf{tl}] \semi m$.
\end{itemize}
\item In either subcase, we satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TE-Sub}: \begin{mathpar}
\inferrule*[right=TE-Sub] {
  \Gamma \semi \Pi \vdexp e: \ptp \mid \epsilon
} {
  \Gamma \semi \Pi \vdexp e: \ptp \mid \mf{Mut}
}
\end{mathpar}
The result follows immediately from the inductive hypothesis
on the more conservative typing premise.
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of expressions.]
\label{oldthm:prezexp}
If
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \vdexp e: \ptp \mid E$,
\item $\Gamma \semi \Pi \vdm m$, and
\item $m \semi e \mapsto e' \semi m'$ for some $e'$ and $m'$,
\end{enumerate}
then
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \vdexp e': \ptp \mid E$ and
\item $\Gamma \semi \Pi \vdm m'$.
\end{enumerate}

\begin{Proof}
By induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{E-Let}: We have \begin{mathpar}
\inferrule*[right=E-Let] {
  \inferrule*[right=(1)] { } {m \semi e_1 \mapsto e_1' \semi m'}
} {
  m \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi m'
}
\and
\inferrule*[right=TE-Let] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdexp e_1 : \ptp_1 \mid E}
  \and
  \inferrule*[right=(3)] { } {
    \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e_2 : \ptp_2 \mid E
  }
} {
  \Gamma \semi \Pi \vdexp \elet{x}{e_1}{e_2}: \ptp_2 \mid E
}
\end{mathpar}\begin{itemize}
\item We apply the inductive hypothesis with (1), (2), and premise \emph{(ii)}
to get: \begin{itemize}
\item $\Gamma \semi \Pi \vdm m'$, satisfying conclusion \emph{(ii)}, and
\item (4) $\Gamma \semi \Pi \vdexp e_1': \ptp_1 \mid E$.
\end{itemize}
\item We reapply \ts{TE-Let} as follows to get conclusion \emph{(i)}:
\begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule*[right=(4)] { } {\Gamma \semi \Pi \vdexp e_1' : \ptp_1 \mid E}
  \and
  \inferrule*[right=(3)] { } {
    \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e_2 : \ptp_2 \mid E
  }
} {
  \Gamma \semi \Pi \vdexp \elet{x}{e_1'}{e_2}: \ptp_2 \mid E
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{E-Ret}: \begin{itemize}
\item We have $m \semi \elet{x}{\eret{v}}{e} \mapsto e[v/x] \semi m$ and:
\begin{mathpar}
\inferrule*[right=TE-Let] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdexp \eret{v} : \ptp_1 \mid E}
  \ 
  \inferrule*[right=(2)] { } {
    \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e : \ptp_2 \mid E
  }
} {
  \Gamma \vdexp \elet{x}{\eret{v}}{e}: \ptp_2 \mid E
}
\end{mathpar}
\item (1) lets us derive (3) $\Gamma \semi \Pi \vdval v: \ptp_1$
by subcasing on $E$ as follows:
\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \inferrule*[right=(3)] { } {\Gamma \semi \Pi \vdval v: \ptp_1}
} {
  \Gamma \semi \Pi \vdexp \eret{v}: \ptp_1 \mid \epsilon
}
\and
\inferrule*[right=TE-Sub] {
  \inferrule*[right=TE-Ret] {
    \inferrule*[right=(3)] { } {\Gamma \semi \Pi \vdval v: \ptp_1}
  } {
    \Gamma \semi \Pi \vdexp \eret{v}: \ptp_1 \mid \epsilon
  }
} {
  \Gamma \semi \Pi \vdexp \eret{v}: \ptp_1 \mid \mf{Mut}
}
\end{mathpar}
\item By \thref{oldthm:subexp} (substitution for expressions)
on (2) and (3), we get
$\Gamma \semi \Pi \vdexp e[v/x] : \ptp_2 \mid E$ and we are done.
\end{itemize}

\tb{Case} \ts{E-AppL}: \begin{itemize}
\item We have $m \semi (\lambda x.e) v \mapsto e[v/x] \semi m$ and:
\begin{mathpar}
\inferrule*[right=TE-AppL] {
  \inferrule*[right=TV-Lam] {
    \inferrule*[right=(1)] { } {
      \Gamma[x \mapsto \ptp_1] \semi \Pi \vdexp e: \ptp_2 \mid E
    }
  } {
    \Gamma \semi \Pi \vdval \lambda x.e : \ptp_1 \xrightarrow{E} \ptp_2
  }
  \:
  \inferrule*[right=(2)] { } {
    \Gamma \semi \Pi \vdval v_2 : \ptp_1
  }
} {
  \Gamma \semi \Pi \vdexp (\lambda x.e)\ v_2: \ptp_2 \mid E
}
\end{mathpar}
\item By \thref{oldthm:subexp} (substitution for expressions)
on (1) and (2), we get
$\Gamma \semi \Pi \vdexp e[v/x] : \ptp_2 \mid E$ and we are done.
\end{itemize}

\tb{Case} \ts{E-AppC}: \begin{itemize}
\item We have $m \semi \C\ v \mapsto \eret{\pair{\C}{v}} \semi m$ and:
\begin{mathpar}
\inferrule*[right=TE-AppC] {
  \inferrule*[right=(1)] { } {
    \Gamma \semi \Pi \vdval \C : \mf{Component}\ \ptp
  }
  \and
  \inferrule*[right=(2)] { } {
    \Gamma \semi \Pi \vdval v : \ptp
  }
} {
  \Gamma \semi \Pi \vdexp \C\ v: \mf{View} \mid \epsilon
}
\end{mathpar}
\item We can then derive conclusion \emph{(i)}:
\begin{mathpar}
\inferrule*[right=TE-Ret] {
  \inferrule*[right=TV-Spec] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval \C: \mf{Component}\ \ptp}
    \ 
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval v: \ptp}
  } {
    \Gamma \semi \Pi \vdval \pair{\C}{v} : \mf{View}
}
} {
  \Gamma \semi \Pi \vdexp \eret{\pair{\C}{v}}: \mf{View} \mid \epsilon
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{E-AppS}: \begin{itemize}
\item We have $m \semi \vset{\ell}{p}\ v \mapsto \eret{()} \semi m'$,
where $m' = m \mid m[p].\mf{st}[\ell] = v$ and $m[p].\mf{rerender} = \mf{tt}$.
\item The typing premise gives us:
\begin{mathpar}
\inferrule*[right=TE-AppS] {
  \inferrule*[right=TV-Set] {
    \inferrule*[right=(1)] { } {\Delta[\Pi[p]][\ell] = \ptp}
  } {
    \Gamma \semi \Pi \vdval \vset{\ell}{p}: \mf{Setter}\ \ptp
  }
  \and
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval v: \ptp}
} {
  \Gamma \semi \Pi \vdexp \vset{\ell}{p}\ v: \tone \mid \mf{Mut}
}
\end{mathpar}
\item Conclusion \emph{(i)} follows from the derivation:
\begin{mathpar}
\inferrule*[right=TE-Sub] {
  \inferrule*[right=TE-Ret] {
    \inferrule*[right=TV-Unit] { } {
      \Gamma \semi \Pi \vdval (): \tone
    }
  } {
    \Gamma \semi \Pi \vdexp \eret{()}: \tone \mid \epsilon
  }
} {
  \Gamma \semi \Pi \vdexp \eret{()}: \tone \mid \mf{Mut}
}
\end{mathpar}
\item To show $\Gamma \semi \Pi \vdm m'$ given premise $\Gamma \semi \Pi \vdm m$,
it suffices to show that 
$\Gamma \semi \Pi \vdval m'[p].\mf{st}[\ell] : \Delta[\Pi[p]][\ell]$.
\item By definition of $m'$, we want to show 
$\Gamma \semi \Pi \vdval v : \Delta[\Pi[p]][\ell]$.
\item This fact follows from (1) and (2), and we are done.
\end{itemize}

\tb{Case} \ts{E-CaseN}: \begin{itemize}
\item We have $m \semi \ecase{[]}{[]}{e_1}{x_\mf{hd} :: x_\mf{tl}}{e_2} \mapsto e_1 \semi m$ and:
\begin{mathpar}
\inferrule*[right=TE-If] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdexp e_1 : \ptp \mid E}
  \and \cdots
} {
  \Gamma \semi \Pi \vdexp \eif{v}{e_1}{e_2} : \ptp \mid E
}
\end{mathpar}
\item The conclusion follows immediately from (1).
\end{itemize}

\tb{Case} \ts{E-CaseC}: \begin{itemize}
\item We have $m \semi \ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2} 
\mapsto e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}] \semi m$ and:
\begin{mathpar}
\inferrule*[right=TE-If] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v_\mf{h} :: v_\mf{t} : [\ptp']}
  \and
  \inferrule*[right=(2)] { } {\Gamma' \semi \Pi \vdexp e_2 : \ptp \mid E}
  \ \cdots
} {
  \Gamma \semi \Pi \vdexp \eif{v}{e_1}{e_2} : \ptp \mid E
}
\end{mathpar}
where $\Gamma' = \Gamma[x_\mf{h} \mapsto \ptp', x_\mf{t} \mapsto [\ptp']]$.
\item We can further derive:
\begin{mathpar}
\inferrule*[right=TV-Cons] {
  \inferrule*[right=(3)] { } {\Gamma \semi \Pi \vdval v_\mf{h}: \ptp'}
  \and
  \inferrule*[right=(4)] { } {\Gamma \semi \Pi \vdval v_\mf{t}: [\ptp']}
} {
  \Gamma \semi \Pi \vdval v_\mf{h} :: v_\mf{t} : [\ptp']\ (1)
}
\end{mathpar}
\item We apply the substitution lemma twice to (2) using values (3) and (4)
to get that 
$\Gamma \semi \Pi \vdval e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}]: \ptp \mid E$
and we are done.
\end{itemize}
\end{Proof}
\end{lemma}

The above two lemmas are sufficient for type soundness of expressions,
but we also prove a "purity lemma" on expressions for later use:

\begin{lemma}[Purity of expressions.]
\label{oldthm:pure}
If $\cdot \semi \Pi \vdexp e: \ptp \mid \epsilon$
and $m \semi e \mapsto e' \semi m'$,
then $m' = m$.
\begin{Proof}
By induction on the derivation of the typing judgment.

\tb{Case} \ts{E-Ret}: We have \begin{mathpar}
\inferrule*[right=TE-Ret] {
  \cdot \semi \Pi \vdval v: \ptp
} {
  \cdot \semi \Pi \vdexp \eret{v}: \ptp \mid \epsilon
}
\end{mathpar}
But there is no rule that steps an expression of form $\eret{v}$,
so the theorem holds vacuously.

\tb{Case} \ts{TE-Let}: We have \begin{mathpar}
\inferrule*[right=E-Let] {
  \inferrule*[right=(1)] { } {m \semi e_1 \mapsto e_1' \semi m'}
} {
  m \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi m'
}
\and
\inferrule*[right=TE-Let] {
  \inferrule*[right=(2)] { } {\cdot \semi \Pi \vdexp e_1 : \ptp_1 \mid \epsilon}
  \and
  \inferrule*[right=(3)] { } {
    [x \mapsto \ptp_1] \semi \Pi \vdexp e_2 : \ptp_2 \mid \epsilon
  }
} {
  \cdot \semi \Pi \vdexp \elet{x}{e_1}{e_2}: \ptp_2 \mid \epsilon
}
\end{mathpar}
The inductive hypothesis on (1) and (2) gives that $m' = m$, and we are done.

\tb{Case} \ts{TE-AppL}: We have
\begin{mathpar}
\inferrule*[right=TE-AppL] {
  \inferrule*[right=(1)] { } {
    \cdot \semi \Pi \vdval v_1 : \ptp_1 \xrightarrow{\epsilon} \ptp_2
  }
  \and
  \cdot \semi \Pi \vdval v_2 : \ptp_1
} {
  \cdot \semi \Pi \vdexp v_1\ v_2: \ptp_2 \mid \epsilon
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on (1), $v_1 = \lambda x.e$.
\item The only step rule which applies is \ts{E-AppL}, which says
that $m \semi (\lambda x.e) v \mapsto e[v/x] \semi m$,
i.e. $m' = m$ and we are done.
\end{itemize}

\tb{Case} \ts{TE-AppC}: We have
\begin{mathpar}
\inferrule*[right=TE-AppC] {
  \inferrule*[right=(1)] { } {
    \cdot \semi \Pi \vdval v_1 : \mf{Component}\ \ptp
  }
  \and
  \cdot \semi \Pi \vdval v_2 : \ptp
} {
  \cdot \semi \Pi \vdexp v_1\ v_2: \mf{View} \mid \epsilon
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on (1), $v_1 = \C$.
\item The only step rule that applies is \ts{E-AppC}, which says
that $m \semi (\lambda x.e) v \mapsto \eret{\pair{\C}{v}} \semi m$,
i.e. $m' = m$ and we are done.
\end{itemize}

\tb{Case} \ts{TE-Case}: We have \begin{mathpar}
\inferrule*[right=TE-Case] {
  \cdot \semi \Pi \vdval v : [\ptp_1]
  \and
  \cdot \semi \Pi \vdexp e_1 : \ptp_2 \mid E
  \\
  [x_\mf{h} \mapsto \ptp_1, x_\mf{t} \mapsto [\ptp_1]]
  \semi \Pi \vdexp e_2 : \ptp_2 \mid E
} {
  \cdot \semi \Pi \vdexp \ecase{v}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2} : \ptp \mid E
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on (1), 
$v_1 = []$ or $v_1 = v_\mf{hd} :: v_\mf{tl}$.
\item If $v_1=[]$, then \ts{E-CaseN} says that\\
$m \semi \ecase{[]}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2} \mapsto e_1 \semi m$.
\item If $v_1=\mf{false}$, then \ts{E-CaseC} says that\\
$m \semi \ecase{v_\mf{h} :: v_\mf{t}}{[]}{e_1}{x_\mf{h} :: x_\mf{t}}{e_2} 
\mapsto e_2[v_\mf{h} / x_\mf{h}, v_\mf{t} / x_\mf{t}] \semi m$.
\item In either case, $m' = m$ and we are done.
\end{itemize}

\tb{Cases} \ts{TE-AppS} and \ts{TE-Sub}
are both impossible, since they type their conclusions
with the $\mf{Mut}$ effect.
\end{Proof}
\end{lemma}

\subsubsection{Top-Level Expressions}

\begin{lemma}[Progress of top-level expressions, initial phase.]
\label{oldthm:progtlei}
If\\[0.2mm]
$\cdot \semi \Pi \semi \Pi[p] \vdtop \hat{e}$ and $\cdot \semi \Pi \vdm m$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $\hat{e} = \eret{v}$, or
\item there exist some $\hat{e}'$ and $m'$ such that
$m \semi \hat{e} \topstep{\mf{Init}}{p} \hat{e}' \semi m'$.
\end{enumerate}

\begin{Proof}
By induction on the derivation of the expression typing judgment.

\tb{Case} \ts{TH-Lift}: \begin{mathpar}
\inferrule*[right=TH-Lift] {
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdexp e: \mf{View} \mid \epsilon}
} {
  \cdot \semi \Pi \semi \C \vdtop e
}
\end{mathpar}\begin{itemize}
\item By \thref{oldthm:progexp} (progress of expressions) on (1), we have two subcases:
either $e=\eret{v}$ (and we are done by conclusion \emph{(i)}), 
or we have some $e'$ and $m'$ such that
(2) $m \semi e \mapsto e' \semi m'$.
\item By \thref{oldthm:pure} (purity) on (1) and (2), $m' = m$.
\item We derive the conclusion as follows:
\begin{mathpar}
\inferrule*[right=EH-Lift] {
  m \semi e \mapsto e' \semi m
} {
  m \semi e \topstep{\mf{Init}}{p} e' \semi m
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{TH-Let}: \begin{mathpar}
\inferrule*[right=TH-Let] {
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdexp e: \ptp_1 \mid \epsilon}
  \and
  [x \mapsto \ptp_1] \semi \Pi \semi \C \vdtop \hat{e}
} {
  \cdot \semi \Pi \semi \C \vdtop \elet{x}{e}{\hat{e}}
}
\end{mathpar}\begin{itemize}
\item By \thref{oldthm:progexp} (progress of expressions) on (1), we have two subcases:
either (a) $e = \eret{v}$,
or (b) we have $m \semi e \mapsto e' \semi m'$.
\begin{itemize}
\item Subcase (a): \ts{EH-Ret} gives us that
$m \semi \elet{x}{\eret{v}}{\hat{e}} \topstep{\mf{Init}}{p} \hat{e}[v/x] \semi m$.
\item Subcase (b): by \thref{oldthm:pure} (purity) on (1), $m' = m$,
so we derive a step as follows:
\begin{mathpar}
\inferrule*[right=EH-Let] {
  m \semi e \mapsto e' \semi m
} {
  m \semi \elet{x}{e}{\hat{e}} \topstep{\mf{Init}}{p} \elet{x}{e'}{\hat{e}} \semi m
}
\end{mathpar}
\end{itemize}
\item Thus, in either subcase, we take a step and satisfy conclusion \emph{(ii)}.
\end{itemize}
\tb{Case} \ts{TH-State}: \begin{mathpar}
\inferrule*[right=TH-State] {
  \Delta[\C][\ell] = \ptp_1
  \and
  \cdot \semi \Pi \vdval v: \ptp_1
  \\
  [x \mapsto \ptp_1, x_\mf{set} \mapsto \mf{Setter}\ \ptp_1]
  \semi \Pi \semi \C \vdtop \hat{e}
} {
  \cdot \semi \Pi \semi \C \vdtop \estate{x}{\ell}{v}{\hat{e}}
}
\end{mathpar}
\begin{itemize}
\item Rule \ts{EH-StI} immediately applies, giving us
$$m \semi \estate{x}{\ell}{v}{\hat{e}}
\topstep{\mf{Init}}{p}
\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}] \semi m'$$
for $m' = m \mid m[p].\mf{st}[\ell] = v$.
\end{itemize}
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of top-level expressions, initial phase.]
\label{oldthm:preztlei}
If
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}$,
\item $\Gamma \semi \Pi \vdm m$ (with $p \in \dom(m)$), and
\item $m \semi \hat{e} \topstep{\mf{Init}}{p} \hat{e}' \semi m'$,
\end{enumerate}
then
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'$,
\item $\Gamma \semi \Pi \vdm m'$, and
\item $\mc{L}(\hat{e}) \cup \dom(m[p].\mf{st}) 
= \mc{L}(\hat{e}') \cup \dom(m'[p].\mf{st})$.
\end{enumerate}

\begin{Proof}
By induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EH-Lift}: We have \begin{mathpar}
\inferrule*[right=EH-Lift] {
  \inferrule*[right=(1)] { } {m \semi e \mapsto e' \semi m}
} {
  m \semi e \topstep{\mf{Init}}{p} e' \semi m
}
\and
\inferrule*[right=TH-Lift] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdexp e: \mf{View} \mid \epsilon}
} {
  \Gamma \semi \Pi \semi \C \vdtop e
}
\end{mathpar}\begin{itemize}
\item By \thref{oldthm:prezexp} (preservation of expressions)
on (1), (2), and premise \emph{(ii)},
we get (3) $\Gamma \semi \Pi \vdexp e': \mf{View} \mid \epsilon$.
\item We satisfy conclusion \emph{(i)} via the following derivation:
\begin{mathpar}
\inferrule*[right=TH-Lift] {
  \inferrule*[right=(3)] { } {\Gamma \semi \Pi \vdexp e': \mf{View} \mid \epsilon}
} {
  \Gamma \semi \Pi \semi \C \vdtop e'
}
\end{mathpar}
\item Since $\mc{L}(e)=\mc{L}(e')=\emptyset$ and $m' = m$,
we also get $\mc{L}(e) \cup \dom(m[p].\mf{st}) 
= \mc{L}(e') \cup \dom(m[p].\mf{st})$, i.e. conclusion \emph{(iii)} holds.
\item Conclusion \emph{(ii)} is trivial, and we are done.
\end{itemize}

\tb{Case} \ts{EH-Let}: We have \begin{mathpar}
\inferrule*[right=EH-Let] {
  \inferrule*[right=(1)] { } {m \semi e \mapsto e' \semi m}
} {
  m \semi \elet{x}{e}{\hat{e}} \topstep{\mf{Init}}{p} \elet{x}{e'}{\hat{e}} \semi m
}
\and
\inferrule*[right=TH-Let] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdexp e: \ptp \mid \epsilon}
  \and
  \inferrule*[right=(3)] { } {
    \Gamma[x \mapsto \ptp] \semi \Pi \semi \C \vdtop \hat{e}
  }
} {
  \Gamma \semi \Pi \semi \C \vdtop \elet{x}{e}{\hat{e}}
}
\end{mathpar}\begin{itemize}
\item By \thref{oldthm:prezexp} (preservation of expressions)
on (1), (2), and premise \emph{(ii)},
we get that (4) $\Gamma \semi \Pi \vdexp e': \ptp \mid \epsilon$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TH-Let] {
  \inferrule*[right=(4)] { } {\Gamma \semi \Pi \vdexp e': \ptp \mid \epsilon}
  \and
  \inferrule*[right=(3)] { } {
    \Gamma[x \mapsto \ptp] \semi \Pi \semi \C \vdtop \hat{e}
  }
} {
  \Gamma \semi \Pi \semi \C \vdtop \elet{x}{e}{\hat{e}}
}
\end{mathpar}
\item By definition, 
$\mc{L}(\elet{x}{e}{\hat{e}})=\mc{L}(\hat{e})=\mc{L}(\elet{x}{e'}{\hat{e}})$.
Since also $m'=m$, conclusion \emph{(iii)} holds.
\item Conclusion \emph{(ii)} is again trivial, and we are done.
\end{itemize}

\tb{Case} \ts{EH-Ret}:\begin{itemize}
\item We have
(1) $m \semi \elet{x}{\eret{v}}{\hat{e}} 
\topstep{\mf{Init}}{p} \hat{e}[v/x] \semi m$ 
and:
\begin{mathpar}
\inferrule*[right=TH-Let] {
  \inferrule*[right=TE-Ret] { 
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdexp v: \ptp}
  } {
    \Gamma \semi \Pi \vdexp \eret{v}: \ptp \mid \epsilon
  }
  \ 
  \inferrule*[right=(3)] { } {
    \Gamma[x \mapsto \ptp] \semi \Pi \semi \C \vdtop \hat{e}
  }
} {
  \Gamma \semi \Pi \semi \C \vdtop \elet{x}{\eret{v}}{\hat{e}}
}
\end{mathpar}
\item By \thref{oldthm:subtle} (substitution for top-level expressions) on (2) and (3),
we get that $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}[v/x]$,
satisfying conclusion \emph{(i)}.
\item By \thref{oldthm:sublab} (substitution for label-sets) and definition of $\mc{L}$,
we get $\mc{L}(\elet{x}{\eret{v}}{\hat{e}})=\mc{L}(\hat{e})=\mc{L}(\hat{e}[v/x])$.
Since also $m' = m$, conclusion \emph{(iii)} holds.
\item Conclusion \emph{(ii)} is once again trivial, and we are done.
\end{itemize}

\tb{Case} \ts{EH-StI}: \begin{itemize}
\item We have
$$m \semi \estate{x}{\ell}{v}{\hat{e}} \topstep{\mf{Init}}{p}
\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}] \semi m'$$
where $m' = m \mid m[p].\mf{st}[\ell] = v$, and:
\begin{mathpar}
\inferrule*[right=TH-State] {
  \Delta[\Pi[p]][\ell] = \ptp_1\ (1)
  \and
  \Gamma \semi \Pi \vdval v: \ptp_1\ (2)
  \\
  \Gamma[x \mapsto \ptp_1, x_\mf{set} \mapsto \mf{Setter}\ \ptp_1]
  \semi \Pi \semi \C \vdtop \hat{e}\ (3)
} {
  \Gamma \semi \Pi \semi \Pi[p] \vdtop \estate{x}{\ell}{v}{\hat{e}}
}
\end{mathpar}
\item We derive the setter type as follows:
\begin{mathpar}
\inferrule*[right=TV-Set] {
  \inferrule*[right=(1)] { } {\Delta[\Pi[p]][\ell] = \ptp_1}
} {
  \Gamma \semi \Pi \vdval \vset{\ell}{p} : \mf{Setter}\ \ptp_1\ (4)
}
\end{mathpar}
\item \thref{oldthm:subtle} (substitution for top-level expressions),
applied twice to (3) using (2) and (4),
yields $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]$,
satisfying conclusion \emph{(i)}.
\item To show conclusion \emph{(ii)} given premise \emph{(ii)}, 
it suffices to show that
$\Gamma \semi \Pi \vdval m'[p].\mf{st}[\ell] : \Delta[\Pi[p]][\ell]$.
By construction of $m'$, we need to show
$\Gamma \semi \Pi \vdval v : \Delta[\Pi[p]][\ell]$.
This is given by (1) and (2).
\item Finally, we need to show conclusion \emph{(iii)}.
By definition of $\mc{L}$, 
$$\mc{L}(\estate{x}{\ell}{v}{\hat{e}}) = \{\ell\} \cup \mc{L}(\hat{e})$$
and by \thref{oldthm:sublab} (substitution for label-sets), 
$\mc{L}(\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}])=\mc{L}(\hat{e})$.
By construction, $\dom(m'[p].\mf{st}) = \dom(m[p].\mf{st}) \cup \{\ell\}$.
Conclusion \emph{(iii)} now follows, and we are done:
$$
(\{\ell\} \cup \mc{L}(\hat{e})) \cup \dom(m[p].\mf{st}) 
= \mc{L}(\hat{e}) \cup (\dom(m[p].\mf{st}) \cup \{\ell\})
$$
\end{itemize}

\tb{Case} \ts{EH-StS}: Impossible,
since the judgment requires that $\phi=\mf{Succ}$.
\end{Proof}
\end{lemma}

\begin{lemma}[Progress of top-level expressions, successor phase.]
\label{oldthm:progtles}
If \begin{enumerate}[label=(\roman*)]
\item $\cdot \semi \Pi \semi \Pi[p] \vdtop \hat{e}$,
\item $\cdot \semi \Pi \vdm m$, and
\item $\mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})$,
\end{enumerate}
then either:
\begin{enumerate}[label=(\roman*)]
\item $\hat{e} = \eret{v}$, or
\item there exist some $\hat{e}'$ and $m'$ such that
$m \semi \hat{e} \topstep{\mf{Succ}}{p} \hat{e}' \semi m'$.
\end{enumerate}
\begin{Proof}
By induction over the derivation of the expression typing judgment.

\tb{Cases} \ts{TH-Lift}, \ts{TH-Let}:
Analogous to \thref{oldthm:progtlei}
(progress at initial phase).

\tb{Case} \ts{TH-State}: \begin{mathpar}
\inferrule*[right=TH-State] {
  \Delta[\C][\ell] = \ptp_1
  \and
  \cdot \semi \Pi \vdval v: \ptp_1
  \\
  [x \mapsto \ptp_1, x_\mf{set} \mapsto \mf{Setter}\ \ptp_1]
  \semi \Pi \semi \C \vdtop \hat{e}
} {
  \cdot \semi \Pi \semi \C \vdtop \estate{x}{\ell}{v}{\hat{e}}
}
\end{mathpar}
We want to apply step rule \ts{EH-StS}, which says:
$$m \semi \estate{x}{\ell}{v}{\hat{e}}
\topstep{\mf{Succ}}{p}
\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}] \semi m$$
where $v' = m[p].\mf{st}[\ell]$.
It suffices to show that $v'$ is well-defined.

By premise \emph{(iii)} and definition of $\mc{L}$,
we must have that $\ell \in \dom(m[p].\mf{st})$.
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of top-level expressions, successor phase.]
\label{oldthm:preztles}
If
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}$,
\item $\Gamma \semi \Pi \vdm m$ (with $p \in \dom(m)$), and
\item $m \semi \hat{e} \topstep{\mf{Succ}}{p} \hat{e}' \semi m'$,
\end{enumerate}
then
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'$,
\item $m = m'$, and
\item $\mc{L}(\hat{e}') \subseteq \mc{L}(\hat{e})$.
\end{enumerate}

\begin{Proof}
By induction on derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EH-Lift}: We have \begin{mathpar}
\inferrule*[right=EH-Lift] {
  m \semi e \mapsto e' \semi m
} {
  m \semi e \topstep{\mf{Succ}}{p} e' \semi m
}
\end{mathpar}\begin{itemize}
\item Conclusion \emph{(i)} is shown analogously to \thref{oldthm:preztlei}
(preservation at initial phase),
and conclusion \emph{(ii)} is trivial.
\item By definition, $\mc{L}(e)=\emptyset=\mc{L}(e')$,
so conclusion \emph{(iii)} holds and we are done.
\end{itemize}

\tb{Case} \ts{EH-Let}: We have \begin{mathpar}
\inferrule*[right=EH-Let] {
  m \semi e \mapsto e' \semi m
} {
  m \semi \elet{x}{e}{\hat{e}} \topstep{\phi}{p} \elet{x}{e'}{\hat{e}} \semi m
}
\end{mathpar}\begin{itemize}
\item Again, conclusion \emph{(i)} is shown analogously to \thref{oldthm:preztlei}
and conclusion \emph{(ii)} is trivial.
\item By definition, 
$\mc{L}(\elet{x}{e}{\hat{e}})=\mc{L}(\hat{e})=\mc{L}(\elet{x}{e'}{\hat{e}})$,
so conclusion \emph{(iii)} holds and we are done.
\end{itemize}

\tb{Case} \ts{EH-Ret}: \begin{itemize}
\item We have $m \semi \elet{x}{\eret{v}}{\hat{e}} 
\topstep{\mf{Init}}{p} \hat{e}[v/x] \semi m$.
\item Once again, conclusion \emph{(i)} is shown analogously to \thref{oldthm:preztlei}
and conclusion \emph{(ii)} is trivial.
\item By definition,
$\mc{L}(\elet{x}{e}{\hat{e}})=\mc{L}(\hat{e})$,
and by \thref{oldthm:sublab} (substitution for label-sets),
$\mc{L}(\hat{e}[v/x])=\mc{L}(\hat{e})$.
Therefore, conclusion \emph{(iii)} holds and we are done.
\end{itemize}

\tb{Case} \ts{EH-StI}: Impossible,
since the judgment requires that $\phi=\mf{Init}$.

\tb{Case} \ts{EH-StS}: 
\begin{itemize}
\item We have
$$m \semi \estate{x}{\ell}{v}{\hat{e}}
\topstep{\mf{Succ}}{p}
\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}] \semi m$$
where $v' = m[p].\mf{st}[\ell]$, and:
\begin{mathpar}
\inferrule*[right=TH-State] {
  \Delta[\Pi[p]][\ell] = \ptp_1\ (1)
  \and
  \Gamma \semi \Pi \vdval v: \ptp_1
  \\
  \Gamma[x \mapsto \ptp_1, x_\mf{set} \mapsto \mf{Setter}\ \ptp_1]
  \semi \Pi \semi \C \vdtop \hat{e}\ (2)
} {
  \Gamma \semi \Pi \semi \Pi[p] \vdtop \estate{x}{\ell}{v}{\hat{e}}
}
\end{mathpar}
\item As in \thref{oldthm:preztlei}, we derive the setter type as follows:
\begin{mathpar}
\inferrule*[right=TV-Set] {
  \inferrule*[right=(1)] { } {\Delta[\Pi[p]][\ell] = \ptp_1}
} {
  \Gamma \semi \Pi \vdval \vset{\ell}{p} : \mf{Setter}\ \ptp_1\ (3)
}
\end{mathpar}
\item By the memory typing premise \emph{(ii)},
we get that $\Gamma \semi \Pi \vdval v' : \Delta[\Pi[p]][\ell]$.
By (1), we get (4) that $\Gamma \semi \Pi \vdval v' : \ptp_1$.
\item Now, we apply \thref{oldthm:subtle} (substitution for top-level expressions)
twice to (2) using (3) and (4) to get that 
$\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}]$,
satisfying conclusion \emph{(i)}.
\item Conclusion \emph{(iii)} follows from definition of $\mc{L}$:
clearly $\mc{L}(\hat{e}) \subseteq \{\ell\} \cup \mc{L}(\hat{e})$.
\item Conclusion \emph{(ii)} is trivial, and we are done.
\end{itemize}

\end{Proof}

\end{lemma}

Later, we will also require the following (easy) lemma:
\begin{lemma}[Top-level preservation of specifications.]
\label{oldthm:prezspec}
If $m \semi \hat{e} \topstep{\phi}{p} \hat{e}' \semi m'$,
then $\forall p \in \dom(m). m[p].\mf{spec} = m'[p].\mf{spec}$.

\begin{Proof}
By induction over the step judgment. Cases \ts{EH-Lift},
\ts{EH-Let}, \ts{EH-Ret}, and \ts{EH-StS} are all trivial,
as they all have $m'=m$.

In case \ts{EH-StI}, we have $m' = m \mid m[p].\mf{st}[\ell] = v$.
But this does not change any specs between $m'$ and $m'$,
so the statement still holds and we are done.
\end{Proof}
\end{lemma}

\subsubsection{Programs}

\begin{lemma}[Progress of programs.]
\label{oldthm:progprog}
If $\cdot \vdprog P$ and $\cdot \semi \Delta \vdd \D$,
then either:
\begin{enumerate}[label=(\roman*)]
\item $P = \eret{v}$, or
\item there exist some $P'$ and $\D'$ such that
$\D \semi P \mapsto P' \semi \D'$.
\end{enumerate}
\begin{Proof}
By induction on the program typing derivation.

\tb{Case} \ts{TP-Lift}: \begin{mathpar}
\inferrule*[right=TP-Lift] {
  \inferrule*[right=(1)] { } {\Gamma \semi \cdot \vdexp e : \mf{View} \mid \epsilon}
} {
  \Gamma \vdprog e
}
\end{mathpar}
\begin{itemize}
\item By definition, the empty memory is well-typed 
by the empty component context, i.e. (2) $\cdot \semi \cdot \vdm \cdot$.
\item We apply \thref{oldthm:progexp} (progress of expressions)
on (1) and the empty memory
to get that either (a) $e = \eret{v}$ or (b) $\cdot \semi e \mapsto e' \semi m$.
\item In subcase (a), we are done by conclusion \emph{(i)}.
\item In subcase (b), we note by \thref{oldthm:pure} (purity) that $m=\cdot$.
We derive a step as follows, satisfying conclusion \emph{(ii)}:
\end{itemize}
\begin{mathpar}
\inferrule*[right=EP-Lift] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  \D \semi e \mapsto e' \semi \D
}
\end{mathpar}

\tb{Case} \ts{TP-Let}: \begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule*[right=(1)] { } {
    \cdot \semi \cdot \vdexp e: \ptp \mid \epsilon
  }
  \and 
  [x \mapsto \ptp] \vdprog P
} {
  \cdot \vdprog \elet{x}{e}{P}
}
\end{mathpar}
\begin{itemize}
\item We again apply \thref{oldthm:progexp}
on (1) and the empty memory
to get that either (a) $e = \eret{v}$ or (b) $\cdot \semi e \mapsto e' \semi m$.
\begin{itemize}
\item Subcase (a): \ts{EP-Ret} gives us that
$\D \semi \elet{x}{\eret{v}}{P} \mapsto P[v/x] \semi \D$.
\item Subcase (b): by \thref{oldthm:pure} (purity) on (1), $m' = \cdot$,
so we derive a step as follows:
\begin{mathpar}
\inferrule*[right=EP-Let] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  \D \semi \elet{x}{e}{P}
  \mapsto
  \elet{x}{e'}{P} \semi \D
}
\end{mathpar}
\end{itemize}
\end{itemize}

\tb{Case} \ts{TP-Def}: \begin{mathpar}
\inferrule*[right=TP-Def] {
  [x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}
  \and
  \cdot \vdprog P
} {
  \cdot \vdprog \elet{\C(x)}{\hat{e}}{P}
}
\end{mathpar}
Rule \ts{EP-Def} immediately applies,
giving us $$\D \semi \elet{\C(x)}{\hat{e}}{P}
\mapsto
P \semi \D[\C \mapsto x.\hat{e}]$$
and we are done.
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of programs.]
\label{oldthm:prezprog}
If
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \vdprog P$,
\item $\Gamma \semi \Delta \vdd \D$, and
\item $\D \semi P \mapsto P' \semi \D'$,
\end{enumerate}
then
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \vdprog P'$,
\item $\Gamma \semi \Delta \vdd \D'$, and
\item $\mc{L}(P') \cup \dom(\D') = \mc{L}(P) \cup \dom(\D)$.
\end{enumerate}

\begin{Proof}
By induction on the derivation of the step judgment \emph{(iii)}.

\tb{Case} \ts{EP-Lift}: We have \begin{mathpar}
\inferrule*[right=EP-Lift] {
  \inferrule*[right=(1)] { } {\cdot \semi e \mapsto e' \semi \cdot}
} {
  \D \semi e \mapsto e' \semi \D
}
\and
\inferrule*[right=TP-Lift] {
  \inferrule*[right=(2)] { } {\Gamma \semi \cdot \vdexp e : \mf{View} \mid \epsilon}
} {
  \Gamma \vdprog e
}
\end{mathpar}
\begin{itemize}
\item \thref{oldthm:prezexp} (preservation of expressions)
on (1), (2), and the empty memory gives us (3) that 
$\Gamma \semi \cdot \vdexp e': \mf{View} \mid \epsilon$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TP-Lift] {
  \inferrule*[right=(3)] { } {\Gamma \semi \cdot \vdexp e': \mf{View} \mid \epsilon}
} {
  \Gamma \vdprog e'
}
\end{mathpar}
\item Conclusion \emph{(ii)} is trivial. Conclusion \emph{(iii)}
follows from definition of $\mc{L}$: we have $\mc{L}(e)=\emptyset=\mc{L}(e')$.
\end{itemize}

\tb{Case} \ts{EP-Let}: We have \begin{mathpar}
\inferrule*[right=EP-Let] {
  \inferrule*[right=(1)] { } {\cdot \semi e \mapsto e' \semi \cdot}
} {
  \D \semi \elet{x}{e}{P}
  \mapsto
  \elet{x}{e'}{P} \semi \D
}
\and
\inferrule*[right=TP-Let] {
  \inferrule*[right=(2)] { } {\Gamma \semi \cdot \vdexp e: \ptp \mid \epsilon}
  \and 
  \inferrule*[right=(3)] { } {\Gamma[x \mapsto \ptp] \vdprog P}
} {
  \Gamma \vdprog \elet{x}{e}{P}
}
\end{mathpar}
\begin{itemize}
\item \thref{oldthm:prezexp}
on (1), (2), and the empty memory gives us (4) that 
$\Gamma \semi \cdot \vdexp e': \ptp \mid \epsilon$.
\item We derive conclusion \emph{(i)} as follows:
\begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule*[right=(4)] { } {\Gamma \semi \cdot \vdexp e': \ptp \mid \epsilon}
  \and 
  \inferrule*[right=(3)] { } {\Gamma[x \mapsto \ptp] \vdprog P}
} {
  \Gamma \vdprog \elet{x}{e'}{P}
}
\end{mathpar}
\item Conclusion \emph{(ii)} is again trivial. Conclusion \emph{(iii)}
follows from definition of $\mc{L}$: we have 
$\mc{L}(\elet{x}{e}{P})=\mc{L}(P)=\mc{L}(\elet{x}{e'}{P})$.
\end{itemize}

\tb{Case} \ts{EP-Ret}: We have
$\D \semi \elet{x}{\eret{v}}{P} \mapsto P[v/x] \semi \D$ and 
\begin{mathpar}
\inferrule*[right=TP-Let] {
  \inferrule*[right=TE-Ret] {
    \inferrule*[right=(1)] { } {
      \Gamma \semi \cdot \vdval v: \ptp
    }
  } {
    \Gamma \semi \cdot \vdexp \eret{v}: \ptp \mid \epsilon
  }
  \and 
  \inferrule*[right=(2)] { } {\Gamma[x \mapsto \ptp] \vdprog P}
} {
  \Gamma \vdprog \elet{x}{\eret{v}}{P}
}
\end{mathpar}
\begin{itemize}
\item \thref{oldthm:subprog} (substitution for programs) on (1) and (2)
gives conclusion \emph{(i)}, that $\Gamma \vdprog P[v/x]$.
\item For conclusion \emph{(iii)}, we have
$\mc{L}(\elet{x}{\eret{v}}{P})=\mc{L}(P)$ by definition.
We get that $\mc{L}(P)=\mc{L}(P[v/x])$
from \thref{oldthm:subname} (component-name substitution).
\item Conclusion \emph{(ii)} is once again trivial, and we are done.
\end{itemize}

\tb{Case} \ts{EP-Def}: We have
$\D \semi \elet{\C(x)}{\hat{e}}{P}
\mapsto
P \semi \D[\C \mapsto x.\hat{e}]$ and
\begin{mathpar}
\inferrule*[right=TP-Def] {
  \inferrule*[right=(1)] { } {
    \Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}
  }
  \and
  \inferrule*[right=(2)] { } {\Gamma \vdprog P}
} {
  \Gamma \vdprog \elet{\C(x)}{\hat{e}}{P}
}
\end{mathpar}
\begin{itemize}
\item Conclusion \emph{(i)} is immediate from (2).
\item To show conclusion \emph{(ii)} given premise \emph{(ii)},
it suffices to show that 
$\Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}$.
This is immediate from (1).
\item For conclusion \emph{(iii)},
we have that $\mc{L}(\elet{\C(x)}{\hat{e}}{P}) = \{\C\} \cup \mc{L}(P)$
by definition, and that
$\dom(\D') = \{\C\} \cup \dom(\D)$ by construction of $\D'$.
Conclusion \emph{(iii)} follows, and we are done:
$$\mc{L}(P) \cup (\{\C\} \cup \dom(\D)) 
= 
(\{\C\} \cup \mc{L}(P)) \cup \dom(\D)$$
\end{itemize}
\end{Proof}
\end{lemma}

\subsection{Runtime Typing Rules}

Judgment: $\Gamma \semi \Pi \vdrt \rho: R$
\begin{mathpar}
\inferrule*[right=TR-Done] { } {
  \Gamma \semi \Pi \vdrt \mf{done}: \tone_\rho
}
\and
\inferrule*[right=TR-Nil] { } {
  \Gamma \semi \Pi \vdrt []_\rho: [R]_\rho
}
\and
\inferrule*[right=TR-Cons] {
  \Gamma \semi \Pi \vdrt \rho_\mf{hd} : R
  \and
  \Gamma \semi \Pi \vdrt \rho_\mf{tl} : [R]_\rho
} {
  \Gamma \semi \Pi \vdrt \rho_\mf{hd} :: \rho_\mf{tl} : [R]_\rho
}
\and
\inferrule*[right=TR-Lit] {
  \Gamma \semi \Pi \vdval v: \tstr
} {
  \Gamma \semi \Pi \vdrt \mf{lit}\ v: \mf{Tree}
}
\and
\inferrule*[right=TR-Node] {
  \Gamma \semi \Pi \vdval v: [\mf{Attr}]
  \and
  \Gamma \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho
} {
  \Gamma \semi \Pi \vdrt \mf{node}(\mf{id}, v, \rho): \mf{Tree}
}
\and
\inferrule*[right=TR-Path] {
  p \in \dom(\Pi) \and \Pi[p] \in \dom(\Delta)
} {
  \Gamma \semi \Pi \vdrt p: \mf{Tree}
}
\and
\inferrule*[right=TR-Init] {
  \Gamma \semi \Pi \vdval v: \mf{View}
} {
  \Gamma \semi \Pi \vdrt \mf{init}\ v: \mf{Tree}
}
\and
\inferrule*[right=TR-Inits] {
  \Gamma \semi \Pi \vdval v: [\mf{View}]
} {
  \Gamma \semi \Pi \vdrt \mf{inits}\ v: [\mf{Tree}]_\rho
}
\and
\inferrule*[right=TR-Sc] {
  \Gamma \semi \Pi \vdval v: \mf{Attr}
} {
  \Gamma \semi \Pi \vdrt \mf{scan}\ v: \tone_\rho
}
\and
\inferrule*[right=TR-Scs] {
  \Gamma \semi \Pi \vdval v: [\mf{Attr}]
} {
  \Gamma \semi \Pi \vdrt \mf{scans}\ v: \tone_\rho
}
\and
\inferrule*[right=TR-Chk] {
  \Gamma \semi \Pi \vdrt \rho: \mf{Tree}
} {
  \Gamma \semi \Pi \vdrt \mf{check}\ \rho: \tone_\rho
}
\and
\inferrule*[right=TR-Chks] {
  \Gamma \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho
} {
  \Gamma \semi \Pi \vdrt \mf{checks}\ \rho: \tone_\rho
}
\and
\inferrule*[right=TR-Srch] {
  \Gamma \semi \Pi \vdrt \rho: \mf{Tree}
} {
  \Gamma \semi \Pi \vdrt \mf{search}\ \rho: \tone_\rho
}
\and
\inferrule*[right=TR-Srchs] {
  \Gamma \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho
} {
  \Gamma \semi \Pi \vdrt \mf{searches}\ \rho: \tone_\rho
}
\and
\inferrule*[right=TR-Eval] {
  \Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\phi_p\ \hat{e}: R(\phi)
}
\and
\inferrule*[right=TR-New] {
  \Gamma \semi \Pi \vdrt \rho: \mf{Tree}
} {
  \Gamma \semi \Pi \vdrt \mf{new}^\phi_p\ \rho: R(\phi)
}
\and
\inferrule*[right=TR-Hdl] {
  \Gamma \semi \Pi \vdexp e: \tone \mid E
} {
  \Gamma \semi \Pi \vdrt \mf{handle}\ e: \tone_\rho
}
\and
\inferrule*[right=TR-Prog] {
  \Gamma \semi \Pi \vdprog P
  \and
  \Delta[\C_0] = \tone
} {
  \Gamma \semi \Pi \vdrt \mf{prog}\ P: \tone_\rho
}
\and
\inferrule*[right=TR-Seq] {
  \Gamma \semi \Pi \vdrt \rho_1: \tone_\rho
  \and
  \Gamma \semi \Pi \vdrt \rho_2: R
} {
  \Gamma \semi \Pi \vdrt \rho_1 \semi \rho_2: R
}
\end{mathpar}

Define $\Gamma \semi \Pi \vdcfg \trip{\D}{m}{\rho}: R$
to abbreviate that
(i) $\Gamma \semi \Delta \vdd \D$,
(ii) $\Gamma \semi \Pi \vdm m$, and
(iii) $\Gamma \semi \Pi \vdrt \rho: R$.

\subsection{Well-Formedness of Configurations}

In addition to the typing judgment,
we require a \emph{well-formedness} judgment on
configurations, written $\D \semi m \wf \rho$,
that describes which components have been defined,
and which labels and paths have been initialized.

\subsubsection{Component Definitions}
\begin{mathpar}
\inferrule*[right=WF-Prog] {
  \mc{L}(P) \cup \dom(\D) \cup \{\C_0\}= \dom(\Delta)
} {
  \D \semi m \wf \mf{prog}\ P
}
\end{mathpar}
\emph{All other rules} have the implicit premise that $\dom(\D) = \dom(\Delta)$.

\subsubsection{Label/Path Initialization}

Define, as abbreviations in the \emph{premises} of all rules
in this section, $m[p].\mf{spec} = \pair{\C}{v}$ and $\D[\C] = x.\hat{e}_0$.
(Thus, as implicit premises, we must have $m[p]$ and $\D[\C]$ defined.)
\begin{mathpar}
\inferrule*[right=WF-Path] {
  \mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})
  \and
  \D \semi m \wf m[p].\mf{tree}
} {
  \D \semi m \wf p
}
\and
\inferrule*[right=WF-EvalI] {
  \mc{L}(\hat{e}) \cup \dom(m[p].\mf{st}) = \mc{L}(\hat{e}_0)
} {
  \D \semi m \wf \mf{eval}^\mf{Init}_p\ \hat{e}
}
\and
\inferrule*[right=WF-EvalS] {
  \mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})
  \and
  \mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})
} {
  \D \semi m \wf \mf{eval}^\mf{Succ}_p\ \hat{e}
}
\and
\inferrule*[right=WF-New] {
  \mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})
  \and
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{new}^\phi_p\ \rho
}
\end{mathpar}

\subsubsection{Boring Rules}

\begin{align*}
\D \semi m &\wf \mf{done} &&\ts{WF-Done}\\
\D \semi m &\wf []_\rho &&\ts{WF-Nil}\\
\D \semi m &\wf \mf{lit}\ v &&\ts{WF-Lit}\\
\D \semi m &\wf \mf{init}\ v &&\ts{WF-Init}\\
\D \semi m &\wf \mf{inits}\ v &&\ts{WF-Inits}\\
\D \semi m &\wf \mf{scan}\ v &&\ts{WF-Scan}\\
\D \semi m &\wf \mf{scans}\ v &&\ts{WF-Scans}\\
\D \semi m &\wf \mf{handle}\ e &&\ts{WF-Hdl}\\
\end{align*}

\begin{mathpar}
\inferrule*[right=WF-Cons] {
  \D \semi m \wf \rho_\mf{hd}
  \and
  \D \semi m \wf \rho_\mf{tl}
} {
  \D \semi m \wf \rho_\mf{hd} :: \rho_\mf{tl}
}
\and
\inferrule*[right=WF-Node] {
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{node}(\mf{id}, v, \rho)
}
\and
\inferrule*[right=WF-Chk] {
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{check}\ \rho
}
\and
\inferrule*[right=WF-Chks] {
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{checks}\ \rho
}
\and
\inferrule*[right=WF-Srch] {
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{search}_\mf{id}\ \rho
}
\and
\inferrule*[right=WF-Srchs] {
  \D \semi m \wf \rho
} {
  \D \semi m \wf \mf{searches}_\mf{id}\ \rho
}
\and
\inferrule*[right=WF-Seq] {
  \D \semi m \wf \rho_1
  \and
  \D \semi m \wf \rho_2
} {
  \D \semi m \wf \rho_1 \semi \rho_2
}
\end{mathpar}

\subsection{Configuration Soundness}

\begin{lemma}[Progress of configurations.]
\label{oldthm:progcfg}
If $\cdot \semi \Pi \vdcfg \trip{\D}{m}{\rho}: R$
and also $\D \semi m \wf \rho$,
then either:
\begin{enumerate}
\item $\rho = u$ (i.e. $\rho$ is a value), or
\item there exist some $\D'$, $m'$, and $\rho'$ such that
$\trip{\D}{m}{\rho}\hookrightarrow \trip{\D'}{m'}{\rho'}$.
\end{enumerate}
\begin{Proof}
By definition of $\vdcfg$, we have sub-premises:
\begin{enumerate}
\item $\Delta \vdd \D$,
\item $\cdot \semi \Pi \vdm m$, and
\item $\cdot \semi \Pi \vdrt \rho: R$.
\end{enumerate}
We proceed by induction over the derivation of \emph{(iii)}.

\tb{Case} 
\ts{TR-Done}, 
\ts{TR-Nil},
\ts{TR-Lit},
\ts{TR-Path}: 
Trivial by conclusion \emph{(i)}. Respectively,
$\mf{done}$, $[]_\rho$, $\mf{lit}\ v$ (for any $v$), and $p$
are values.

\tb{Case} \ts{TR-Cons}: \begin{mathpar}
\inferrule*[right=TR-Cons] {
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdrt \rho_\mf{hd} : R}
  \and
  \inferrule*[right=(2)] { } {\cdot \semi \Pi \vdrt \rho_\mf{tl} : [R]}
} {
  \cdot \semi \Pi \vdrt \rho_\mf{hd} :: \rho_\mf{tl} : [R]
}
\and
\inferrule*[right=WF-Cons] {
  \inferrule*[right=(3)] { } {\D \semi m \wf \rho_\mf{hd}}
  \and
  \inferrule*[right=(4)] { } {\D \semi m \wf \rho_\mf{tl}}
} {
  \D \semi m \wf \rho_\mf{hd} :: \rho_\mf{tl}
}
\end{mathpar}
\begin{itemize}
\item By \emph{(i)}, \emph{(ii)}, and (1), we have 
(5) $\cdot \semi \Pi \vdcfg \trip{\D}{m}{\rho_\mf{hd}}$.
\item Inductive hypothesis on (3) and (5) gives either
(6a) that $\trip{\D}{m}{\rho_\mf{hd}}\hookrightarrow \trip{\D'}{m'}{\rho'_\mf{hd}}$
or (6b) that $\rho_\mf{hd} = u_\mf{hd}$.
\begin{itemize}
\item In subcase (6a), we apply \ts{ER-Ctx} with $\E=[\cdot] ::_\rho \rho_\mf{tl}$,
satisfying conclusion \emph{(ii)}:
\begin{mathpar}
\inferrule*[right=ER-Ctx] {
  \inferrule*[right=\tn{(6a)}] { } {
    \trip{\D}{m}{\rho_\mf{hd}} \hookrightarrow \trip{\D'}{m'}{\rho_\mf{hd}'}
  }
} {
  \trip{\D}{m}{\rho_\mf{hd} ::_\rho \rho_\mf{tl}}
  \hookrightarrow 
  \trip{\D'}{m'}{\rho_\mf{hd}' ::_\rho \rho_\mf{tl}}
}
\end{mathpar}
\end{itemize}
\item Otherwise, we get (7) that 
$\cdot \semi \Pi \vdcfg \trip{\D}{m}{\rho_\mf{tl}}$ via 
\emph{(i)}, \emph{(ii)}, and (2),
and we apply the inductive hypothesis to (3) and (7)
to get two more subcases: either 
(8a) $\trip{\D}{m}{\rho_\mf{tl}}\hookrightarrow \trip{\D'}{m'}{\rho'_\mf{tl}}$
or (8b) $\rho_\mf{tl} = u_\mf{tl}$.
\item In subcase (8a), we apply \ts{ER-Ctx} with $\E= u_\mf{hd} ::_\rho [\cdot]$,
satisfying conclusion \emph{(ii)}:
\begin{mathpar}
\inferrule*[right=ER-Ctx] {
  \inferrule*[right=\tn{(8a)}] { } {
    \trip{\D}{m}{\rho_\mf{tl}} \hookrightarrow \trip{\D'}{m'}{\rho_\mf{tl}'}
  }
} {
  \trip{\D}{m}{u_\mf{hd} ::_\rho \rho_\mf{tl}}
  \hookrightarrow 
  \trip{\D'}{m'}{u_\mf{hd} ::_\rho \rho_\mf{tl}'}
}
\end{mathpar}
\item Otherwise, by (6b) and (8b), we have that $\rho = u_\mf{hd} ::_\rho u_\mf{tl}$,
i.e. $\rho$ is a value, satisfying conclusion \emph{(i)}.
\end{itemize}

\tb{Case} \ts{TR-Node}: \begin{mathpar}
\inferrule*[right=TR-Node] {
  \cdot \semi \Pi \vdval v: [\mf{Attr}]
  \and
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho}
} {
  \cdot \semi \Pi \vdrt \mf{node}(\mf{id}, v, \rho): \mf{Tree}
}
\and
\inferrule*[right=WF-Node] {
  \inferrule*[right=(2)] { } {\D \semi m \wf \rho}
} {
  \D \semi m \wf \mf{node}(\mf{id}, v, \rho)
}
\end{mathpar}
\begin{itemize}
\item By \emph{(i)}, \emph{(ii)}, and (1), we have 
(3) $\cdot \semi \Pi \vdcfg \trip{\D}{m}{\rho}$.
We apply the inductive hypothesis to (2) and (3) to get that either
(4a) $\rho = u$ or (4b) 
$\trip{\D}{m}{\rho}
\hookrightarrow 
\trip{\D'}{m'}{\rho'}$.
\item In subcase (4a), we have that $\mf{node}(\mf{id}, v, u)$ is a value,
satisfying conclusion \emph{(i)}.
\item In subcase (4b), we apply \ts{ER-Ctx} with $\E=\mf{node}(\mf{id}, v, [\cdot])$
to satisfy conclusion \emph{(ii)} as follows:
\begin{mathpar}
\inferrule*[right=ER-Ctx] {
  \inferrule*[right=\tn{(4b)}] { } {
    \trip{\D}{m}{\rho} \hookrightarrow \trip{\D'}{m'}{\rho'}
  }
} {
  \trip{\D}{m}{\mf{node}(\mf{id}, v, \rho)}
  \hookrightarrow 
  \trip{\D'}{m'}{\mf{node}(\mf{id}, v, \rho')}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{TR-Init}: \begin{mathpar}
\inferrule*[right=TR-Init] {
  \inferrule*[right=(1)] { } {\cdot \semi \Pi \vdval v: \mf{View}}
} {
  \cdot \semi \Pi \vdrt \mf{init}\ v: \mf{Tree}
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on (1),
we have three subcases: either
(a) $v=\mf{text}\ v'$,
(b) $v=\mf{tag}(v_1, v_2)$, or
(c) $v=\pair{\C}{v_0}$.
\item In subcase (a), \ts{ER-IText} simply says
$\trip{\D}{m}{\mf{init\ text}\ v'} \hookrightarrow
\trip{\D}{m}{\mf{lit}\ v'}$.
\item In subcase (b), we apply \ts{ER-ITag} (with some fresh $\mf{id}$)
to get that $\trip{\D}{m}{\mf{init\ tag}(v_1, v_2)} \hookrightarrow
\trip{\D}{m}{\mf{node}(\mf{id}, v_1, \mf{inits}\ v_2)}$.
\item (HERE DOWN NEEDS REFACTORING)
\item In subcase (c), we appeal to our well-formedness premise:
\begin{mathpar}
\inferrule*[right=WF-Init] {
  \inferrule*[right=WF-Spec] {
    \inferrule*[right=(2)] { } {\C \in \D}
  } {
    \D \wf \pair{\C}{v_0}
  }
} {
  \D \semi m \wf \mf{init}\ \pair{\C}{v_0}
}
\end{mathpar}
\item By (2), we can now apply \ts{ER-ISpec} (with some fresh $p$:)
\begin{mathpar}
\inferrule*[right=ER-ISpec] {
  p\ \mf{fresh}
  \and 
  \inferrule*[right=(2)] { } {\D[\C]=x.\hat{e}}
  \\
  \pi_0=\{
    \mf{spec}=\pair{\C}{v_0}, 
    \mf{st}: \cdot, 
    \mf{tree}: \cdot, 
    \mf{rerender}: \mf{ff}
  \}
} {
  \trip{\D}{m}{\mf{init}\ \pair{\C}{v}}
  \hookrightarrow
  \trip{\D}{m[p \mapsto \pi_0]}{\mf{eval}^\mf{Init}_p\ \hat{e}[v/x]}
}
\end{mathpar}
\item Thus, in any subcase, we satisfy conclusion \emph{(ii)}.
\end{itemize}

\tb{Case} \ts{TR-Inits}: IH/canonical lists, inil/icons

\tb{Case} \ts{TR-Scan}: \begin{mathpar}
\inferrule*[right=TR-Scan] {
  \cdot \semi \Pi \vdval v: \mf{Attr}
} {
  \cdot \semi \Pi \vdrt \mf{scan}\ v: \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on $v$,
either (a) $v=\mf{attr}(v_1, v_2)$ or (b) $v=\mf{onClick}\ v'$.
\item In subcase (a), \ts{ER-ScAttr} says
$\trip{\D}{m}{\mf{scan}\ \mf{attr}(v_1, v_2)} \hookrightarrow \trip{\D}{m}{\mf{done}}$.
\item In subcase (b), we apply \ts{ER-ScClick} to get
$\trip{\D}{m}{\mf{scan}\ \mf{onClick}(v')} \hookrightarrow \trip{\D}{m}{\mf{handle}\ (v\ ())}$.
\item Thus, in either subcase we satisfy conclusion \emph{{ii}}.
\end{itemize}

\tb{Case} \ts{TR-Scans}: \begin{mathpar}
\inferrule*[right=TR-Scans] {
  \Gamma \semi \Pi \vdval v: [\mf{Attr}]
} {
  \Gamma \semi \Pi \vdrt \mf{scans}\ v: \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item By \thref{oldthm:canon} (canonical forms) on $v$,
we have two subcases: (a) $v=[]$, or else (b) $v=v_\mf{hd} :: v_\mf{tl}$.
\item In subcase (a), \ts{ER-ScsNil} gives
$\trip{\D}{m}{\mf{scans}\ []} \hookrightarrow \trip{\D}{m}{\mf{done}}$.
\item In subcase (b), we apply \ts{ER-ScsCons} to get
$\trip{\D}{m}{\mf{scans}\ v_\mf{hd} :: v_\mf{tl}} 
\hookrightarrow \trip{\D}{m}{\mf{scan}\ v_\mf{hd} \semi \mf{scans}\ v_\mf{tl}}$.
\item Thus, in either subcase we satisfy conclusion \emph{{ii}}.
\end{itemize}

\tb{Case} \ts{TR-Chk}: IH on $\rho$, if not val then e-ctx,
if val then canonical on rho: either lit, node, or p; rules for each
(wf-path tells us m[p], d[c] are defined)

\tb{Case} \ts{TR-Chks}: IH/canonical on lists, chksnil/chkscons

\tb{Case} \ts{TR-Srch}: IH/e-ctx or canonically tree => step,
m[p].tree defined by wf p

\tb{Case} \ts{TR-Srchs}: standard for lists

\tb{Case} \ts{TR-Eval $(\phi=\mf{Init})$}: progtle-i,
if e steps then er-eval, else ret v then er-new

\tb{Case} \ts{TR-Eval $(\phi=\mf{Succ})$}: analogous,
but uses wf propery that $\mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})$
to apply progtle-s

\tb{Case} \ts{TR-New}: IH/e-ctx, canon tree -> step newi/news

\tb{Case} \ts{TR-Hdl}: progexp, er-hdl or canon unit -> er-hdldone

\tb{Case} \ts{TR-Prog}: progprog, er-prog or canon end -> er-pdone

\tb{Case} \ts{TR-Seq}: IH/e-ctx, canon done -> er-seq
\end{Proof}
\end{lemma}

\begin{lemma}[Preservation of configurations.]
\label{oldthm:prezcfg}
If \begin{enumerate}[label=(\roman*)]
\item $\trip{\D}{m}{\rho} \hookrightarrow \trip{\D'}{m'}{\rho'}$,
\item $\Gamma \semi \Pi \vdcfg \trip{\D}{m}{\rho}: R$, and
\item $\D \semi m \wf \rho$,
\end{enumerate}
then there exists some $\Pi'$ such that
\begin{enumerate}[label=(\roman*)]
\item $\Pi \po \Pi'$,
\item $\Gamma \semi \Pi' \vdcfg \trip{\D'}{m'}{\rho'}: R$, and
\item $\D' \semi m' \wf \rho'$.
\end{enumerate}

\begin{Proof}
To show conclusion \emph{(ii)}, we need the following three sub-conclusions:
\begin{enumerate}[label=(\emph{ii}-\alph*)]
\item $\Gamma \semi \Delta \vdd \D'$,
\item $\Gamma \semi \Pi' \vdm m'$, and
\item $\Gamma \semi \Pi' \vdrt \rho': R$.
\end{enumerate}

(We refer to premise \emph{(ii)} in similar fashion, 
e.g. \emph{(ii)}-a says $\Gamma \semi \Delta \vdd \D$.)

Unless otherwise mentioned, we let $\Pi'=\Pi$, 
and conclusion \emph{(i)} holds trivially.

We proceed by induction on the small-step derivation \emph{(iii)}.

\tb{Case} \ts{ER-Ctx}: \hl{TODO}; need eval ctx typing, but should be standard

\tb{Case} \ts{ER-Seq}: We have
$\trip{\D}{m}{\mf{done} \semi \rho}
\hookrightarrow
\trip{\D}{m}{\rho}$ and, by \emph{(ii)}, \begin{mathpar}
\inferrule*[right=TR-Seq] {
  \Gamma \semi \Pi \vdrt \mf{done}: \tone_\rho
  \and
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt \rho: R}
} {
  \Gamma \semi \Pi \vdrt \mf{done} \semi \rho: R
}
\end{mathpar}
\begin{itemize}
\item Conclusion \emph{(ii)} follows from (1).
\item Conclusion \emph{(iii)} is given by premise \emph{(iii)}
as follows: \begin{mathpar}
\inferrule*[right=WF-Seq] {
  \inferrule*[right=WF-Done] { } {\D \semi m \wf \mf{done}}
  \and
  \inferrule*[right=\textnormal{\emph{(iii)}}] { } {\D \semi m \wf \rho}
} {
  \D \semi m \wf \mf{done} \semi \rho
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-Prog}: We have \begin{mathpar}
\inferrule*[right=ER-Prog] {
  \inferrule*[right=(1)] { } {\D \semi P \mapsto P' \semi \D'}
} {
  \trip{\D}{m}{\mf{prog}\ P}
  \hookrightarrow
  \trip{\D'}{m}{\mf{prog}\ P'}
}
\and
\inferrule*[right=TR-Prog] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdprog P}
  \and
  \inferrule*[right=(3)] { } {\Delta[\C_0] = \tone}
} {
  \Gamma \semi \Pi \vdrt \mf{prog}\ P: \tone_\rho
}
\and
\inferrule*[right=WF-Prog] {
  \inferrule*[right=(4)] { } {
    \mc{L}(P) \cup \dom(\D) \cup \{\C_0\}= \dom(\Delta)
  }
} {
  \D \semi m \wf \mf{prog}\ P
}
\end{mathpar}
\begin{itemize}
\item \thref{oldthm:prezprog} (preservation of programs) on (1) and (2)
gives us that
(5) $\Gamma \semi \Pi \vdprog P'$,
(6) $\Gamma \semi \Delta \vdd \D'$, and
(7) $\mc{L}(P') \cup \dom(\D') = \mc{L}(P) \cup \dom(\D)$.
\item To show conclusion \emph{(ii)}, it suffices to show that
$\Gamma \semi \Pi \vdrt \mf{prog}\ P': \tone_\rho$ and $\Gamma \semi \Delta \vdd \D'$.
The latter is precisely given by (6), and
the former is given by the following derivation:
\begin{mathpar}
\inferrule*[right=TR-Prog] {
  \inferrule*[right=(5)] { } {\Gamma \semi \Pi \vdprog P'}
  \and
  \inferrule*[right=(3)] { } {\Delta[\C_0] = \tone}
} {
  \Gamma \semi \Pi \vdrt \mf{prog}\ P': \tone_\rho
}
\end{mathpar}
\item By (4) and (7), we get (8) that 
$\mc{L}(P') \cup \dom(\mc{D'}) \cup \{\C_0\}= \dom(\Delta)$,
and then we can derive conclusion \emph{(iii)} as follows:
\begin{mathpar}
\inferrule*[right=WF-Prog] {
  \inferrule*[right=(8)] { } {
    \mc{L}(P') \cup \dom(\D') \cup \{\C_0\}= \dom(\Delta)
  }
} {
  \mc{D'} \semi m \wf \mf{prog}\ P'
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-PDone}: We have
$\trip{\D}{m}{\mf{prog\ \eret{v}}} 
\hookrightarrow
\trip{\D'}{m'}{\mf{new}^\mf{Succ}_{p_0}\ \mf{init}\ v}$ and
\begin{mathpar}
\inferrule*[right=TR-Prog] {
  \inferrule*[right=TP-Lift] {
    \inferrule*[right=TE-Ret] {
      \inferrule*[right=(1)] { } {
        \Gamma \semi \cdot \vdval v: \mf{View}
      }
    } {
      \Gamma \semi \cdot \vdval \eret{v}: \mf{View} \mid \epsilon
    }
  } {
    \Gamma \vdprog \eret{v}
  }
  \and
  \inferrule*[right=(2)] { } {\Delta[\C_0] = \tone}
} {
  \Gamma \semi \Pi \vdrt \mf{prog}\ \eret{v}: \tone_\rho
}
\and
\inferrule*[right=WF-Prog] {
  \inferrule*[right=(3)] { } {
    \mc{L}(\eret{v}) \cup \dom(\D) \cup \{\C_0\}= \dom(\Delta)
  }
} {
  \D \semi m \wf \mf{prog}\ \eret{v}
}
\end{mathpar}
with abbreviations
\begin{align*}
m' &= m[p_0 \mapsto \{\mf{spec}: \pair{\C_0}{()}, \mf{st}: \cdot,
  \mf{tree}: \cdot, \mf{rerender}: \mf{ff}\}]\\
\D' &= \D[\C_0 \mapsto x_0.\eret{v}]
\end{align*}

\begin{itemize}
\item Choose $\Pi' = \Pi[p_0 \mapsto \C_0]$.
By global freshness of $p_0$, we have $p_0 \notin \dom(\Pi)$, 
so $\Pi \po \Pi'$,
i.e. conclusion \emph{(i)} holds.
\item To show conclusion (\emph{ii}-a) (that $\Gamma \semi \Delta \vdd \D'$),
by premise (\emph{ii}-a) and global freshness of $\C_0$, it suffices to show that 
$\Gamma[x_0 \mapsto \Delta[\C_0]] \semi \cdot \semi \C_0 \vdtop \eret{v}$.
(Say $\Gamma' = \Gamma[x_0 \mapsto \Delta[\C_0]]$.)
\item By global freshness of $x_0$, we have $x_0 \notin \dom(\Gamma)$,
and so $\Gamma \po \Gamma'$. By \thref{oldthm:wktpv} (weakening) on (1),
we get (4) that $\Gamma' \semi \cdot \vdval v: \mf{View}$,
and we derive the conclusion as follows:
\begin{mathpar}
\inferrule*[right=TH-Lift] {
  \inferrule*[right=TE-Ret] {
    \inferrule*[right=(4)] { } {
      \Gamma' \semi \cdot \vdval v: \mf{View}
    }
  } {
    \Gamma' \semi \cdot \vdexp \eret{v} : \mf{View} \mid \epsilon
  }
} {
  \Gamma' \semi \cdot \semi \C_0 \vdtop \eret{v}
}
\end{mathpar}
\item To show conclusion (\emph{ii}-b) (that $\Gamma \semi \Pi' \vdm m'$),
we first apply \thref{oldthm:wkpim} (weakening) to premise (\emph{ii}-b) to get that
$\Gamma \semi \Pi' \vdm m$. Now, by definition 
(and since both the $\mf{st}$ and $\mf{tree}$ fields of $m'[p_0]$ are empty),
it suffices to show that $\Pi'[p_0]=\C_0$ and 
$\Gamma \semi \Pi' \vdval (): \Delta[\C_0]$.
\item The former is true by definition of $\Pi'$, and the latter holds by (2)
and rule \ts{TVal-Unit}: $\Gamma \semi \Pi' \vdval (): \tone$. Thus,
conclusion (\emph{ii}-b) holds.
\item By \thref{oldthm:wkpiv} (weakening) on (1),
we get (5) that $\Gamma \semi \Pi' \vdval v: \mf{View}$. 
We derive conclusion (\emph{ii}-c) as follows:
\begin{mathpar}
\inferrule*[right=TR-New] {
  \inferrule*[right=TR-Init] {
    \inferrule*[right=(5)] { } {\Gamma \semi \Pi' \vdval v: \mf{View}}
  } {
    \Gamma \semi \Pi' \vdrt \mf{init}\ v: \mf{Tree}
  }
} {
  \Gamma \semi \Pi' \vdrt \mf{new}^\mf{Succ}_{p_0}\ \mf{init}\ v: \tone_\rho
}
\end{mathpar}
\item Finally, to show conclusion \emph{(iii)}, we first observe that
$\mc{L}(\eret{v})=\emptyset$, so from (3) we get (6) that
$\dom(\D) \cup \{\C_0\} = \dom(\Delta)$.
By definition of $\D'$, $\dom(\D')=\dom(\D) \cup \{\C_0\}$,
so therefore $\dom(\D') = \dom(\Delta)$, satisfying the implicit premise.
\item We construct the following derivation, where (7) is given by
definition of $\mc{L}$ and construction of $m'$:
\begin{mathpar}
\inferrule*[right=WF-New] {
  \inferrule*[right=(7)] { } {\mc{L}(\eret{v}) = \dom(m'[p].\mf{st})}
  \quad
  \inferrule*[right=WF-Init] { } {\D' \semi m' \wf \mf{init}\ v}
} {
  \D' \semi m' \wf \mf{new}^\mf{Succ}_{p_0}\ \mf{init}\ v
}
\end{mathpar}
\item We have thus shown all desired conclusions, and the case holds.
\end{itemize}

\tb{Case} \ts{ER-ISpec}: We have
$\trip{\D}{m}{\mf{init}\ \pair{\C}{v}}
\hookrightarrow
\trip{\D}{m'}{\mf{eval}^\mf{Init}_p\ \hat{e}[v/x]}$ and
\begin{mathpar}
\inferrule*[right=TR-Init] {
  \inferrule*[right=TV-Spec] {
    \inferrule*[right=TV-Comp] {
      \inferrule*[right=(1)] { } {\Delta[\C] = \ptp}
    } {
      \Gamma \semi \Pi \vdval \C: \mf{Component}\ \ptp
    }
    \ 
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval v: \ptp}
  } {
    \Gamma \semi \Pi \vdval \pair{\C}{v}: \mf{View}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{init}\ \pair{\C}{v}: \mf{Tree}
}
\end{mathpar}
with $p$ fresh and abbreviations
\begin{align*}
m' &= m[p \mapsto \{
    \mf{spec}=\pair{\C}{v}, 
    \mf{st}: \cdot, 
    \mf{tree}: \cdot, 
    \mf{rerender}: \mf{ff}
  \}]\\
\D[\C]&=x.\hat{e}
\end{align*}
\begin{itemize}
\item Choose $\Pi' = \Pi[p \mapsto \C]$.
Since $p$ is fresh, $\Pi \po \Pi'$, i.e. conclusion \emph{(i)} holds.
Furthermore, conclusion (\emph{ii}-a) is trivial.
\item We weaken premise (\emph{ii}-b) by \thref{oldthm:wkpim}
to get that $\Gamma \semi \Pi' \vdm m$. Thus, to show $\Gamma \semi \Pi' \vdm m'$,
by freshness of $p$, to show conclusion (\emph{ii}-b),
it suffices to show that $\Pi'[p]=\C$ (trivial)
and that $\Gamma \semi \Pi' \vdval v: \Delta[\C]$.
\item By (1) and (2), $\Gamma \semi \Pi \vdval v: \Delta[\C]$.
To this, we apply \thref{oldthm:wkpiv} (weakening)
to show conclusion (\emph{ii}-b) holds.
\item To show conclusion (\emph{ii}-c), we appeal to premise (\emph{ii}-a),
which gives us that 
$\Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}$.
We first weaken this (by \thref{oldthm:wkpih}) to get 
$\Gamma[x \mapsto \Delta[\C]] \semi \Pi \semi \C \vdtop \hat{e}$,
where we can then apply \thref{oldthm:subtle} (substitution)
with value from (2) and using the equality from (1) to get
$\Gamma \semi \Pi \semi \C \vdtop \hat{e}[v/x]$.
\item We apply \thref{oldthm:wkpih} once more to say (4) that
$\Gamma \semi \Pi' \semi \C \vdtop \hat{e}[v/x]$.
We note that $\C=\Pi'[p]$, and we conclude with the derivation:
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \inferrule*[right=(4)] { } {
    \Gamma \semi \Pi' \semi \Pi'[p] \vdtop \hat{e}[v/x]
  }
} {
  \Gamma \semi \Pi' \vdrt \mf{eval}^\mf{Init}_p\ \hat{e}[v/x]: \mf{Tree}
}
\end{mathpar}
\item Only conclusion \emph{(iii)} remains to be shown. Via the following
derivation, it suffices to show (5):
\begin{mathpar}
\inferrule*[right=WF-EvalI] {
  \inferrule*[right=(5)] { } {
    \mc{L}(\hat{e}[v/x]) \cup \dom(m'[p].\mf{st}) = \mc{L}(\hat{e})
  }
} {
  \D \semi m' \wf \mf{eval}^\mf{Init}_p\ \hat{e}[v/x]
}
\end{mathpar}
\item By construction, $\dom(m'[p].\mf{st})=\emptyset$, so
it suffices to show $\mc{L}(\hat{e}[v/x]) = \mc{L}(\hat{e})$.
This is given directly by \thref{oldthm:sublab} (substitution for label-sets),
and we are done.
\end{itemize}

\tb{Case} \ts{ER-IText}: We have 
$\trip{\D}{m}{\mf{init\ text}\ v}
\hookrightarrow
\trip{\D}{m}{\mf{lit}\ v}$ and: \begin{mathpar}
\inferrule*[right=TR-Init] {
  \inferrule*[right=TV-Text] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \tstr}
  } {
    \Gamma \semi \Pi \vdval \mf{text}\ v: \mf{View}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{init\ text}\ v: \mf{Tree}
}
\end{mathpar}
\begin{itemize}
\item Conclusion (\emph{ii}-c) is given by the following derivation:
\begin{mathpar}
\inferrule*[right=TR-Lit] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \tstr}
} {
  \Gamma \semi \Pi \vdrt \mf{lit}\ v: \mf{Tree}
}
\end{mathpar}
\item Conclusions (\emph{ii}-a), (\emph{ii}-b), and \emph{(iii)}
are all trivial, and we are done.
\end{itemize}

\tb{Case} \ts{ER-ITag}: We have 
$\trip{\D}{m}{\mf{init\ tag}(v_1, v_2)}
\hookrightarrow
\trip{\D}{m}{\mf{node}(\mf{id}, v_1, \mf{inits}\ v_2)}$ and:
\begin{mathpar}
\inferrule*[right=TR-Init] {
  \inferrule*[right=TV-Tag] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v_1: [\mf{Attr}]}
    \and
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval v_2: [\mf{View}]}
  } {
    \Gamma \semi \Pi \vdval \mf{tag}(v_1, v_2): \mf{View}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{init\ tag}(v_1, v_2): \mf{Tree}
}
\end{mathpar}
\begin{itemize}
\item Conclusion (\emph{ii}-c) is given by the following derivation:
\begin{mathpar}
\inferrule*[right=TR-Node] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v_1: [\mf{Attr}]}
  \
  \inferrule*[right=TR-Inits] {
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval v_2: [\mf{View}]}
  } {
    \Gamma \semi \Pi \vdrt \mf{inits}\ v_2: [\mf{Tree}]_\rho
  }
} {
  \Gamma \semi \Pi \vdrt \mf{node}(\mf{id}, v_1, \mf{inits}\ v_2): \mf{Tree}
}
\end{mathpar}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial. \emph{(iii)}
is shown as follows:
\begin{mathpar}
\inferrule*[right=WF-Node] {
  \inferrule*[right=WF-Inits] { } {\D \semi m \wf \mf{inits}\ v_2}
} {
  \D \semi m \wf \mf{node}(\mf{id}, v, \mf{inits}\ v_2)
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-INil}: We have
$\trip{\D}{m}{\mf{inits}\ []}
\hookrightarrow
\trip{\D}{m}{[]_\rho}$ and:
\begin{mathpar}
\inferrule*[right=TR-Inits] {
  \Gamma \semi \Pi \vdval []: [\mf{View}]
} {
  \Gamma \semi \Pi \vdrt \mf{inits}\ []: [\mf{Tree}]_\rho
}
\end{mathpar}
\begin{itemize}
\item The typing conclusion (\emph{ii}-c) is immediate: 
\begin{mathpar}
\inferrule*[right=TR-Nil] { } {
  \Gamma \semi \Pi \vdrt []_\rho: [\mf{Tree}]_\rho
}
\end{mathpar}
\item Conclusions (\emph{ii}-a), (\emph{ii}-b), and \emph{(iii)} are all trivial.
\end{itemize}

\tb{Case} \ts{ER-ICons}: We have
$\trip{\D}{m}{\mf{inits}\ (v :: vs)}
\hookrightarrow
\trip{\D}{m}{(\mf{init}\ v) ::_\rho (\mf{inits}\ vs)}$ and:
\begin{mathpar}
\inferrule*[right=TR-Inits] {
  \inferrule*[right=TV-Cons] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \mf{View}}
    \and
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval vs: [\mf{View}]}
  } {
    \Gamma \semi \Pi \vdval v :: vs: [\mf{View}]
  }
} {
  \Gamma \semi \Pi \vdrt \mf{inits}\ (v :: vs): [\mf{Tree}]_\rho
}
\end{mathpar}
\begin{itemize}
\item We derive conclusion (\emph{ii}-c) from (1) and (2) as follows:
\end{itemize}
\begin{mathpar}
\inferrule*[right=TR-Cons] {
  \inferrule*[right=TR-Init] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \mf{View}}
  } {
    \Gamma \semi \Pi \vdrt \mf{init}\: v : \mf{Tree}
  }
  \,
  \inferrule*[right=TR-Inits] {
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval vs: [\mf{View}]}
  } {
    \Gamma \semi \Pi \vdrt \mf{inits}\: vs : [\mf{Tree}]_\rho
  }
} {
  \Gamma \semi \Pi \vdrt (\mf{init}\ v) ::_\rho (\mf{inits}\ vs) : [\mf{Tree}]_\rho
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a), (\emph{ii}-b), and \emph{(iii)} are all trivial.
\end{itemize}

\tb{Case} \ts{ER-Eval ($\phi=\mf{Init}$)}: We have
\begin{mathpar}
\inferrule*[right=ER-Eval] {
  \inferrule*[right=(1)] { } {
    m \semi \hat{e} \topstep{\mf{Init}}{p} \hat{e}' \semi m'
  }
} {
  \trip{\D}{m}{\mf{eval}^\mf{Init}_p\ \hat{e}}
  \hookrightarrow
  \trip{\D}{m'}{\mf{eval}^\mf{Init}_p\ \hat{e}'}
}
\and
\inferrule*[right=TR-Eval] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}}
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Init}_p\ \hat{e}: \mf{Tree}
}
\and
\inferrule*[right=WF-EvalI] {
  \inferrule*[right=(3)] { } {\mc{L}(\hat{e}) \cup \dom(m[p].\mf{st}) = \mc{L}(\hat{e}_0)}
} {
  \D \semi m \wf \mf{eval}^\mf{Init}_p\ \hat{e}
}
\end{mathpar}
\begin{itemize}
\item It follows from the well-definedness 
of $\hat{e}_0$ in (3) that $p \in \dom(m)$. 
Therefore, we can apply \thref{oldthm:preztlei} 
(top-level preservation at $\phi=\mf{Init}$) 
using (2), premise (\emph{ii}-b),and (1) to get the following:
\begin{enumerate}[label=(\arabic*),start=(4)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'$,
\item $\Gamma \semi \Pi \vdm m'$, and
\item $\mc{L}(\hat{e}) \cup \dom(m[p].\mf{st}) 
= \mc{L}(\hat{e}') \cup \dom(m'[p].\mf{st})$.
\end{enumerate}
\item Conclusion (\emph{ii}-a) is trivial, and (\emph{ii}-b) is given by (5).
(\emph{ii}-c) is shown as follows:
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \inferrule*[right=(4)] { } {\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'}
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Init}_p\ \hat{e}': \mf{Tree}
}
\end{mathpar}
\item By \thref{oldthm:prezspec} (spec preservation) and (3),
we have that $m'[p].\mf{spec} = m[p].\mf{spec}$,
and therefore $\hat{e}_0$ remains the same between $m$ and $m'$.
By (4) and (6), we get (8) that
$\mc{L}(\hat{e}') \cup \dom(m'[p].\mf{st}) = \mc{L}(\hat{e}_0)$.
We can now derive conclusion \emph{(iii)} as follows:
\end{itemize}
\begin{mathpar}
\inferrule*[right=WF-EvalI] {
  \inferrule*[right=(8)] { } {
    \mc{L}(\hat{e}') \cup \dom(m'[p].\mf{st}) = \mc{L}(\hat{e}_0)
  }
} {
  \D \semi m' \wf \mf{eval}^\mf{Init}_p\ \hat{e}'
}
\end{mathpar}

\tb{Case} \ts{ER-Eval ($\phi=\mf{Succ}$)}: We have
\begin{mathpar}
\inferrule*[right=ER-Eval] {
  \inferrule*[right=(1)] { } {
    m \semi \hat{e} \topstep{\mf{Succ}}{p} \hat{e}' \semi m'
  }
} {
  \trip{\D}{m}{\mf{eval}^\mf{Succ}_p\ \hat{e}}
  \hookrightarrow
  \trip{\D}{m'}{\mf{eval}^\mf{Succ}_p\ \hat{e}'}
}
\and
\inferrule*[right=TR-Eval] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}}
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Succ}_p\ \hat{e}: \tone_\rho
}
\and
\inferrule*[right=WF-EvalS] {
  \inferrule*[right=(3)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \inferrule*[right=(4)] { } {\mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})}
} {
  \D \semi m \wf \mf{eval}^\mf{Succ}_p\ \hat{e}
}
\end{mathpar}
\begin{itemize}
\item By (4), we again have $p \in \dom(m)$, so we can apply \thref{oldthm:preztles}
(top-level preservation at $\phi=\mf{Succ}$)
using (2), premise (\emph{ii}-b), and (1) to get the following:
\begin{enumerate}[label=(\arabic*),start=(5)]
\item $\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'$,
\item $m = m'$, and
\item $\mc{L}(\hat{e}') \subseteq \mc{L}(\hat{e})$.
\end{enumerate}
\item Conclusion (\emph{ii}-a) is trivial, 
and (\emph{ii}-b) is immediate from (6) and premise (\emph{ii}-b).
Conclusion (\emph{ii}-c) is shown as follows:
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \inferrule*[right=(5)] { } {\Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}'}
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Succ}_p\ \hat{e}': \tone_\rho
}
\end{mathpar}
\item Finally, (4) and (7) give us (8) 
that $\mc{L}(\hat{e}') \subseteq \dom(m[p].\mf{st})$,
and conclusion \emph{(iii)} follows:
\begin{mathpar}
\inferrule*[right=WF-EvalS] {
    \inferrule*[right=(3)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \inferrule*[right=(8)] { } {\mc{L}(\hat{e}') \subseteq \dom(m[p].\mf{st})}
} {
  \D \semi m \wf \mf{eval}^\mf{Succ}_p\ \hat{e}'
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-New ($\phi=\mf{Init}$)}: We have
$$\trip{\D}{m}{\mf{eval}^\mf{Init}_p\ \eret{v}}
\hookrightarrow
\trip{\D}{m}{\mf{new}^\mf{Init}_p\ \mf{init}\ v}$$
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \inferrule*[right=TH-Lift] {
    \inferrule*[right=TE-Ret] {
      \inferrule*[right=(1)] { } {
        \Gamma \semi \Pi \vdval v: \mf{View}
      }
    } {
      \Gamma \semi \Pi \vdexp \eret{v} : \mf{View} \mid \epsilon
    }
  } {
    \Gamma \semi \Pi \semi \Pi[p] \vdtop \eret{v}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Init}_p\ \eret{v}: \mf{Tree}
}
\and
\inferrule*[right=WF-EvalI] {
  \inferrule*[right=(2)] { } {
    \mc{L}(\eret{v}) \cup \dom(m[p].\mf{st}) = \mc{L}(\hat{e}_0)
  }
} {
  \D \semi m \wf \mf{eval}^\mf{Init}_p\ \eret{v}
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
The following derivation gives conclusion (\emph{ii}-c):
\begin{mathpar}
\inferrule*[right=TR-New] {
  \inferrule*[right=TR-Init] {
    \inferrule*[right=(1)] { } {
      \Gamma \semi \Pi \vdval v: \mf{View}
    }
  } {
    \Gamma \semi \Pi \vdrt \mf{init}\ v: \mf{Tree}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{new}^\mf{Init}_p\ \mf{init}\ v: \mf{Tree}
}
\end{mathpar}
\item To show conclusion \emph{(iii)}, we note that $\mc{L}(\eret{v}) = \emptyset$
by definition, so (2) reduces to (3) $\dom(m[p].\mf{st}) = \mc{L}(\hat{e}_0)$.
Well-formedness then follows by derivation:
\begin{mathpar}
\inferrule*[right=WF-New] {
  \inferrule*[right=(3)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \inferrule*[right=WF-Init] { } {\D \semi m \wf \mf{init}\ v}
} {
  \D \semi m \wf \mf{new}^\mf{Init}_p\ \mf{init}\ v
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-New ($\phi=\mf{Succ}$)}: We have
$$\trip{\D}{m}{\mf{eval}^\mf{Succ}_p\ \eret{v}}
\hookrightarrow
\trip{\D}{m}{\mf{new}^\mf{Succ}_p\ \mf{init}\ v}$$
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \inferrule*[right=TH-Lift] {
    \inferrule*[right=TE-Ret] {
      \inferrule*[right=(1)] { } {
        \Gamma \semi \Pi \vdval v: \mf{View}
      }
    } {
      \Gamma \semi \Pi \vdexp \eret{v} : \mf{View} \mid \epsilon
    }
  } {
    \Gamma \semi \Pi \semi \Pi[p] \vdtop \eret{v}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Succ}_p\ \eret{v}: \tone_\rho
}
\and
\inferrule*[right=WF-EvalS] {
  \inferrule*[right=(2)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \mc{L}(\eret{v}) \subseteq \dom(m[p].\mf{st})
} {
  \D \semi m \wf \mf{eval}^\mf{Succ}_p\ \eret{v}
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
The following derivation gives conclusion (\emph{ii}-c):
\begin{mathpar}
\inferrule*[right=TR-New] {
  \inferrule*[right=TR-Init] {
    \inferrule*[right=(1)] { } {
      \Gamma \semi \Pi \vdval v: \mf{View}
    }
  } {
    \Gamma \semi \Pi \vdrt \mf{init}\ v: \mf{Tree}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{new}^\mf{Succ}_p\ \mf{init}\ v: \tone_\rho
}
\end{mathpar}
\item Conclusion \emph{(iii)} also follows quickly, and we are done:
\begin{mathpar}
\inferrule*[right=WF-New] {
  \inferrule*[right=(2)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \inferrule*[right=WF-Init] { } {\D \semi m \wf \mf{init}\ v}
} {
  \D \semi m \wf \mf{new}^\mf{Succ}_p\ \mf{init}\ v
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-NewI}: We have
$\trip{\D}{m}{\mf{new}^\mf{Init}_p\ u}
\hookrightarrow
\trip{\D}{m'}{p}$ and:
\begin{mathpar}
\inferrule*[right=TR-New] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt u: \mf{Tree}}
} {
  \Gamma \semi \Pi \vdrt \mf{new}^\mf{Init}_p\ u: \mf{Tree}
}
\and
\inferrule*[right=WF-New] {
  \inferrule*[right=(2)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \inferrule*[right=(3)] { } {\D \semi m \wf u}
} {
  \D \semi m \wf \mf{new}^\mf{Init}_p\ u
}
\end{mathpar}
where $m' = m \mid m[p].\mf{tree} = u$.
\begin{itemize}
\item Conclusion (\emph{ii}-a) is trivial.
\item To show conclusion (\emph{ii}-b) given premise (\emph{ii}-b),
it suffices to show that $\Gamma \semi \Pi \vdrt u: \mf{Tree}$.
This is given directly by (1).
\item To show conclusion (\emph{ii}-c), we note that,
since $p \in \dom(m)$ (e.g. by (2))
and $\Gamma \semi \Pi \vdm m$ (by premise (\emph{ii}-b)),
it must be that (4) $p \in \Pi$. Also
from the memory typing judgment, $\Pi[p] =\C$
for some $\C \in \dom(\Delta)$, i.e. (5) $\Pi[p] \in \dom(\Delta)$.
Conclusion (\emph{ii}-c) now follows from the derivation:
\begin{mathpar}
\inferrule*[right=TR-Path] {
  \inferrule*[right=(4)] { } {p \in \dom(\Pi)}
  \and
  \inferrule*[right=(5)] { } {\Pi[p] \in \dom(\Delta)}
} {
  \Gamma \semi \Pi \vdrt p: \mf{Tree}
}
\end{mathpar}
\item Finally, we need to show conclusion \emph{(iii)}.
Clearly $m'[p].\mf{spec} = m[p].\mf{spec}$ and $m'[p].\mf{st} = m[p].\mf{st}$,
which lets us conclude from (2) that (6) $\mc{L}(\hat{e}_0) = \dom(m'[p].\mf{st})$.
We construct the following derivation:
\begin{mathpar}
\inferrule*[right=WF-Path] {
  \inferrule*[right=(6)] { } {\mc{L}(\hat{e}_0) = \dom(m'[p].\mf{st})}
  \and
  \inferrule*[right=(7)] { } {\D \semi m' \wf m'[p].\mf{tree}}
} {
  \D \semi m' \wf p
}
\end{mathpar}
\item Therefore, it suffices to show (7). \hl{This is... not given by (3)}
\end{itemize}

\tb{Case} \ts{ER-NewS}: We have
$\trip{\D}{m}{\mf{new}^\mf{Succ}_p\ u}
\hookrightarrow
\trip{\D}{m'}{\mf{done}}$ and:
\begin{mathpar}
\inferrule*[right=TR-New] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt u: \mf{Tree}}
} {
  \Gamma \semi \Pi \vdrt \mf{new}^\mf{Succ}_p\ u: \tone_\rho
}
\and
\inferrule*[right=WF-New] {
  \mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})
  \and
  \D \semi m \wf u
} {
  \D \semi m \wf \mf{new}^\mf{Succ}_p\ u
}
\end{mathpar}
where $m' = m \mid m[p].\mf{tree} = u$.
\begin{itemize}
\item Conclusions (\emph{ii}-a), (\emph{ii}-c), and \emph{(iii)} are all trivial.
\item To show conclusion (\emph{ii}-b), given premise (\emph{ii}-b),
it suffices to show that $\Gamma \semi \Pi \vdrt u: \mf{Tree}$.
This is given by (1) and we are done.\\
\hl{WE LOSE INFO HERE: work wf-judgment into memory?}
\end{itemize}

\tb{Case} \ts{ER-ScAttr}: We have
$\trip{\D}{m}{\mf{scan\ attr}(v_1, v_2)}
\hookrightarrow
\trip{\D}{m}{\mf{done}}$
and:
\begin{mathpar}
\inferrule*[right=TR-Sc] {
  \Gamma \semi \Pi \vdval v: \mf{Attr}
} {
  \Gamma \semi \Pi \vdrt \mf{scan\ attr}(v_1, v_2): \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item All conclusions are trivial.
\end{itemize}

\tb{Case} \ts{ER-ScClick}: We have
$\trip{\D}{m}{\mf{scan\ onClick}(v)}
\hookrightarrow
\trip{\D}{m}{\mf{handle}(v\ ())}$
and:
\begin{mathpar}
\inferrule*[right=TR-Sc] {
  \inferrule*[right=TV-OnClick] {
    \inferrule*[right=(1)] { } {
      \Gamma \semi \Pi \vdval v: \tone \xrightarrow{E} \tone
    }
  } {
    \Gamma \semi \Pi \vdval \mf{onClick}\ v: \mf{Attr}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{scan\ onClick}(v): \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a), (\emph{ii}-b), and \emph{(iii)} are all trivial.
\item We derive conclusion (\emph{ii}-c) as follows:
\begin{mathpar}
\inferrule*[right=TR-Hdl] {
  \inferrule*[right=TE-AppL] { 
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \tone \xrightarrow{E} \tone}
    \
    \inferrule*[right=TV-Unit] { } {\Gamma \semi \Pi \vdval (): \tone}
  } {
    \Gamma \semi \Pi \vdexp v\ (): \tone \mid E
  }
} {
  \Gamma \semi \Pi \vdrt \mf{handle}(v\ ()): \tone_\rho
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-ScsNil}: We have
$\trip{\D}{m}{\mf{scans}\ []}
\hookrightarrow
\trip{\D}{m}{\mf{done}}$
and:
\begin{mathpar}
\inferrule*[right=TR-Scs] {
  \Gamma \semi \Pi \vdval []: [\mf{Attr}]
} {
  \Gamma \semi \Pi \vdrt \mf{scans}\ []: \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item All conclusions are trivial.
\end{itemize}

\tb{Case} \ts{ER-ScsCons}: We have
$\trip{\D}{m}{\mf{scans}\ (v :: vs)}
\hookrightarrow
\trip{\D}{m}{\mf{scan}\ v \semi \mf{scans}\ vs}$
and:
\begin{mathpar}
\inferrule*[right=TR-Scs] {
  \inferrule*[right=TV-Cons] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \mf{Attr}}
    \and
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval vs: [\mf{Attr}]}
  } {
    \Gamma \semi \Pi \vdval v :: vs: [\mf{Attr}]
  }
} {
  \Gamma \semi \Pi \vdrt \mf{scans}\ (v :: vs): \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a), (\emph{ii}-b), and \emph{(iii)} are all trivial.
\item We derive conclusion (\emph{ii}-c) as follows:
\begin{mathpar}
\inferrule*[right=TR-Seq] {
  \inferrule*[right=TR-Sc] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdval v: \mf{Attr}}
  } {
    \Gamma \semi \Pi \vdrt \mf{scan}\ v: \tone_\rho
  }
  \,
  \inferrule*[right=TR-Scs] {
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdval vs: [\mf{Attr}]}
  } {
    \Gamma \semi \Pi \vdrt \mf{scans}\ vs: \tone_\rho
  }
} {
  \Gamma \semi \Pi \vdrt \mf{scan}\ v \semi \mf{scans}\ vs: \tone_\rho
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-Hdl}: We have
\begin{mathpar}
\inferrule*[right=ER-Hdl] {
  \inferrule*[right=(1)] { } {m \semi e \mapsto e' \semi m'}
} {
  \trip{\D}{m}{\mf{handle}\ e}
  \hookrightarrow
  \trip{\D}{m'}{\mf{handle}\ e'}
}
\and
\inferrule*[right=TR-Hdl] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdexp e: \tone \mid E}
} {
  \Gamma \semi \Pi \vdrt \mf{handle}\ e: \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{iii}) are trivial.
\item We apply \thref{oldthm:prezexp} (expression preservation)
to (1), (2), and premise (\emph{ii}-b) to get:
\begin{enumerate}[start=3,label=(\arabic*)]
\item $\Gamma \semi \Pi \vdexp e': \tone \mid E$ and
\item $\Gamma \semi \Pi \vdm m'$.
\end{enumerate}
\item (4) gives us conclusion (\emph{ii}-b) directly.
Conclusion (\emph{ii}-c) follows from the derivation:
\begin{mathpar}
\inferrule*[right=TR-Hdl] {
  \inferrule*[right=(3)] { } {\Gamma \semi \Pi \vdexp e': \tone \mid E}
} {
  \Gamma \semi \Pi \vdrt \mf{handle}\ e': \tone_\rho
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-HdlDone}: We have
$\trip{\D}{m}{\mf{handle}\ \eret{()}}
\hookrightarrow
\trip{\D}{m}{\mf{done}}$ and:
\begin{mathpar}
\inferrule*[right=TR-Hdl] {
  \Gamma \semi \Pi \vdexp \eret{()}: \tone \mid E
} {
  \Gamma \semi \Pi \vdrt \mf{handle}\ \eret{()}: \tone_\rho
}
\end{mathpar}
\begin{itemize}
\item All conclusions are trivial.
\end{itemize}

\tb{Case} \ts{ER-ChkPF}: We have \begin{mathpar}
\inferrule*[right=ER-ChkPF] {
  m[p].\mf{rerender} = \mf{ff}
} {
  \trip{\D}{m}{\mf{check}\ p}
  \hookrightarrow
  \trip{\D}{m}{\mf{check}\ m[p].\mf{tree}}
}
\and
\inferrule*[right=TR-Chk] {
  \Gamma \semi \Pi \vdrt p: \mf{Tree}
} {
  \Gamma \semi \Pi \vdrt \mf{check}\ p: \tone_\rho
}
\and
\inferrule*[right=WF-Chk] {
  \inferrule*[right=(WF-Path)] {
    \mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})
    \and
    \inferrule*[right=(1)] { } {\D \semi m \wf m[p].\mf{tree}}
  } {
    \D \semi m \wf p
  }
} {
  \D \semi m \wf \mf{check}\ p
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
\item By premise (\emph{ii}-b), we get (2) that 
$\Gamma \semi \Pi \vdrt m[p].\mf{tree}: \mf{Tree}$.
Therefore, we can show conclusion (\emph{ii}-c):
\begin{mathpar}
\inferrule*[right=TR-Chk] {
  \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdrt m[p].\mf{tree}: \mf{Tree}}
} {
  \Gamma \semi \Pi \vdrt \mf{check}\ m[p].\mf{tree}: \tone_\rho
}
\end{mathpar}
\item Finally, conclusion \emph{(iii)} follows from (1):
\begin{mathpar}
\inferrule*[right=WF-Chk] {
  \inferrule*[right=(1)] { } {\D \semi m \wf m[p].\mf{tree}}
} {
  \D \semi m \wf \mf{check}\ m[p].\mf{tree}
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-ChkPT}: We have \begin{mathpar}
\inferrule*[right=ER-ChkPT] {
  m[p].\mf{rerender} = \mf{tt}
} {
  \trip{\D}{m}{\mf{check}\ p}
  \hookrightarrow
  \trip{\D}{m \mid m[p].\mf{rerender} = \mf{ff}}{\mf{eval}^\mf{Succ}_p\ \hat{e}_0[v_0/x]}
}
\and
\inferrule*[right=TR-Chk] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt m[p].\mf{tree}: \mf{Tree}}
} {
  \Gamma \semi \Pi \vdrt \mf{check}\ m[p].\mf{tree}: \tone_\rho
}
\and
\inferrule*[right=WF-Chk] {
  \inferrule*[right=WF-Path] {
    \inferrule*[right=(2)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
    \and
    \D \semi m \wf m[p].\mf{tree}
  } {
    \D \semi m \wf p
  }
} {
  \D \semi m \wf \mf{check}\ p
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
\item Recall that we defined $\hat{e}_0$ by $m[p].\mf{spec}=\pair{\C}{v_0}$
and $\D[\C] = x.\hat{e}_0$.
By premise (\emph{ii}-a), we have
(3) $\Gamma[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}_0$.
By premise (\emph{ii}-b), we also have
(4) $\Gamma \semi \Pi \vdval v_0: \Delta[\C]$.
\item By \thref{oldthm:wkpih} and \thref{oldthm:subtle} on (3) and (4),
we get $\Gamma \semi \cdot \semi \C \vdtop \hat{e}_0[v_0/x]$.
Noting that $\Pi[p]=\C$ by premise (\emph{ii}-b), we can now derive
conclusion (\emph{ii}-c):
\begin{mathpar}
\inferrule*[right=TR-Eval] {
  \Gamma \semi \Pi \semi \Pi[p] \vdtop \hat{e}_0[v_0/x]
} {
  \Gamma \semi \Pi \vdrt \mf{eval}^\mf{Succ}_p\ \hat{e}_0[v_0/x]: \tone_\rho
}
\end{mathpar}
\item Conclusion \emph{(iii)} remains to be shown. By \thref{oldthm:sublab}
(label-set substitution), we have $\mc{L}(\hat{e}_0) = \mc{L}(\hat{e}_0[v_0/x])$,
and by (2), this means (5) that $\mc{L}(\hat{e}_0[v_0/x]) \subseteq \dom(m[p].\mf{st})$.
We conclude via derivation:
\begin{mathpar}
\and
\inferrule*[right=WF-EvalS] {
  \inferrule*[right=(2)] { } {\mc{L}(\hat{e}_0) = \dom(m[p].\mf{st})}
  \and
  \mc{L}(\hat{e}_0[v_0/x]) \subseteq \dom(m[p].\mf{st})
} {
  \D \semi m \wf \mf{eval}^\mf{Succ}_p\ \hat{e}_0[v_0/x]
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-ChkLit}: Trivial.

\tb{Case} \ts{ER-ChkNode}: We have
$\trip{\D}{m}{\mf{check\ node}(\mf{id}, v, \rho)}
\hookrightarrow
\trip{\D}{m}{\mf{checks}\ \rho}$ and:
\begin{mathpar}
\inferrule*[right=TR-Chk] {
  \inferrule*[right=TV-Node] {
    \Gamma \semi \Pi \vdval v: [\mf{Attr}]
    \and
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho}
  } {
    \Gamma \semi \Pi \vdrt \mf{node}(\mf{id}, v, \rho): \mf{Tree}
  }
} {
  \Gamma \semi \Pi \vdrt \mf{check\ node}(\mf{id}, v, \rho): \tone_\rho
}
\and
\inferrule*[right=WF-Chk] {
  \inferrule*[right=WF-Node] {
    \inferrule*[right=(2)] { } {\D \semi m \wf \rho}
  } {
    \D \semi m \wf \mf{node}(\mf{id}, v, \rho)
  }
} {
  \D \semi m \wf \mf{check\ node}(\mf{id}, v, \rho)
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
\item Conclusion (\emph{ii}-c) is derived as follows:
\begin{mathpar}
\inferrule*[right=TR-Chks] {
  \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt \rho: [\mf{Tree}]_\rho}
} {
  \Gamma \semi \Pi \vdrt \mf{checks}\ \rho: [\tone_\rho]_\rho
}
\end{mathpar}
\item Conclusion \emph{(iii)} is derived as follows:
\begin{mathpar}
\inferrule*[right=WF-Chks] {
  \inferrule*[right=(2)] { } {\D \semi m \wf \rho}
} {
  \D \semi m \wf \mf{checks}\ \rho
}
\end{mathpar}
\end{itemize}

\tb{Case} \ts{ER-ChksNil}: Trivial.

\tb{Case} \ts{ER-ChksCons}: We have
$$\trip{\D}{m}{\mf{checks}\ \rho_1 ::_\rho \rho_2}
\hookrightarrow
\trip{\D}{m}{\mf{check}\ \rho_1 \semi \mf{checks}\ \rho_2}$$
\begin{mathpar}
\inferrule*[right=TR-Chks] {
  \inferrule*[right=TR-Cons] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt \rho_1: \mf{Tree}}
    \and
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdrt \rho_2: [\mf{Tree}]_\rho}
  } {
    \Gamma \semi \Pi \vdrt \rho_1 ::_\rho \rho_2: [\mf{Tree}]_\rho
  }
} {
  \Gamma \semi \Pi \vdrt \mf{checks}\ \rho_1 ::_\rho \rho_2 : \tone_\rho
}
\and
\inferrule*[right=WF-Chks] {
  \inferrule*[right=WF-Cons] {
    \inferrule*[right=(3)] { } {\D \semi m \wf \rho_1}
    \and
    \inferrule*[right=(4)] { } {\D \semi m \wf \rho_2}
  } {
    \D \semi m \wf \rho_1 ::_\rho \rho_2
  }
} {
  \D \semi m \wf \mf{checks}\ \rho_1 ::_\rho \rho_2
}
\end{mathpar}
\begin{itemize}
\item Conclusions (\emph{ii}-a) and (\emph{ii}-b) are trivial.
\item Conclusion (\emph{ii}-c) is as follows:
\begin{mathpar}
\inferrule* {
  \inferrule*[right=TR-Chk] {
    \inferrule*[right=(1)] { } {\Gamma \semi \Pi \vdrt \rho_1: \mf{Tree}}
  } {
    \Gamma \semi \Pi \vdrt \mf{check}\ \rho_1: \tone_\rho
  }
  \,
  \inferrule*[right=TR-Chks] {
    \inferrule*[right=(2)] { } {\Gamma \semi \Pi \vdrt \rho_2: [\mf{Tree}]_\rho}
  } {
    \Gamma \semi \Pi \vdrt \mf{checks}\ \rho_2: \tone_\rho
  }
} {
  \Gamma \semi \Pi \vdrt \mf{check}\ \rho_1 \semi \mf{checks}\ \rho_2: \tone_\rho
  \and (\ts{TR-Seq})
}
\end{mathpar}
\item Conclusion \emph{(iii)} is as follows:
\begin{mathpar}
\inferrule*[right=WF-Seq] {
  \inferrule*[right=WF-Chk] {
    \inferrule*[right=(3)] { } {\D \semi m \wf \rho_1}
  } {
    \D \semi m \wf \mf{check}\ \rho_1
  }
  \and
  \inferrule*[right=WF-Chk] {
    \inferrule*[right=(4)] { } {\D \semi m \wf \rho_2}
  } {
    \D \semi m \wf \mf{check}\ \rho_1
  }
} {
  \D \semi m \wf \mf{check}\ \rho_1 \semi \mf{checks}\ \rho_2
}
\end{mathpar}
\end{itemize}

\tb{Cases} to do with searching: \hl{unfinished}, but should be easy.

\end{Proof}
\end{lemma}

\pagebreak

\section{Big-Step Runtime (In Complete Disarray)}

\subsection{(Old) Runtime Typing}

\subsubsection{Trees}

\emph{\tb{Typing}}. Judgment: $\Pi \vdtree t$
\begin{mathpar}
\inferrule*[right=TTree-Lit] {
  \cdot \semi \Pi \vdval v: \tstr
} {
  \Pi \vdtree \mf{lit}\ v
}
\and
\inferrule*[right=TTree-Path] {
  p \in \dom(\Pi)
} {
  \Pi \vdtree p
}
\and
\inferrule*[right=TTree-Node] {
  \mf{id\ fresh}
  \and
  \cdot \semi \Pi \vdval v: \tstr
  \\
  (\forall i)\ \cdot \semi \Pi \vdval v_i: \mf{Attr}
  \and
  (\forall j)\ \Pi \vdtree t_j
} {
  \Pi \vdtree \mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})
}
\end{mathpar}

\emph{\tb{Well-Formedness}}.
In addition to our typing judgment on trees,
we have a \emph{well-formedness} judgment on trees
with respect to a concrete memory $m$ and definition set $\D$.
Informally, this says that all paths in a tree have been
"properly initialized".

Judgment: $\D \semi m \wf t$
\begin{mathpar}
\inferrule*[right=WF-Lit] { } {\D \semi m \wf \mf{lit}\ v}
\and
\inferrule*[right=WF-Node] {
  (\forall j)
  \quad
  \D \semi m \wf t_j
} {
  \D \semi m \wf \mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})
}
\and
\inferrule*[right=WF-Path] {
  m[p].\mf{spec} = \pair{\C}{v}
  \and
  \D[\C] = x.\hat{e}
  \\
  \mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})
  \and
  \D \semi m \wf m[p].\mf{child}
} {
  \D \semi m \wf p
}
\end{mathpar}

\subsubsection{Component Context}
Define $\Delta \vDash \D$ to say that,
for all $\C \in \dom(\D)$
(such that $\D[\C] = x.\hat{e}$),
we have $[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}$.
(Implicitly, $\dom(\D) \subseteq \dom(\Delta)$).

\subsubsection{Memory}
We say $\Gamma \semi \Pi \vdm m$ if, for all $p \in \dom(m)$,
\begin{itemize}
\item for $m[p].\mf{spec}=\pair{\C}{v}$,
$\Pi[p] = \C$ and $\Gamma \semi \Pi \vdval v: \Delta[\C]$, and
\item for all $\ell$ in $m[p].\mf{st}$,
$\Gamma \semi \Pi \vdval m[p].\mf{st}[\ell] : \Delta[\C][\ell]$.
\item if $m[p].\mf{child}$ is defined, then $\Pi \vdtree m[p].\mf{child}$.
\end{itemize}

\subsubsection{Modes}

Judgment: $\Pi \vdmu \mu$
\begin{mathpar}
\inferrule*[right=TMode-Idle] { } {\Pi \vdmu \mf{idle}}
\and
\inferrule*[right=TMode-Search] { } {\Pi \vdmu \mf{search}(id)}
\and
\inferrule*[right=TMode-Start] {
  \cdot \semi \Pi \vdprog P
} {
  \Pi \vdmu \mf{start}(P)
}
\and
\inferrule*[right=TMode-HandleNil] { } {\Pi \vdmu \mf{handle}([])}
\and
\inferrule*[right=TMode-HandleCons] {
  \cdot \semi \Pi \vdexp e : \tone \mid E
  \and
  \Pi \vdmu \mf{handle}(es)
} {
  \Pi \vdmu \mf{handle}(e :: es)
}
\end{mathpar}

\subsubsection{Configurations}

Define a configuration $\four{\D}{m}{t}{\mu}$
to be well-typed via $\Pi \vdcfg \four{\D}{m}{t}{\mu}$ if:
\begin{itemize}
\item $\Delta \vDash \D$;
\item $\Pi \vdmu \mu$;
\item if $m$ is defined, then $\cdot \semi \Pi \vdm m$; and
\item if $t$ is defined, then both $\Pi \vdtree t$ and $\D \semi m \wf t$.
\end{itemize}

\subsection{Program Semantics}

Throughout the semantics, we use the notation $m \mid x = v$
to concisely denote nested map/record updates.
The above should be read as ``$m$ with field $x$ set to $v$''.
For instance, $m \mid m[p].\mf{st}[\ell] = v$ updates the
state field of $m$ at path $p$ and location $\ell$ to value $v$.

Again, we \hl{highlight} interesting rules.

\subsubsection{Expression Semantics}

Judgment: $m \semi e \mapsto e' \semi m'$
\begin{mathpar}
\inferrule*[right=E-Let] {
  m \semi e_1 \mapsto e_1' \semi m'
} {
  m \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi m'
}
\end{mathpar}
\begin{align*}
m \semi \elet{x}{\eret{v}}{e} &\mapsto e[v/x] \semi m
&&\ts{E-Ret}\\
m \semi (\lambda x.e) v &\mapsto e[v/x] \semi m
&&\ts{E-AppFun}\\
m \semi \C\ v &\mapsto \eret{\pair{\C}{v}} \semi m
&&\ts{\hl{E-AppC}}\\
m \semi \vset{\ell}{p}\ v &\mapsto \eret{()} \semi m'
&&\ts{\hl{E-AppSet}}\\
(m' = m \mid m[p].\mf{st}[\ell] &= v\ 
    \mf{and}\ m[p].\mf{rerender} = \mf{tt})\\
m \semi \eif{\mf{true}}{e_1}{e_2} &\mapsto e_1 \semi m
&&\ts{E-IfTrue}\\
m \semi \eif{\mf{false}}{e_1}{e_2} &\mapsto e_2 \semi m
&&\ts{E-IfFalse}
\end{align*}

\subsubsection{Top-Level Expression Semantics}

Judgment: $m \semi \hat{e} \topstep{\phi}{p} \hat{e}' \semi m'$
\begin{mathpar}
\inferrule*[right=E-Lift] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  m \semi e \topstep{\phi}{p} e' \semi m
}
\and
\inferrule*[right=E-LetTop] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  m \semi \elet{x}{e}{\hat{e}} \topstep{\phi}{p} \elet{x}{e'}{\hat{e}} \semi m
}
\end{mathpar}
\begin{align*}
m \semi \elet{x}{\eret{v}}{\hat{e}} &\topstep{\phi}{p} \hat{e}[v/x] \semi m
&&\ts{E-RetTop}\\
m \semi \estate{x}{\ell}{v}{\hat{e}}
&\topstep{\mf{Init}}{p}
\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}] \semi m'
&&\ts{\hl{E-StInit}}\\
(m' &= m \mid m[p].\mf{st}[\ell] = v)\\
m \semi \estate{x}{\ell}{v}{\hat{e}}
&\topstep{\mf{Succ}}{p}
\hat{e}[v'/x, \vset{\ell}{p}/x_\mf{set}] \semi m
&&\ts{\hl{E-StSucc}}\\
(v' &= m[p].\mf{st}[\ell])
\end{align*}

\subsubsection{Program Expression Semantics}

Judgment: $\D \semi P \mapsto P' \semi \D'$
\begin{mathpar}
\inferrule*[right=P-Lift] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  \D \semi e \mapsto e \semi \D
}
\and
\inferrule*[right=P-Let] {
  \cdot \semi e \mapsto e' \semi \cdot
} {
  \D \semi \elet{x}{e}{P}
  \mapsto
  \elet{x}{e'}{P} \semi \D
}
\end{mathpar}
\begin{align*}
\D \semi \elet{x}{\eret{v}}{P}
&\mapsto
P[v/x] \semi \D
&&\ts{E-RetProg}\\
\D \semi \elet{\C(x)}{\hat{e}}{P}
&\mapsto
P \semi \D[\C \mapsto x.\hat{e}]
&&\ts{\hl{E-CompDef}}
\end{align*}

\subsection{Event-Loop Semantics}

Say $m \semi \hat{e} \Topstep{\phi}{p} v \semi m'$ 
if $m \semi \hat{e} \xmapsto{*}^{\phi}_{p} \eret{v} \semi m'$.
\pagebreak
\subsubsection{Initialization}

Judgment: $\D \vdash v \bsinit{n} \pair{t}{m}$.

Define $\pi_0(\C, v)$ as $
\{\mf{spec}: \pair{\C}{v}, \mf{st}: \emptyset, \mf{child}: \emptyset,
\mf{rerender}: \mf{ff}\}$.

\begin{mathpar}
\inferrule*[right=\ts{Init-Lit}] {
  n > 0
} {
  \D \vdash \mf{text}\ v 
  \bsinit{n}
  \pair{\mf{lit}\ v}{\cdot}
}
\and
\inferrule*[right=\ts{Init-Node}] {
  n > 0
  \and
  \mf{id\ fresh}
  \and
  (\forall j)
  \quad
  \D \vdash v_j
  \bsinit{n-1}
  \pair{t_j}{m_j}
} {
  \D \vdash \mf{tag}(v, \num{v_i}, \num{v_j})
  \bsinit{n}
  \pair{\mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})}{\bigcup_j m_j}
}
\and
\inferrule*[right=\ts{Init-Comp}] {
  n > 0
  \and
  p\ \mf{fresh}
  \and
  \D[\C] = x.\hat{e}
  \\
  [p \mapsto \pi_0(\C, v)] 
  \semi \hat{e}[v/x] \Topstep{\mf{Init}}{p} v' \semi m 
  \\
  \D \vdash v' \bsinit{n-1} \pair{t}{m'}
} {
  \D \vdash \pair{\C}{v}
  \bsinit{n}
  \pair{p}{m' \uplus (m \mid m[p].\mf{child} = t)}
}
\and
\inferrule*[right=\ts{Init-Div}] { } {
  \D \vdash \bsinit{0} \bot
}
\and
\inferrule*[right=\ts{Init-Node}$_\bot$] {
  n > 0
  \and
  (\exists j)
  \quad
  \D \vdash v_j \bsinit{n-1} \bot
} {
  \D \vdash \mf{node}(\mf{id}, v, \num{v_i}, \num{v_j}) \bsinit{n} \bot
}
\and
\inferrule*[right=\ts{Init-Comp}$_\bot$] {
  n > 0
  \and
  p\ \mf{fresh}
  \and
  \D[\C] = x.\hat{e}
  \\
  [p \mapsto \pi_0(\C, v)] 
  \semi \hat{e}[v/x] \Topstep{\mf{Init}}{p} v' \semi m 
  \\
  \D \vdash v' \bsinit{n-1} \bot
} {
  \D \vdash \pair{\C}{v}
  \bsinit{n}
  \pair{p}{m' \uplus (m \mid m[p].\mf{child} = t)}
}
\end{mathpar}

\subsubsection{Rerendering}

Judgment: $\D \semi m \vdash t \bssucc{n} m'$

\begin{mathpar}
\inferrule*[right=Check-Lit] { } {
  \D \semi m \vdash \mf{lit}\ v \bssucc{n} \cdot
}
\and
\inferrule*[right=Check-Node] {
  (\forall j) \quad
  \D \semi m \vdash t_j \bssucc{n} m_j
} {
  \D \semi m \vdash \mf{node}(\mf{id}, v, \num{v_i}, \num{t_j}) 
  \bssucc{n} \bigcup_{\forall j} m_j
}
\and
\inferrule*[right=\hl{Check-PathF}] {
  m[p].\mf{rerender} = \mf{ff}
  \\
  \D \semi m \vdash m[p].\mf{child} \bssucc{n} m'
} {
  \D \semi m \vdash p \bssucc{n} m'
}
\and
\inferrule*[right=\hl{Check-PathT}] { 
  m[p].\mf{rerender} = \mf{tt}
  \\
  m'[p].\mf{spec} = \pair{\C}{v}
  \and
  \D[\C] = x.\hat{e}
  \\
  m \semi \hat{e}[v/x] \Topstep{\mf{Succ}}{p} \eret{v'} \semi \_
  \\
  \D \semi m \vdash v' \bsinit{n} \pair{t}{m'}
} {
  \D \semi m \vdash p \bssucc{n} m' \mid 
  m'[p].\mf{child}=t\ \mf{and}\ m'[p].\mf{rerender}=\mf{ff}
}
\and
\inferrule*[right=\hl{Check-PathDiv}] { 
  m[p].\mf{rerender} = \mf{tt}
  \\
  m'[p].\mf{spec} = \pair{\C}{v}
  \and
  \D[\C] = x.\hat{e}
  \\
  m \semi \hat{e}[v/x] \Topstep{\mf{Succ}}{p} \eret{v'} \semi \_
  \\
  \D \semi m \vdash v' \bsinit{n} \bot
} {
  \D \semi m \vdash p \bssucc{n} \bot
}
\and
\inferrule*[right=Check-Node$_\bot$] {
  (\exists j)
  \quad
  \D \semi m \vdash t_j \bssucc{n} \bot
} {
  \D \semi m \vdash \mf{node}(\mf{id}, v, \num{v_i}, \num{t_j}) \bssucc{n} \bot
}
\and
\inferrule*[right=Check-PathF$_\bot$] {
  m[p].\mf{rerender} = \mf{ff}
  \\
  \D \semi m \vdash m[p].\mf{child} \bssucc{n} \bot
} {
  \D \semi m \vdash p \bssucc{n} \bot
}
\end{mathpar}

\subsubsection{Handler Search}

Judgment: $m \semi \mf{id} \vdash \mf{handlers}(t) = \num{v}$

\begin{mathpar}
\inferrule*[right=Handlers-Lit] { } {
  m \semi \mf{id} \vdash \mf{handlers}(\mf{lit}\ v) = \{\}
}
\and
\inferrule*[right=Handlers-Comp] {
  m \semi \mf{id} \vdash \mf{handlers}(m[p].\mf{child}) = \num{v}
} {
  m \semi \mf{id} \vdash \mf{handlers}(p) = \num{v}
}
\and
\inferrule*[right=Handlers-Tgt] { } {
  m \semi \mf{id} \vdash \mf{handlers}(\mf{node}(id, v, \num{v_i}, \num{v_j}))
  = \{v_h \mid \mf{onClick}\ v_h \in \num{v_i}\}
}
\and
\inferrule*[right=Handlers-Node] {
  \mf{id} \neq \mf{id}'
  \and
  (\forall j)\ 
  m \semi \mf{id}\vdash \mf{handlers}(v_j) = h_j
} {
  m \semi \mf{id} \vdash \mf{handlers}(\mf{node}(id', v, \num{v_i}, \num{v_j}))
  = \bigcup_{\forall j} h_j
}
\end{mathpar}

\subsubsection{Event Loop}

Judgment: $\four{\D}{m}{t}{\mu} \hookrightarrow \four{\D'}{m'}{t}{\mu'}$

\begin{mathpar}
\inferrule*[right=\hl{Loop-Start}] {
  \D \semi P \mapsto P' \semi \D'
} {
  \four{\D}{m}{t}{\mf{start}(P)} 
  \hookrightarrow
  \four{\D'}{m}{t}{\mf{start}(P')}
}
\and
\inferrule*[right=\hl{Loop-Init}] {
  (\forall n \in \mathbb{N})
  \quad
  \D \vdash v \bsinit{n} \pair{t}{m}
} {
  \four{\D}{\_}{\_}{\mf{start}(\eret{v})} 
  \hookrightarrow 
  \four{\D}{m}{t}{\mf{idle}}
}
\and
\inferrule*[right=\hl{Loop-Event}] { } {
  \four{\D}{m}{t}{\mf{idle}} 
  \hookrightarrow 
  \four{\D}{m}{t}{\mf{search}(id)}
}
\and
\inferrule*[right=\hl{Loop-Search}] {
  m \semi \mf{id} \vdash \mf{handlers}(t) = \num{v}
} {
  \four{\D}{m}{t}{\mf{search}(id)}
  \hookrightarrow
  \four{\D}{m}{t}{\mf{handle}(\num{v\ ()})}
}
\and
\inferrule*[right=\hl{Loop-HandleCong}] {
  m \semi e \mapsto e' \semi m'
} {
  \four{\D}{m}{t}{\mf{handle}(e :: es)}
  \hookrightarrow
  \four{\D}{m'}{t}{\mf{handle}(e' :: es)}
}
\and
\inferrule*[right=\hl{Loop-HandleNext}] { } {
  \four{\D}{m}{t}{\mf{handle}(\eret{()} :: es)}
  \hookrightarrow
  \four{\D}{m}{t}{\mf{handle}(es)}
}
\and
\inferrule*[right=\hl{Loop-Rerender}] {
  (\forall n \in \mathbb{N})
  \quad
  \D \semi m \vdash t \bssucc{n} m'
} {
  \four{\D}{m}{t}{\mf{handle}([])}
  \hookrightarrow
  \four{\D}{m'}{t}{idle}
}
\end{mathpar}

\subsection{(Old) Runtime Type Soundness}

\subsubsection{Initialization}

\begin{lemma}[Progress of initialization]
If $\Delta \vDash \D$ and $\cdot \semi \Pi \vdval v: \mf{View}$,
then, for all $n \in \mathbb{N}$,
either $\D \vdash v \divinit{n}$ or $\D \vdash v \bsinit{n} \pair{t}{m}$.
\end{lemma}

\begin{proof}
By induction on $n$.

In the base case, when $n=0$, we can always apply rule \ts{Init-Div}
to conclude that $\D \vdash v \divinit{0}$ and we are done.

In the inductive case, we have $n>0$ by assumption.
We apply the canonical forms lemma to the typing premise
and case-analyze the result.

\tb{Case} $v = \mf{text}\ v'$: 
We simply apply $\ts{Init-Lit}$ to get 
$\D \vdash \mf{text}\ v' \bsinit{n} \pair{\mf{lit}\ v'}{\cdot}$
and we are done.

\tb{Case} $v = \mf{tag}(v', \num{v_i}, \num{v_j})$: unfinished

\tb{Case} $v = \pair{\C}{v}$: unfinished

\end{proof}

\begin{lemma}[Preservation of initialization]
If \begin{enumerate}[label=(\roman*)]
\item $\Delta \vDash \D$, 
\item $\cdot \semi \Pi \vdval v: \mf{View}$, and
\item $\D \vdash v \bsinit{n} \pair{t}{m}$,
\end{enumerate}
then there exists some $\Pi'$ with \begin{enumerate}[label=(\roman*)]
\item $\Pi \po \Pi'$, 
\item $\cdot \semi \Pi' \vdm m$, and
\item $\Pi' \vdtree t$. We also have
\item $\D \semi m \wf t$.
\end{enumerate}
\end{lemma}

\begin{proof}
By induction on the derivation of the big-step judgment.
\end{proof}

\begin{theorem}[Progress of configurations]
If $\Pi \vdcfg \four{\D}{m}{t}{\mu}$, either:
\begin{enumerate}[label=(\roman*)]
\item $\mu = \mf{idle}$,
\item $\mu = \mf{start}(\eret{v})$ and $\D \vdash v \divinit{ }$,
\item $\mu = \mf{handle}([])$ and $\D \semi m \vdash t \divsucc{ }$, or
\item $\four{\D}{m}{t}{\mu} \hookrightarrow \four{\D'}{m'}{t'}{\mu'}$.
\end{enumerate}
\end{theorem}

\begin{theorem}[Preservation of configurations]
If $\Pi \vdcfg \four{\D}{m}{t}{\mu}$\\
and $\four{\D}{m}{t}{\mu} \hookrightarrow \four{\D'}{m'}{t'}{\mu'}$,
there exists some $\Pi'$ with $\Pi \po \Pi'$
and $\Pi' \vdcfg \four{\D'}{m'}{t'}{\mu'}$.
    
\end{theorem}

\subsubsection{Initialization (Even Older)}

We first prove a number of lemmas which are necessary in our critical property.

\begin{lemma}[Path Preservation]
If $m \semi \hat{e} \topstep{\phi}{p} \hat{e}' \semi m'$
and $p \in \dom(m)$,
then $\dom(m) = \dom(m')$.
\end{lemma}

\begin{proof}
By induction on the derivation of the small-step judgment.

\tb{Case} \ts{E-StInit}: \begin{itemize}
\item We have
$m \semi \estate{x}{\ell}{v}{\hat{e}} \topstep{\mf{Init}}{p}
\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}] \semi m'$
where $m' = m \mid m[p].\mf{st}[\ell] = v$.
\item Since we assumed $p \in \dom(m)$, the addition of $p$
to $m'$ does not change its domain, and therefore we have $\dom(m)=\dom(m')$.
\end{itemize}

\tb{All other cases} do not alter $m$, and the lemma holds trivially.

\end{proof}

\begin{corollary}
If $m \semi \hat{e} \Topstep{\phi}{p} v \semi m'$
and $p \in \dom(m)$,
then $\dom(m) = \dom(m')$.
\end{corollary}

\begin{proof}
Immediate from path preservation and definition of $\Topstep{\phi}{p}$.
\end{proof}

\begin{lemma}[Memory Freshness]
If $\D \vdash v \bsinit{n} \pair{t}{m}$,
then for all $p \in \dom(m)$, we have $p\ \mf{fresh}$.
\end{lemma}

\begin{proof}
By induction over the derivation of the big-step judgment.

\tb{Case} \ts{Init-Lit}: $m=\cdot$, so the conclusion holds trivially.

\tb{Case} \ts{Init-Node}: \begin{itemize}
\item We have
$\D \vdash \mf{tag}(v, \num{v_i}, \num{v_j}) 
\bsinit{n}
\pair{\mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})}{\bigcup_j m_j}$.
\item For all $j$, we also have $\D \vdash v_j \bsinit{n} \pair{t_j}{m_j}$.
\item We apply the inductive hypothesis to each, getting that
all $m_j$ only contain fresh $p$.
\item Therefore, their union $\bigcup_j m_j$ also only contains fresh $p$,
and we are done.
\end{itemize}

\tb{Case} \ts{Init-Comp}: \begin{itemize}
\item We start with $\D \vdash \pair{\C}{v} 
\bsinit{n} \pair{p}{m' \cup (m \mid m[p].\mf{child} = t)}$.
\item Let $\D[\C] = x.\hat{e}$ and
$m_0 = \{\mf{spec}: \pair{\C}{v_0}, \mf{st}: \emptyset, \mf{child}: \emptyset\}$.
\item We have the following premises:
\begin{enumerate}[label=(\alph*)]
  \item $p$ is fresh,
  \item $m_0 \semi \hat{e}[v_0/x] \Topstep{\mf{Init}}{p} v \semi m$, and
  \item $\D \vdash v \bsinit{n} \pair{t}{m'}$.
\end{enumerate}
\item By (a), clearly $m_0$ has all $p$ fresh.
\item By the path preservation corollary on (b), $m$ also has all $p$ fresh.
\item By inductive hypothesis on $c$, $m'$ also has all $p$ fresh.
\item Therefore, $m' \cup (m \mid m[p].\mf{child} = t)$
has all $p$ fresh and we are done.
\end{itemize}
\end{proof}

\begin{lemma}[Label Initialization]
If $m \semi \hat{e} \topstep{\mf{Init}}{p} \hat{e}' \semi m'$,
then $\mc{L}(\hat{e}) \setminus \mc{L}(\hat{e}') \subseteq \dom(m'[p].\mf{st})$.
\end{lemma}

\begin{proof}
By case-analysis of the small-step judgment. Note that if 
$\mc{L}(\hat{e}) \setminus \mc{L}(\hat{e}') = \emptyset$,
the conclusion holds trivially.

\tb{Case} \ts{E-Lift}: \begin{itemize}
\item We have $\cdot \semi e \mapsto e' \semi \cdot$.
\item By definition, $\mc{L}(e)=\emptyset=\mc{L}(e')$.
\item Clearly then $\mc{L}(e) \setminus \mc{L}(e')=\emptyset$ and we are done.
\end{itemize}

\tb{Case} \ts{E-LetTop}: \begin{itemize}
\item We have $m \semi \elet{x}{e}{\hat{e}} \topstep{\mf{Init}}{p} \elet{x}{e'}{\hat{e}} \semi m$,
with $\cdot \semi e \mapsto e' \semi \cdot$.
\item By definition, $\mc{L}(\elet{x}{e}{\hat{e}})=\mc{L}(\elet{x}{e'}{\hat{e}})=\mc{L}(\hat{e})$.
\item We get $\mc{L}(\elet{x}{e}{\hat{e}}) \setminus \mc{L}(\elet{x}{e'}{\hat{e}})=\emptyset$ and we are done.
\end{itemize}

\tb{Case} \ts{E-RetTop}: \begin{itemize}
\item We have $m \semi \elet{x}{\eret{v}}{\hat{e}} 
\topstep{\mf{Init}}{p} \hat{e}[v/x] \semi m$.
\item By definition, $\mc{L}(\elet{x}{\eret{v}}{\hat{e}})=\mc{L}(\hat{e})$.
\item By label preservation over substitution, 
$\mc{L}(\hat{e}[v/x])=\mc{L}(\hat{e})$.
\item We get $\mc{L}(\elet{x}{\eret{v}}{\hat{e}}) 
\setminus \mc{L}(\hat{e}[v/x])=\emptyset$ and we are done.
\end{itemize}

\tb{Case} \ts{E-StInit}: \begin{itemize}
\item We have $m \semi \estate{x}{\ell}{v}{\hat{e}} 
\topstep{\mf{Init}}{p} \hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]
\semi m'$, where $m' = m \mid m[p].\mf{st}[\ell] = v$.
\item By definition, $\mc{L}(\estate{x}{\ell}{v}{\hat{e}}) 
= \{\ell\} \cup \mc{L}(\hat{e})$.
\item By label preservation over substitution,
$\mc{L}(\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]) = \mc{L}(\hat{e})$.
\item We subcase on whether $\ell \in \mc{L}(\hat{e})$.
\item Subcase 1: $\ell \in \mc{L}(\hat{e})$.
\begin{itemize}
  \item It follows that $\mc{L}(\estate{x}{\ell}{v}{\hat{e}}) = \mc{L}(\hat{e})$.
  \item Thus, $\mc{L}(\estate{x}{\ell}{v}{\hat{e}}) \setminus
\mc{L}(\hat{e}[v/x, \vset{\ell}{p}/x_\mf{set}]) = \emptyset$ and we are done.
\end{itemize}
\item Subcase 2: $\ell \notin \mc{L}(\hat{e})$.
\begin{itemize}
  \item Then $\mc{L}(\estate{x}{\ell}{v}{\hat{e}}) \setminus \mc{L}(\hat{e}) = \{\ell\}$.
  \item Thus, we need to show $\ell \in m'[p].\mf{st}$.
  \item This is given by definition of $m'$, and we are done.
\end{itemize}
\end{itemize}

\tb{Case} \ts{E-StSucc}: Impossible, 
since this case requires $\phi=\mf{Succ}$,
which contradicts our assumption that $\phi=\mf{Init}$.

\end{proof}

\begin{corollary}
If $m \semi \hat{e} \Topstep{\mf{Init}}{p} v \semi m'$,
then $\mc{L}(\hat{e}) \subseteq \dom(m'[p].\mf{st})$.
\end{corollary}

\begin{proof}
Follows from label initialization and the fact that 
$\mc{L}(\eret{v})=\emptyset$, which means that 
$\mc{L}(\hat{e}) \setminus \mc{L}(\eret{v}) = \mc{L}(\hat{e})$.
\end{proof}

\begin{lemma}[Strong Disjoint Memory]
If \begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Pi \semi \D \vdm m$,
\item $\Gamma \semi \Pi \semi \D \vdm m'$, and
\item $\dom(m) \cap \dom(m') = \emptyset$,
\end{enumerate}
then $\Gamma \semi \Pi \semi \D \vdm m \cup m'$.
\end{lemma}

\begin{proof}
Follows easily from definition of $\vdm$.
\end{proof}

\begin{theorem}[Critical Property of Initialization]
If \begin{enumerate}[label=(\roman*)]
\item $\D \vdash v \bsinit{n} \pair{t}{m}$,
\item $\Delta \vDash \D$, and
\item $\cdot \semi \Pi \vdval v: \mf{View}$,
\end{enumerate}
then there exists some $\Pi'$ such that: \begin{enumerate}[label=(\roman*)]
\item $\Pi \sqsubseteq \Pi'$,
\item $\Pi' \vdtree t$, and
\item $\cdot \semi \Pi' \semi \D \vdm m$.
\end{enumerate}
\end{theorem}

\begin{proof}
By induction on derivation of \emph{(i)}.

\tb{Case} \ts{Init-Lit}: \begin{itemize}
\item We have $\D \vdash \mf{text}\ v \bsinit{n} \pair{\mf{lit}\ v}{\cdot}$.
\item Choose $\Pi'=\Pi$; conclusions \emph{(i)} and \emph{(iii)} are trivial.
\item By premise \emph{(iii)} and \ts{TV-Text},\\
we have $\cdot \semi \Pi \vdval v: \tstr$.
\item We conclude via \ts{TTree-Lit} that $\Pi \vdtree \mf{lit}\ v$
and we are done. 
\end{itemize}

\tb{Case} \ts{Init-Node}: \begin{itemize}
\item We have $\D \vdash \mf{tag}(v, \num{v_i}, \num{v_j})
  \bsinit{n}
  \pair{\mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})}{\bigcup_j m_j}$.
\item Furthermore, for all $j$, we have (a) that $\D \vdash v_j \bsinit{n} \pair{t_j}{m_j}$.
\item By \emph{(iii)} and \ts{TV-Tag}, we have \begin{enumerate}[label=(\alph*)]
  \stepcounter{enumi}
  \item $\cdot \semi \Pi \vdval v: \tstr$,
  \item $\cdot \semi \Pi \vdval v_i: \mf{Attr}$ for all $i$, and
  \item $\cdot \semi \Pi \vdval v_j: \mf{View}$ for all $j$.
\end{enumerate}
\item We \emph{iteratively} apply the inductive hypothesis for each $j$.
\begin{itemize}
  \item Let $j$ range from $0$ to $n$ (exclusive).
  \item Let $\Pi_0=\Pi$.
  \item At step $j$, we weaken (d) to get (e) that $\cdot \semi \Pi_j \vdval v_j: \mf{View}$.
  \begin{itemize}
    \item That $\Pi \po \Pi_j$ is given by reflexivity at $j=0$, 
    or the previous conclusion (f) \& transitivity of $\po$ otherwise.
    (We have a chain $\Pi_0 \po \Pi_1 \po \cdots \po \Pi_j$).
  \end{itemize}
  \item We apply the inductive hypothesis using (a), (e), and premise \emph{(ii)}.
  \item We get some $\Pi_{j+1}$ with:
  \begin{enumerate}[label=(\alph*)]
    \setcounter{enumi}{5}
    \item $\Pi_j \po \Pi_{j+1}$,
    \item $\Pi_{j+1} \vdtree t_j$, and
    \item $\cdot \semi \Pi_{j+1} \semi \D \vdm m_{j}$.
  \end{enumerate}
\end{itemize}
\item Choose $\Pi'=\Pi_n$.
\item By (f) and transitivity of $\po$, we get $\Pi \po \Pi_n$, i.e. conclusion \emph{(i)}.
\item Since $\Pi_{j+1} \po \Pi_n$ for all $j$, weaken (g) and (h) to say
\begin{enumerate}[label=(\alph*')]
  \setcounter{enumi}{6}
  \item $\Pi_n \vdtree t_j$ and
  \item $\cdot \semi \Pi_{n} \semi \D \vdm m_j$.
\end{enumerate}
\item By the memory freshness lemma on (a), we get that all $p \in m_j$ are fresh,
i.e. all $m_j$ are disjoint.
\item Therefore, by the strong disjoint memory lemma,
we get by (h$'$) that
$\cdot \semi \Pi_n \vdm \bigcup_j m_j$, i.e. conclusion \emph{(iii)}.
\item Finally, we apply \ts{TTree-Node} using (b) and (c) weakened on $\Pi$,
as well as (g$'$),
to get $\Pi_n \vdtree \mf{node}(\mf{id}, v, \num{v_i}, \num{t_j})$, 
i.e conclusion \emph{(ii)}, and we are done.
\end{itemize}

\tb{Case} \ts{Init-Comp}: \begin{itemize}
\item We start with $\D \vdash \pair{\C}{v_0}
  \bsinit{n}
  \pair{p}{m' \cup (m \mid m[p].\mf{child} = t)}$.
\item Let $\D[\C] = x.\hat{e}$ and
$m_0 = \{\mf{spec}: \pair{\C}{v_0}, \mf{st}: \emptyset, \mf{child}: \emptyset\}$.
\item We have the following premises:
\begin{enumerate}[label=(\alph*)]
  \item $p$ is fresh,
  \item $m_0 \semi \hat{e}[v_0/x] \Topstep{\mf{Init}}{p} v \semi m$ (S3), and
  \item $\D \vdash v \bsinit{n} \pair{t}{m'}$.
\end{enumerate}
\item Let $\Pi_0=\Pi[p \mapsto \C]$; by freshness of $p$, $\Pi \po \Pi_0$.
Note that $\cdot \semi \Pi_0 \vdm m_0$ (S2) by definition.
\item By premise \emph{(ii)}, we get 
$[x \mapsto \Delta[\C]] \semi \cdot \semi \C \vdtop \hat{e}$.
\item Premise \emph{(iii)} gives the following derivation (bottom-up):
\begin{mathpar}
\inferrule*[right=TV-Spec] {
  \inferrule*[right=TV-Comp] {
    \inferrule*{(d)} {\Delta[\C] = \ptp}
  } {
    \cdot \semi \Pi \vdval \C: \mf{Component}\ \ptp
  }
  \and
  \inferrule*{(e)} {\cdot \semi \Pi \vdval v_0: \ptp}
} {
  \cdot \semi \Pi \vdval \pair{\C}{v_0} : \mf{View}
}
\end{mathpar}
\item By (d), $[x \mapsto \ptp] \semi \cdot \semi \C \vdtop \hat{e}$.
\item By (e), weakening, and substitution lemma,
$\cdot \semi \Pi \semi \C \vdtop \hat{e}[v/x]$.
\item We weaken this again to get $\cdot \semi \Pi_0 \semi \C \vdtop \hat{e}[v/x]$ (S1).
\item We can therefore apply the soundness of top-level expressions at initial phase
to (S1-3), and get that:
\begin{enumerate}[label=(\alph*)]
  \setcounter{enumi}{5}
  \item $\cdot \semi \Pi_0 \vdval v: \mf{View}$, and
  \item $\cdot \semi \Pi_0 \vdm m$.
\end{enumerate}
\item We then apply the inductive hypothesis to (c) and (f) to get some $\Pi_1$ with:
\begin{enumerate}[label=(\alph*)]
  \setcounter{enumi}{7}
  \item $\Pi_0 \po \Pi_1$,
  \stepcounter{enumi}
  \item $\Pi_1 \vdtree t$, and
  \item $\cdot \semi \Pi_1 \semi \D \vdm m'$.
\end{enumerate}
\item Choose this $\Pi_1$ as our $\Pi'$. We now need to show each of the conclusions.
\item That $\Pi \po \Pi'$ follows from (h), definition of $\Pi_0$,
and transitivity of $\po$. (Conclusion \emph{(i)} satisfied.)
\item That $\Pi' \vdtree p$ (conclusion \emph{(ii)}) 
is immediate from \ts{TTree-Path} and $\Pi_0 \po \Pi'$.
\item We then need to show conclusion \emph{(iii)}, that 
$\cdot \semi \Pi' \semi \D \vdm m' \cup (m \mid m[p].\mf{child} = t)$.
\item Let $m_1 = m \mid m[p].\mf{child} = t$.
By (k) and the strong disjoint memory lemma, it suffices to show
that $\cdot \semi \Pi' \semi \D \vdm m_1$.
\item By definition of $\vdm$, we need the following sub-conclusions:
\begin{enumerate}[label=(iii-\roman*)]
  \item $\cdot \semi \Pi' \vdm m_1$,
  \item $\Delta \vDash \D$,
  \item for all $p \in \dom(m_1)$, for $\D[\Pi[p]]=x.\hat{e}$,
  $\mc{L}(\hat{e}) \subseteq \dom(m_1[p].\mf{st})$, and
  \item for all $p \in \dom(m_1)$, $\Pi' \vdtree m_1[p].\mf{child}$.
\end{enumerate}
\item (\emph{iii}-a) follows from (g) and weakening via (h).
\item (\emph{iii}-b) is immediate by premise \emph{(ii)}.
\item For (\emph{iii}-c) and (\emph{iii}-d), we apply the path preservation lemma
to (b) to say that $\dom(m_0) = \dom(m)$.
\item By definition of each of $m_0$ and $m_1$, we get $\dom(m_1)=\{p\}$.
\item Therefore, (\emph{iii}-d) is satisfied by (j).
\item Finally, the label initialization corollary gives us that\\
$\mc{L}(\hat{e}) \subseteq \dom(m[p].\mf{st})$, 
i.e. sub-conclusion (\emph{iii}-c), and we are done.
\end{itemize}
\end{proof}

\section{$\lambda_{CC}$}

\subsection{Syntax}

Additions/changes from React-tRace Core are \hl{highlighted}.
Intermediate-only constructs are \grey{greyed}
\begin{align*}
\text{Value } v &::= x \mid () \mid \mf{true} \mid \mf{false} 
\mid s \mid \lambda x: \ptp.e\\
&\mid \mf{text}\ v \mid \mf{tag}(v, \num{v}, \num{v})
\mid \mf{attr}(v, v) \mid \mf{onClick}\ v \mid \mgrey{\ell}\\
\text{Expression } e &::= \eret{v} \mid \elet{x}{e}{e} 
\mid v\ v \mid \eif{v}{e}{e}\\
&\mid \mhl{\eref{v}} \mid \mhl{\eget{e}} \mid \mhl{\eset{v}{v}}\\
\text{Type } \ptp &::= \tone \mid \mf{Bool} \mid \mhl{\ptp \rightarrow \ptp}
\mid \mf{View} \mid \mf{Attr} \mid \mhl{\mf{Ref}\ \ptp}\\
\end{align*}
Internal representations:
\begin{align*}
\text{Type context } \Gamma &::= \map{x}{\ptp}\\
\text{Store } \sigma &::= \map{\ell}{v}\\
\text{Tree }t &::= \mf{lit}\ v \mid \mf{node}(id, v, \num{v}, \num{t})\\
\text{Mode }\mu &::= \mf{start}(e) \mid \mf{idle} \mid \mf{render}
\end{align*}

\subsection{Typing}

Non-standard rules are \hl{highlighted}, although on the whole,
they are the same as in React-tRace Core. (Most relevant are the omissions.)

\pagebreak

\subsubsection{Value Typing}

Judgment: $\Gamma \vdashv v: \ptp$

\begin{mathpar}
\inferrule*[right=TVal-Var] {
  x : \ptp \in \Gamma
} {
  \Gamma \vdashv x: \ptp
}
\and
\inferrule*[right=TVal-Unit] { } {
  \Gamma \vdashv () : \tone
}
\and
\inferrule*[right=TVal-True] { } {
  \Gamma \vdashv \mf{true} : \mf{Bool}
}
\and
\inferrule*[right=TVal-False] { } {
  \Gamma \vdashv \mf{false} : \mf{Bool}
}
\and
\inferrule*[right=TVal-Str] { } {
  \Gamma \vdashv s : \tstr
}
\and
\inferrule*[right=TVal-Lam] {
  \Gamma, x: \ptp_1 \vdashe e : \ptp_2
} {
  \Gamma \vdashv \lambda x: \ptp_1.e: \ptp_1 \rightarrow \ptp_2
}
\and
\inferrule*[right=\hl{TVal-Text}] {
  \Gamma \vdashv v: \ptp
  \and
  \ptp \in \{\mf{Bool},\tstr\}
} {
  \Gamma \vdashv \mf{text}\ v: \mf{View}
}
\and
\inferrule*[right=\hl{TVal-Tag}] {
  \Gamma \vdashv v: \tstr
  \and
  (\forall i) \
  \Gamma \vdashv v_i : \mf{Attr}
  \and
  (\forall j) \
  \Gamma \vdashv v_j: \mf{View}
}{
  \Gamma \vdashv \mf{tag}(v, \num{v_i}, \num{v_j}): \mf{View}
}
\and
\inferrule*[right=\hl{TVal-Attr}] {
  \Gamma \vdashv v_1: \tstr
  \and
  \Gamma \vdashv v_2: \tstr
} {
  \Gamma \vdashv \mf{attr}(v_1, v_2): \mf{Attr}
}
\and
\inferrule*[right=\hl{TVal-OnClick}] {
  \Gamma \vdashv v: \tone \rightarrow \tone
} {
  \Gamma \vdashv \mf{onClick}\ v: \mf{Attr}
}
\end{mathpar}

\pagebreak

\subsubsection{Expression Typing}

Additions relative to React-tRace core are \hl{highlighted},
although these are just standard rules for references.

A top-level program should have type $\tone \rightarrow \mf{View}$.

Judgment: $\Gamma \vdashe e: \ptp$

\begin{mathpar}
\inferrule*[right=TExp-Ret] {
  \Gamma \vdashv v: \ptp
} {
  \Gamma \vdashe \eret{v}: \ptp
}
\and
\inferrule*[right=TExp-Let] {
  \Gamma \vdashe e_1: \ptp_1
  \and
  \Gamma, x: \ptp_1 \vdashe e_2: \ptp_2
} {
  \Gamma \vdashe \elet{x}{e_1}{e_2}: \ptp_2
}
\and
\inferrule*[right=TExp-App] {
  \Gamma \vdashv v_1: \ptp_1 \rightarrow \ptp_2
  \and
  \Gamma \vdashv v_2: \ptp_2
} {
  \Gamma \vdashe v_1\ v_2: \ptp_2
}
\and
\inferrule*[right=TExp-If] {
  \Gamma \vdashv v : \mf{Bool}
  \and
  \Gamma \vdashe e_1 : \ptp
  \and
  \Gamma \vdashe e_2 : \ptp
} {
  \Gamma \vdashe \eif{v}{e_1}{e_2} : \ptp
}
\and
\inferrule*[right=\hl{TExp-Ref}] {
  \Gamma \vdashv v: \ptp
} {
  \Gamma \vdashe \eref{v}: \mf{Ref}\ \ptp
}
\and
\inferrule*[right=\hl{TExp-Get}] {
  \Gamma \vdashv v : \mf{Ref}\ \ptp
} {
  \Gamma \vdashe \eget{v} : \ptp
}
\and
\inferrule*[right=\hl{TExp-Set}] {
  \Gamma \vdashv v_1: \mf{Ref}\ \ptp
  \and
  \Gamma \vdashv v_2: \ptp
} {
  \Gamma \vdashe \eset{v_1}{v_2}: \tone
}
\end{mathpar}

\subsection{Operational Semantics}

\subsubsection{Expression Evaluation}

Judgment: $\sigma, e \mapsto e' \semi \sigma'$
\begin{mathpar}
\inferrule*[right=E-Let] {
  \sigma \semi e_1 \mapsto e_1' \semi \sigma'
} {
  \sigma \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi \sigma'
}
\end{mathpar}
\begin{align*}
\sigma \semi \elet{x}{\eret{v}}{e} 
&\mapsto e[v/x] \semi \sigma
&&\ts{E-Ret}\\
\sigma \semi (\lambda x.e) v
&\mapsto e[v/x] \semi \sigma
&&\ts{E-Beta}\\
\sigma \semi \eif{\mf{true}}{e_1}{e_2} 
&\mapsto e_1 \semi \sigma
&&\ts{E-IfTrue}\\
\sigma \semi \eif{\mf{false}}{e_1}{e_2} 
&\mapsto e_2 \semi \sigma
&&\ts{E-IfFalse}\\
\sigma \semi \eref{v} 
&\mapsto \eret{\ell} \semi \sigma[\ell \mapsto v] \quad (\ell \text{ fresh})
&&\ts{E-Ref}\\
\sigma \semi \eget{\ell}
&\mapsto \eret{\sigma[\ell]} \semi \sigma
&&\ts{E-Get}\\
\sigma \semi \eset{\ell}{v}
&\mapsto \eret{()} \semi \sigma[\ell \mapsto v]
&&\ts{E-Set}
\end{align*}

\subsubsection{Tree Annotation}

Judgment: $\mf{annotate}(v) = t$

\begin{mathpar}
\inferrule*[right=Annot-Text] { } {
  \mf{annotate}(\mf{text}\ v) = \mf{lit}\ v
}
\and
\inferrule*[right=Annot-Tag] {
    id \text{ fresh}
    \and
    (\forall j)\
    \mf{annotate}(v_j) = t_j
} {
  \mf{annotate}(\mf{tag}(v, \num{v_i}, \num{v_j})) 
  = \mf{node}(id, v, \num{v_i}, \num{t_j})\\
}
\end{mathpar}

\subsubsection{Handler Search}

Judgment: $\mf{handlers}(t, id) = \num{v}$

\begin{mathpar}
\inferrule*[right=Handlers-Lit] { } {
  m \vdash \mf{handlers}(\mf{lit}\ v, id) = \{\}
}
\and
\inferrule*[right=Handlers-Tgt] { } {
  m \vdash \mf{handlers}(\mf{node}(id, v, \num{v_i}, \num{v_j}), id)
  = \{v_h \mid \mf{onClick}\ v_h \in \num{v_i}\}
}
\and
\inferrule*[right=Handlers-Node] {
  id \neq id'
  \and
  (\forall j)\ 
  m \vdash \mf{handlers}(v_j, id) = h_j
} {
  m \vdash \mf{handlers}(\mf{node}(id', v, \num{v_i}, \num{v_j}), id)
  = \bigcup_{\forall j} h_j
}
\end{mathpar}

\subsubsection{Reactive Semantics}

Judgment: $\four{\sigma}{t}{v}{\mu} \hookrightarrow \four{\sigma'}{t'}{v'}{\mu'}$

\begin{mathpar}
\inferrule*[right=R-Start] {
  \cdot \semi e \mapsto^* \eret{v} \semi \sigma
} {
  \four{\cdot}{\cdot}{\cdot}{\mf{start}(e)}
  \hookrightarrow
  \four{\sigma}{\cdot}{v}{\mf{render}}
}
\and
\inferrule*[right=R-Render] {
  \sigma \semi v_r\; () \mapsto^* \eret{v} \semi \sigma'
  \and
  \mf{annotate}(v) = t
} {
  \four{\sigma}{\_}{v_r}{\mf{render}}
  \hookrightarrow
  \four{\sigma'}{t}{v_r}{\mf{idle}}
}
\and
\inferrule*[right=R-Event] {
  \exists id
  \and
  \mf{handlers}(t, id) = \num{v_i}
  \\
  (0 \leq i < n) \quad
  \sigma_i \semi v_i\; () \mapsto^* \eret{()} \semi \sigma_{i+1}
} {
  \four{\sigma_0}{t}{v_r}{\mf{idle}}
  \hookrightarrow
  \four{\sigma_n}{t}{v_r}{\mf{render}}
}
\end{mathpar}

\subsection{Type Soundness (Expressions)}

\subsubsection{Store Context}

We introduce the additional context $\Sigma=\map{\ell}{\ptp}$
to type standalone locations.

Define $\Gamma \semi \Sigma \vDash \sigma$ to say that 
$\mf{dom}(\Sigma) = \mf{dom}(\sigma)$ and
$\Gamma \semi \Sigma \vdashv \sigma[\ell]: \Sigma[\ell]$.

Further define $\Sigma \sqsubseteq \Sigma'$ to hold when,
for all $\ell \in \Sigma$, both $\ell \in \Sigma'$ and $\Sigma(\ell) = \Sigma'(\ell)$.
Overload $\sqsubseteq$ analogously for type contexts $\Gamma$.

\subsubsection{Typing Rules}

We extend all typing rules with $\Sigma$, and introduce
a new rule \ts{TVal-Loc} to use it.

Augmented value judgment: $\Gamma \semi \Sigma \vdashv v: \ptp$

\begin{mathpar}
\inferrule*[right=\hl{TVal-Loc}] {
  \ell : \ptp \in \Sigma
} {
  \Gamma \semi \Sigma \vdashv \ell: \mf{Ref}\ \ptp
}
\and
\inferrule*[right=TVal-Var] {
  x : \ptp \in \Gamma
} {
  \Gamma \semi \Sigma \vdashv x: \ptp
}
\and
\inferrule*[right=TVal-Unit] { } {
  \Gamma \semi \Sigma \vdashv () : \tone
}
\and
\inferrule*[right=TVal-True] { } {
  \Gamma \semi \Sigma \vdashv \mf{true} : \mf{Bool}
}
\and
\inferrule*[right=TVal-False] { } {
  \Gamma \semi \Sigma \vdashv \mf{false} : \mf{Bool}
}
\and
\inferrule*[right=TVal-Str] { } {
  \Gamma \semi \Sigma \vdashv s : \tstr
}
\and
\inferrule*[right=TVal-Lam] {
  \Gamma, x: \ptp_1 \semi \Sigma \vdashe e : \ptp_2
} {
  \Gamma \semi \Sigma \vdashv \lambda x: \ptp_1.e: \ptp_1 \rightarrow \ptp_2
}
\and
\inferrule*[right=TVal-Text] {
  \Gamma \semi \Sigma \vdashv v: \ptp
  \and
  \ptp \in \{\mf{Bool},\tstr\}
} {
  \Gamma \semi \Sigma \vdashv \mf{text}\ v: \mf{View}
}
\and
\inferrule*[right=TVal-Tag] {
  \Gamma \semi \Sigma \vdashv v: \tstr
  \\
  (\forall i) \
  \Gamma \semi \Sigma \vdashv v_i : \mf{Attr}
  \and
  (\forall j) \
  \Gamma \semi \Sigma \vdashv v_j: \mf{View}
}{
  \Gamma \semi \Sigma \vdashv \mf{tag}(v, \num{v_i}, \num{v_j}): \mf{View}
}
\and
\inferrule*[right=TVal-Attr] {
  \Gamma \semi \Sigma \vdashv v_1: \tstr
  \and
  \Gamma \semi \Sigma \vdashv v_2: \tstr
} {
  \Gamma \semi \Sigma \vdashv \mf{attr}(v_1, v_2): \mf{Attr}
}
\and
\inferrule*[right=TVal-OnClick] {
  \Gamma \semi \Sigma \vdashv v: \tone \rightarrow \tone
} {
  \Gamma \semi \Sigma \vdashv \mf{onClick}\ v: \mf{Attr}
}
\end{mathpar}

Augmented expression judgment: $\Gamma \semi \Sigma \vdashe e: \ptp$

\begin{mathpar}
\inferrule*[right=TExp-Ret] {
  \Gamma \semi \Sigma \vdashv v: \ptp
} {
  \Gamma \semi \Sigma \vdashe \eret{v}: \ptp
}
\and
\inferrule*[right=TExp-Let] {
  \Gamma \semi \Sigma \vdashe e_1: \ptp_1
  \and
  \Gamma, x: \ptp_1 \semi \Sigma \vdashe e_2: \ptp_2
} {
  \Gamma \semi \Sigma \vdashe \elet{x}{e_1}{e_2}: \ptp_2
}
\and
\inferrule*[right=TExp-App] {
  \Gamma \semi \Sigma \vdashv v_1: \ptp_1 \rightarrow \ptp_2
  \and
  \Gamma \semi \Sigma \vdashv v_2: \ptp_2
} {
  \Gamma \semi \Sigma \vdashe v_1\ v_2: \ptp_2
}
\and
\inferrule*[right=TExp-If] {
  \Gamma \semi \Sigma \vdashv v : \mf{Bool}
  \and
  \Gamma \semi \Sigma \vdashe e_1 : \ptp
  \and
  \Gamma \semi \Sigma \vdashe e_2 : \ptp
} {
  \Gamma \semi \Sigma \vdashe \eif{v}{e_1}{e_2} : \ptp
}
\and
\inferrule*[right=TExp-Ref] {
  \Gamma \semi \Sigma \vdashv v: \ptp
} {
  \Gamma \semi \Sigma \vdashe \eref{v}: \mf{Ref}\ \ptp
}
\and
\inferrule*[right=TExp-Get] {
  \Gamma \semi \Sigma \vdashv v : \mf{Ref}\ \ptp
} {
  \Gamma \semi \Sigma \vdashe \eget{v} : \ptp
}
\and
\inferrule*[right=TExp-Set] {
  \Gamma \semi \Sigma \vdashv v_1: \mf{Ref}\ \ptp
  \and
  \Gamma \semi \Sigma \vdashv v_2: \ptp
} {
  \Gamma \semi \Sigma \vdashe \eset{v_1}{v_2}: \tone
}
\end{mathpar}

\subsubsection{Unproved Lemmas}

We assume that the typical structural rules for intuitionistic calculi,
i.e. weakening, contraction, and exchange, hold on both $\Gamma$ and $\Sigma$.
For example:

\begin{lemma}[Weakening $\Gamma$ in $\vdashv$]
If $\Gamma \semi \Sigma \vdashv v: \ptp$
then $\Gamma' \semi \Sigma \vdashv v: \ptp$ 
for any $\Gamma'$ such that $\Gamma \sqsubseteq \Gamma'$.
\end{lemma}

\begin{proof}
By induction on the derivation of the typing judgment.
\end{proof}

We also assume the following canonical forms lemma on values:

\begin{lemma}[Canonical Forms]
If $\cdot \semi \Sigma \vdashv v: \ptp$, then
\begin{itemize}
\item If $\ptp=\mf{Bool}$, then $v=\mf{true}$ or $v=\mf{false}$.
\item If $\ptp=\tone$, then $v=()$.
\item If $\ptp=\tstr$, then $v=s$ for some string $s$.
\item If $\ptp=\ptp_1 \rightarrow \ptp_2$, then $v=\lambda x: \ptp_1.e$.
\item If $\ptp=\mf{Attr}$, then $v=\mf{attr}(v_1, v_2)$ or $v=\mf{onClick} v$.
\item If $\ptp=\mf{View}$, then $v=\mf{text}\ v$ or $v=\mf{tag}(v, \num{v_i}, \num{v_j})$. 
\item If $\ptp=\mf{Ref}\ \ptp'$, then $v=\ell$.
\end{itemize}
\end{lemma}

\subsubsection{Substitution}

\begin{lemma}[Type preservation under substitution]
If $\Gamma \semi \Sigma \vdashv v: \ptp$, then the following two conditionals hold:
\begin{enumerate}[label=(\roman*)]
\item If $\Gamma, x: \ptp \semi \Sigma \vdashv v': \ptp'$, 
then $\Gamma \semi \Sigma \vdashv v'[v/x]: \ptp'$.
\item If $\Gamma, x: \ptp \semi \Sigma \vdashe e: \ptp'$, 
then $\Gamma \semi \Sigma \vdashe e[v/x]: \ptp'$.
\end{enumerate}
\end{lemma}

\begin{proof}
By mutual induction on \emph{(i)} and \emph{(ii)}. For each,
we induct over the derivation of the respective typing judgment
in the antecedent.

\tb{Case} \ts{TVal-Var}:
We have $\Gamma, x: \ptp \semi \Sigma \vdashv x: \ptp'$.
Applying substitution, we want to show 
$\Gamma \semi \Sigma \vdashv v: \ptp'$, which is exactly our first premise
(with $\ptp'=\ptp$).

\tb{Case} \ts{TVal-Unit}:
We have $\Gamma, x: \ptp \semi \Sigma \vdashv (): \tone$.
Trivial, since $()[v/x]=()$ and $\Gamma \semi \Sigma \vdashv (): \tone$ axiomatically.
We elide analogous cases for boolean and string constants.

\tb{Case} \ts{TVal-Loc}:
We have $\Gamma, x: \ptp \semi \Sigma \vdashv \ell: \ptp$.
By inversion, $\ell: \ptp \in \Sigma$,
and since $\ell[v/x]=\ell$, we get $\Gamma \semi \Sigma \vdashv \ell: \ptp$.

\tb{Case} \ts{TVal-Lam}:
We have $\Gamma, x: \ptp \semi \Sigma \vdashv \lambda y.e : \ptp_1 \rightarrow \ptp_2$.
First suppose $y=x$: by definition of substitution, $(\lambda x.e)[v/x] = \lambda x.e$,
and the conclusion follows from inversion \& reapplication of \ts{TVal-Lam},
noting that $x: \ptp$ is overridden by $x: \ptp_1$ in the premise.

If $y \neq x$, then $(\lambda y.e)[v/x] = \lambda y.(e[v/x])$,
and inversion gives us $\Gamma, x: \ptp, y: \ptp_1 \semi \Sigma \vdashe e: \ptp_2$.
Applying case \emph{(ii)} of the inductive hypothesis, alongside exchange,
gives that $\Gamma, y: \ptp_1 \semi \Sigma \vdashe e[v/x]: \ptp_2$.
We conclude $\Gamma \semi \Sigma \vdashv (\lambda y.e) [v/x]: \ptp_1 \rightarrow \ptp_2$
by reapplication of \ts{TVal-Lam}.

\tb{Case} \ts{TVal-Tag}:
We have  $\Gamma, x: \ptp \semi \Sigma \vdashv 
\mf{tag}(v', \num{v_i}, \num{v_j}): \mf{View}$.
By inversion, $\Gamma, x: \ptp \semi \Sigma \vdashv v': \tstr$,
$\Gamma, x: \ptp \semi \Sigma \vdashv v_i: \mf{Attr}$,
and $\Gamma, x: \ptp \semi \Sigma \vdashv v_j: \mf{View}$
for all $i, j$.
We apply case \emph{(i)} of the inductive hypothesis to each, to obtain
$\Gamma \semi \Sigma \vdashv v'[v/x]: \tstr$,
$\Gamma \semi \Sigma \vdashv v_i[v/x]: \mf{Attr}$,
and $\Gamma \semi \Sigma \vdashv v_j[v/x]: \mf{View}$, respectively. 
By \ts{TVal-Tag} and definition of substitution on $\mf{tag}$,
we conclude $\Gamma \semi \Sigma \vdashv 
\mf{tag}(v', \num{v_i}, \num{v_j})[v/x]: \mf{View}$.
We elide the simpler but analogous cases for data constructors 
\textsf{text}, \textsf{attr}, and \textsf{onClick}.

\tb{Case} \ts{TExp-Ret}:
We have $\Gamma, x: \ptp \semi \Sigma \vdashe \eret{v'}: \ptp'$.
By inversion, $\Gamma, x: \ptp \semi \Sigma \vdashv v': \ptp'$.
Case \emph{(i)} of the inductive hypothesis gives $\Gamma \semi \Sigma \vdashv v'[v/x]: \ptp'$,
and reapplication \& definition of substitution let us conclude
$\Gamma \semi \Sigma \vdashe (\eret{v'})[v/x]: \ptp'$.

\tb{Case} \ts{TVal-Let}:
$\Gamma, x: \ptp \semi \Sigma \vdashe
\elet{y}{e_1}{e_2}: \ptp_2$. Inversion gives 
$\Gamma, x: \ptp \semi \Sigma \vdashe e_1: \ptp_1$.
We have two subcases, when $y=x$ and when not.
In either case, $\Gamma \semi \Sigma \vdashe e_1[v/x] : \ptp_1$
by inductive hypothesis.

If $y=x$, then $(\elet{y}{e_1}{e_2})[v/x]=\elet{y}{e_1[v/x]}{e_2}$.
We reapply \ts{TExp-Let} with the second premise obtained via inversion and
we are done. (Like the $\lambda$ case, we note the overwriting of $x: \ptp$ in
the second premise.)

If $y \neq x$, then we apply the inductive hypothesis to the second premise,
giving $\Gamma, x: \ptp, y: \ptp_1 \semi \Sigma \vdashe e_2: \ptp_2$, and
by inductive hypothesis and exchange, $\Gamma, y: \ptp_1 \vdashe e_2[v/x]: \ptp_2$.
Reapplication of \ts{TExpr-Let} and definition of substitution give
$\Gamma \semi \Sigma \vdashe (\elet{y}{e_1}{e_2})[v/x]: \ptp_2$.

We elide the remaining cases for expressions, which are analogous to the above.

\end{proof}

\subsubsection{Progress}

\begin{theorem}[Progress of expressions]
If $\cdot \semi \Sigma \vdashe e: \ptp$, then either:
\begin{enumerate}[label=(\roman*)]
\item $e = \eret{v}$, or
\item for all $\sigma$ such that $\cdot \semi \Sigma \vDash \sigma$, 
there exist some $e'$ and $\sigma'$ such that 
$\sigma \semi e \mapsto e' \semi \sigma'$.
\end{enumerate}
\end{theorem}

\begin{proof}
By induction on the derivation of the typing judgment.

\tb{Case} \ts{TExp-Ret}: 
We have $\cdot \semi \Sigma \vdashe \eret{v} \semi \ptp$.
The theorem holds trivially by case \emph{(i)}.

\tb{Case} \ts{TExp-Let}:
We have $\cdot \semi \Sigma \vdashe \elet{x}{e_1}{e_2}: \ptp_2$.
By inversion, $\cdot \semi \Sigma \vdashe e_1: \ptp_1$
and $x: \ptp_1 \semi \Sigma \vdashe e_2: \ptp_2$.
By inductive hypothesis on the first premise,
we have two subcases on $e_1$: either $e_1=\eret{v}$ 
or $\sigma \semi e_1 \mapsto e_1' \semi \sigma'$
(for $\cdot \semi \Sigma \vDash \sigma$).

If $e_1=\eret{v}$, then by \ts{E-Ret},
we have $\sigma, \elet{x}{\eret{v}}{e_2} \mapsto e_2[v/x] \semi \sigma$
for any $\sigma$.

If instead $\sigma \semi e_1 \mapsto e_1' \semi \sigma'$,
by \ts{E-Let}, we can conclude that 
$\sigma \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi \sigma'$.

Thus, in either subcase, $\elet{x}{e_1}{e_2}$ steps given a proper $\sigma$,
so the case holds.

\tb{Case} \ts{TExp-App}:
We have $\cdot \semi \Sigma \vdashe v_1\ v_2: \ptp_2$.
By inversion, $\cdot \semi \Sigma \vdashv v_1: \ptp_1 \rightarrow \ptp_2$
and $\cdot \semi \Sigma \vdashv v_2: \ptp_1$.
By the canonical forms lemma, $v_1=\lambda x.e$,
and so we apply rule \ts{E-Beta}, i.e.
$\sigma \semi (\lambda x.e) v_2 \mapsto e[v_2/x] \semi \sigma$ for
any $\sigma$ such that $\cdot \semi \Sigma \vDash \sigma$.

\tb{Case} \ts{TExp-If}:
We have $\cdot \semi \Sigma \vdashe \eif{v}{e_1}{e_2} : \ptp$.
By inversion, $\cdot \semi \Sigma \vdashv v: \mf{Bool}$,
and by canonical forms, either $v=\mf{true}$ or $v=\mf{false}$.
If $v=true$, then \ts{E-IfTrue} says 
$\sigma \semi \eif{v}{e_1}{e_2} \mapsto e_1 \semi \sigma$,
and if $v=false$, then $\sigma \semi \eif{v}{e_1}{e_2} \mapsto e_2 \semi \sigma$
by \ts{E-IfFalse}. In either case, we satisfy \emph{(ii)}.

\tb{Case} \ts{TExp-Ref}:
We have $\cdot \semi \Sigma \vdashe \eref{v} : \mf{Ref}\ \ptp$.
Immediately apply \ts{E-Ref} to get that 
$\sigma, \eref{v} \mapsto \eret{\ell} \semi \sigma[\ell \mapsto v]$,
which satisfies \emph{(ii)}.

\tb{Case} \ts{TExp-Get}:
We have $\cdot \semi \Sigma \vdashe \eget{v} : \ptp$.
By inversion, $\cdot \semi \Sigma \vdashv v: \mf{Ref}\ \ptp$,
and by canonical forms, $v=\ell$.
Again by inversion (on \ts{TVal-Loc}), $\ell: \ptp \in \Sigma$.
Since $\cdot \semi \Sigma \vDash \sigma$ for our arbitrary $\sigma$,
we have that $\sigma[\ell]$ exists.
Thus, we apply \ts{E-Get} to say
$\sigma \semi \eget{v} \mapsto \eret{\sigma[\ell]} \semi \sigma$.

\tb{Case} \ts{TExp-Set}:
We have $\cdot \semi \Sigma \vdashe \eset{v_1}{v_2}: \tone$.
By inversion, $\cdot \semi \Sigma \vdashv v_1: \mf{Ref}\ \ptp$,
and by canonical forms, $v=\ell$.
Applying \ts{E-Set} then gives 
$\sigma \semi \eset{v_1}{v_2} \mapsto \eret{()} \semi \sigma[\ell \mapsto v_2]$.

\end{proof}

\subsubsection{Preservation}

\begin{theorem}[Preservation of expressions]
If
\begin{enumerate}[label=(\roman*)]
\item $\Gamma \semi \Sigma \vdashe e: \ptp$,
\item $\Gamma \semi \Sigma \vDash \sigma$ for some $\sigma$, and
\item $\sigma \semi e \mapsto e' \semi \sigma'$ for some $e'$ and $\sigma'$,
\end{enumerate}
then there is some $\Sigma'$ such that:
\begin{itemize}
\item $\Sigma \sqsubseteq \Sigma'$,
\item $\Gamma \semi \Sigma' \vDash \sigma'$, and
\item $\Gamma \semi \Sigma' \vdashe e': \ptp$.
\end{itemize}
\end{theorem}

\begin{proof}
By induction on derivation of \emph{(iii)}.

\tb{Case} \ts{E-Let}:
We have $\sigma \semi \elet{x}{e_1}{e_2} \mapsto \elet{x}{e_1'}{e_2} \semi \sigma'$.
By inversion on \ts{E-Let}, we also have $\sigma \semi e_1 \mapsto e_1' \semi \sigma'$.
By inversion on \emph{(i)}, $\Gamma \semi \Sigma \vdashe e_1: \ptp_1$.
By inductive hypothesis, there exists some $\Sigma'$
with $\Gamma \semi \Sigma' \vDash \sigma'$
and $\Gamma \semi \Sigma' \vdashe e_1: \ptp$.
Since $\Sigma \sqsubseteq \Sigma'$, and $\Gamma, x: \ptp_1 \semi \Sigma \vdashe e_2: \ptp$,
we also get that $\Gamma, x: \ptp_1 \semi \Sigma' \vdashe e_2: \ptp_2$.
We apply \ts{E-Let} to get $\Gamma \semi \Sigma' \vdashe \elet{x}{e_1'}{e_2}: \ptp$.

\tb{Case} \ts{E-Ret}:
We have $\sigma \semi \elet{x}{\eret{v}}{e} \mapsto e[v/x] \semi \sigma$.
Inversion on \emph{(i)} gives that $\Gamma \semi \Sigma \vdashv v: \ptp_1$ and
$\Gamma, x: \ptp_1 \semi \Sigma \vdashe e: \ptp$. We conclude
$\Gamma \semi \Sigma \vdashe e[v/x]: \ptp$ via the substitution lemma.

\tb{Case} \ts{E-Beta}:
We have $\sigma \semi (\lambda x: \ptp_1.e)v \mapsto e[v/x] \semi \sigma$.
Inversion on \emph{(i)} gives $\Gamma, x: \ptp_1 \semi \Sigma \vdashe e: \ptp$
and $\Gamma \semi \Sigma \vdashv v: \ptp_1$. We again get
$\Gamma \semi \Sigma \vdashe e[v/x]: \ptp$ via the substitution lemma.

\tb{Case} \ts{E-IfTrue}:
We have $\sigma \semi \eif{\mf{true}}{e_1}{e_2} \mapsto e_1 \semi \sigma$.
Immediate by inversion on \emph{(i)}. Case for \ts{E-IfFalse} is analogous.

\tb{Case} \ts{E-Ref}:
We have $\sigma \semi \eref{v} \mapsto \eret{\ell} \semi \sigma[\ell \mapsto v]$
(for $\ell$ fresh).
By \emph{(i)} and enumeration of rules, 
$\Gamma \semi \Sigma \vdashe \eref{v}: \mf{Ref}\ \ptp$.
Inversion gives $\Gamma \semi \Sigma \vdashv v: \ptp$.

Choose $\Sigma' = \Sigma[\ell \mapsto \ptp]$; 
since $\ell$ is fresh, clearly $\Sigma \sqsubseteq \Sigma'$.
Since both $\Sigma'$ and $\sigma'$ are extensions of
$\Sigma$ and $\sigma$ by fresh label $\ell$, by definition of \emph{(ii)},
to show $\Gamma \semi \Sigma' \vDash \sigma'$,
it suffices to show $\cdot \semi \Sigma' \vdashv \sigma'[\ell]: \Sigma'[\ell]$.
Now since $\sigma'[\ell]=v$ and we said $\Sigma'[\ell] = \ptp$,
we need only apply the proposition from the previous paragraph.

We also need to show that $\Gamma \semi \Sigma' \vdashe \eret{\ell} : \ptp$.
This follows by \ts{TExp-Ret}, \ts{TVal-Loc}, and $\Sigma'[\ell] = \ptp$.

\tb{Case} \ts{E-Get}:
We have $\sigma \semi \eget{\ell} \mapsto \eret{\sigma[\ell]} \semi \sigma$.

Choose $\Sigma'=\Sigma$; 
trivially $\Gamma \semi \Sigma' \vDash \sigma$ and $\Sigma \sqsubseteq \Sigma'$.

By inversion on \emph{(i)}, $\Gamma \semi \Sigma \vdashv \ell: \mf{Ref}\ \ptp$
and $\ell: \ptp \in \Sigma$.
We can then apply \ts{TVal-Loc} and \ts{TExp-Ret}
to get $\Gamma \semi \Sigma \vdashe \eret{\ell}: \ptp$.

\tb{Case} \ts{E-Set}:
We have $\sigma \semi \eset{\ell}{v} \mapsto \eret{()} \semi \sigma[\ell \mapsto v]$.

Again choose $\Sigma'=\Sigma$. 
It is easy to show $\Gamma \semi \Sigma \vdashe \eret{()} : \tone$,
and by enumeration of rules and \emph{(i)} 
we have $\Gamma \semi \Sigma \vdashe \eset{\ell}{v}: \tone$.

We also need to show $\Gamma \semi \Sigma \vDash \sigma[\ell \mapsto v]$.
By \emph{(ii)}, this holds for all $\ell' \neq \ell$.
For $\ell$, we want to show $\cdot \semi \Sigma \vdashv \sigma'[\ell]: \Sigma[\ell]$,
i.e. that $\cdot \semi \Sigma \vdashv v: \Sigma[\ell]$.
Inversion on \emph{(i)} gives us both that $\Sigma[\ell] = \ptp$
and that $\Gamma \semi \Sigma \vdashv v: \ptp$.
\end{proof}

\subsubsection{Soundness}

\begin{theorem}[Type soundness for expressions]
If
\begin{itemize}
\item $\cdot \semi \Sigma \vdashe e: \ptp$,
\item $\Sigma \vDash \sigma$ for arbitrary $\sigma$,
\item $\sigma \semi e \mapsto^* e' \semi \sigma'$ for some $e'$ and $\sigma'$,
\item and $e' \not \mapsto$ (i.e. $e'$ cannot step further),
\end{itemize}
then $e' = \eret{v}$, and for some $\Sigma'$ with $\Sigma \subseteq \Sigma'$
and $\Sigma' \vDash \sigma'$, we get $\cdot \semi \Sigma' \vdashv v: \ptp$.
\end{theorem}

\begin{proof}
Follows directly from progess \& preservation.
\end{proof}

\begin{corollary}
If $\cdot \semi \cdot \vdashe e: \ptp$ 
and $\cdot \semi e \mapsto^* e' \semi \sigma$
(and $e' \not \mapsto$),
then $e' = \eret{v}$,
and for any $\Sigma$ with $\Sigma \vDash \sigma$,
we get $\cdot \semi \Sigma \vdashv v: \ptp$.
\end{corollary}

\subsection{Configuration Typing}



\section{Translation}

From React-tRace Core into $\lambda_{CC}$.

This requires the addition of (mutual) recursion into $\lambda_{CC}$,
which is entirely standard.

\subsection{Types}
Judgment: $\llrr{\ptp_{RTC}} = \ptp_{CC}$

\begin{align*}
\llrr{\tone} &= \tone\\
\llrr{\mf{Bool}} &= \mf{Bool}\\
\llrr{\mf{View}} &= \tone \rightarrow \mf{View}\\
\llrr{\mf{Attr}} &= \mf{Attr}\\
\llrr{\ptp_1 \xrightarrow{E} \ptp_2} &= \llrr{\ptp_1} \rightarrow \llrr{\ptp_2}\\
\llrr{\mf{Component}\ \ptp} &= \llrr{\ptp} \rightarrow \llrr{\mf{View}}\\
\llrr{\mf{Setter}\ \ptp} &= \llrr{\ptp} \rightarrow \tone
\end{align*}

\subsection{Values}

Judgment: $\llrr{v_{RTC}} = v_{CC}$
\begin{align*}
\llrr{x} &= x\\
\llrr{()} &= ()\\
\llrr{\mf{true}} &= \mf{true}\\
\llrr{\mf{false}} &= \mf{false}\\
\llrr{s} &= s\\
\llrr{\lambda x: \ptp . e} & = \lambda x: \llrr{\ptp}.\llrr{e}\\
\llrr{\mf{text}\ v} &= \mf{text}\ \llrr{v}\\
\llrr{\mf{tag}(v, \num{v_i}, \num{v_j})} 
&= \mf{tag}(\llrr{v}, \num{\llrr{v_i}}, \num{\llrr{v_j}})\\
\llrr{\mf{attr}(v_1, v_2)} &= \mf{attr}(\llrr{v_1}, \llrr{v_2})\\
\llrr{\mf{onClick}\ v} &= \mf{onClick}\ \llrr{v}\\
\llrr{\C} &= x_\C
\end{align*}

\subsection{Expressions}
Judgment: $\llrr{e_{RTC}} = e_{CC}$
\begin{align*}
\llrr{\eret{v}} &= \eret{\llrr{v}}\\
\llrr{\elet{x}{e_1}{e_2}} &= \elet{x}{\llrr{e_1}}{\llrr{e_2}}\\
\llrr{v_1\ v_2} &= \llrr{v_1}\ \llrr{v_2}\\
\llrr{\eif{v}{e_1}{e_2}} &= \eif{\llrr{v}}{\llrr{e_1}}{\llrr{e_2}}\\
\end{align*}

\subsection{Top-level Expressions}
Judgment: $\Theta \vdash \llrr{\hat{e}_{RTC}} = \hat{e}_{CC}$
where $\Theta ::= \cdot \mid (x, \ell), \Theta$

Also define $\Theta - x = \Theta \setminus \{x, \ell\}$ for any $\ell$
\begin{mathpar}
\inferrule*{
  \Theta - x \vdash \llrr{\hat{e}} = e'
} {
  \Theta \vdash \llrr{\elet{x}{e}{\hat{e}}} = \elet{x}{e}{e'}
}
\and
\inferrule*{
  \ell_x\ \mf{fresh}
  \and
  \Theta, (x, \ell_x) \vdash \llrr{\hat{e}} = e'
} {
  \Theta \vdash \llrr{\estate{x}{\ell}{v}{\hat{e}}} =\\
  \elet{\ell_x}{\eref{\llrr{v}}}{
    \elet{x_\mf{set}}{\lambda y: ?.\eset{\ell_x}{y}}{e'}
  }
}
\and
\inferrule*{
  \Theta \vdash \mf{deref}(\llrr{e}) = e'
} {
  \Theta \vdash \llrr{\hat{e}} = e'
}
\and
\inferrule*{
  \Theta \vdash \mf{deref}(e)
} {
  \Theta, (x, \ell_x) \vdash \mf{deref}(e) 
  = \elet{x}{\eget{\ell_x}}{e'}
}
\and
\inferrule*{ } {
  \cdot \vdash \mf{deref}(e) = e
}
\end{mathpar}

\subsection{Programs}
Judgment: $\Omega \vdash \llrr{P} = e$ 
where $\Omega = \cdot \mid \four{\C}{x}{\ptp}{\hat{e}}, \Omega$
\begin{mathpar}
\inferrule*{
  \Omega \vdash \llrr{P} = e'
} {
  \Omega \vdash \llrr{\elet{x}{e}{P}} = \elet{x}{\llrr{e}}{e'}
}
\and
\inferrule*{
  \Omega, \four{\C}{x}{\ptp}{\hat{e}} \vdash \llrr{P} = e'
} {
  \Omega \vdash \llrr{\elet{\C(x: \ptp)}{\hat{e}}{P}} = e'
}
\and
\inferrule*{
  \forall \four{\C_i}{x_i}{\ptp_i}{\hat{e}_i} \in \Omega
  \qquad
  \cdot \vdash \llrr{\hat{e}_i} = e_i
} {
  \Omega \vdash \erun{e} 
  = \eletrec{\num{\C_i: \llrr{\mf{Component}\ \ptp_i}}}
  {\num{\lambda x_i: \llrr{\ptp_i} . e_i}}{\llrr{e}}
}
\end{mathpar}

\end{document}